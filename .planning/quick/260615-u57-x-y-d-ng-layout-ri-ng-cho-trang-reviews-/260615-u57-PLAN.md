---
phase: quick-260615-u57
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
  - src/components/RatingBadge.astro
  - src/components/ProsConsBox.astro
  - src/components/ReviewCTA.astro
  - src/components/ReviewJumpNav.astro
  - src/components/review/CompanyReviewBody.astro
  - src/components/review/ComparisonBody.astro
  - src/components/review/ListicleBody.astro
  - src/layouts/ReviewLayout.astro
  - src/pages/[category]/[slug].astro
  - src/content/articles/review-cong-ty-chung-khoan-cho-nguoi-moi.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Visiting /reviews/review-cong-ty-chung-khoan-cho-nguoi-moi/ renders via ReviewLayout + ListicleBody, not the default ArticleLayout"
    - "The listicle page shows a ranked list of brokers (rank badge, rating, name, pros/cons, CTA) ordered by the rankings field"
    - "Existing non-review articles still render via ArticleLayout unchanged"
    - "astro check && astro build completes with zero errors"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Extended Zod schema with reviewType, companyInfo, accountSteps, comparisonCriteria, winnerByCriteria, bestForScenarios, rankings, brokerSchema.ctaUrl — all optional/defaulted"
      contains: "reviewType: z.enum"
    - path: "src/components/RatingBadge.astro"
      provides: "Reusable rating number + stars display"
    - path: "src/components/ProsConsBox.astro"
      provides: "Two-column pros/cons list"
    - path: "src/components/ReviewCTA.astro"
      provides: "CTA button, sticky on mobile"
    - path: "src/components/ReviewJumpNav.astro"
      provides: "Sticky anchor nav for review sections"
    - path: "src/components/review/CompanyReviewBody.astro"
      provides: "Body layout for reviewType=company"
    - path: "src/components/review/ComparisonBody.astro"
      provides: "Body layout for reviewType=comparison"
    - path: "src/components/review/ListicleBody.astro"
      provides: "Body layout for reviewType=listicle, ordered by rankings"
    - path: "src/layouts/ReviewLayout.astro"
      provides: "Dispatcher layout reusing ArticleLayout chrome, renders body by reviewType"
    - path: "src/pages/[category]/[slug].astro"
      provides: "Dispatch to ReviewLayout when layoutType === review"
      contains: "ReviewLayout"
    - path: "src/content/articles/review-cong-ty-chung-khoan-cho-nguoi-moi.md"
      provides: "Sample listicle article with reviewType, rankings, ctaUrl per broker"
      contains: "reviewType: listicle"
  key_links:
    - from: "src/pages/[category]/[slug].astro"
      to: "src/layouts/ReviewLayout.astro"
      via: "conditional render based on entry.data.layoutType"
      pattern: "layoutType.*review"
    - from: "src/layouts/ReviewLayout.astro"
      to: "src/components/review/ListicleBody.astro"
      via: "conditional render based on entry.data.reviewType"
      pattern: "reviewType.*listicle"
    - from: "src/components/review/ListicleBody.astro"
      to: "src/components/RatingBadge.astro"
      via: "import and render per ranked broker"
      pattern: "RatingBadge"
---

<objective>
Build a dedicated layout system for the Reviews category supporting 3 content types (company review, comparison, listicle): extend the content schema with review-specific fields, create shared review UI components (RatingBadge, ProsConsBox, ReviewCTA, ReviewJumpNav), build per-type body layouts (CompanyReviewBody, ComparisonBody, ListicleBody) plus a ReviewLayout dispatcher that reuses ArticleLayout's chrome (breadcrumb, FAQ, citations, author box, schema.org JSON-LD), wire routing dispatch in `[category]/[slug].astro`, and migrate the existing `review-cong-ty-chung-khoan-cho-nguoi-moi.md` article to `reviewType: listicle` as the reference example.

Purpose: Reviews currently render through the generic ArticleLayout which doesn't surface ratings, pros/cons, CTAs, or ranked comparisons — the core trust/conversion signals for YMYL broker review content.
Output: Extended `src/content.config.ts` schema, 4 shared components, 3 body layouts, 1 dispatcher layout, updated routing, and a migrated sample article rendering end-to-end via `astro build`.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/nguyenvietloc/Documents/investing/src/content.config.ts
@/Users/nguyenvietloc/Documents/investing/src/layouts/ArticleLayout.astro
@/Users/nguyenvietloc/Documents/investing/src/components/ComparisonTable.astro
@/Users/nguyenvietloc/Documents/investing/src/components/KeyTakeaways.astro
@/Users/nguyenvietloc/Documents/investing/src/pages/[category]/[slug].astro
@/Users/nguyenvietloc/Documents/investing/src/content/articles/review-cong-ty-chung-khoan-cho-nguoi-moi.md
@/Users/nguyenvietloc/Documents/investing/src/data/site.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend content schema (brokerSchema.ctaUrl + review-type fields)</name>
  <files>src/content.config.ts</files>
  <action>
  In `src/content.config.ts`, add `ctaUrl: z.string().optional()` to `brokerSchema` (after `logo`).

  In the `articles` collection schema, add the following new optional fields after the existing `reviewSummary` field, all optional and non-breaking for existing articles:
  - `reviewType: z.enum(["company", "comparison", "listicle"]).optional()`
  - `companyInfo: z.object({ founded: z.string().optional(), hq: z.string().optional(), license: z.string().optional(), website: z.string().optional(), products: z.array(z.string()).default([]) }).optional()`
  - `accountSteps: z.array(z.object({ title: z.string(), desc: z.string() })).default([]).optional()` — use `.default([]).optional()` so it resolves to `[]` when omitted but the field itself stays optional in the type (consistent with other array defaults in this file); if Zod complains about combining default+optional, drop `.optional()` and keep `.default([])` since default already makes it omittable in YAML.
  - `comparisonCriteria: z.array(z.object({ label: z.string(), key: z.string() })).default([])`
  - `winnerByCriteria: z.record(z.string(), z.string()).default({})`
  - `bestForScenarios: z.array(z.object({ scenario: z.string(), winnerSlug: z.string(), reason: z.string() })).default([])`
  - `rankings: z.array(z.object({ slug: z.string(), rank: z.number(), badge: z.string() })).default([])`

  For array/record fields prefer `.default([])` / `.default({})` over `.optional()` (matches the existing pattern for `tags`, `faq`, `sources`, `citations` in this file) so downstream components can safely `.map()`/iterate without undefined checks. Only `companyInfo` (a singular object) should be `.optional()` with no default.

  Do not modify any other existing fields. Run `astro check` after editing to confirm no schema errors against existing content.
  </action>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>Schema compiles, astro check reports no new errors related to content.config.ts, and all new fields are optional/defaulted so existing articles remain valid.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared review components (RatingBadge, ProsConsBox, ReviewCTA, ReviewJumpNav)</name>
  <files>src/components/RatingBadge.astro, src/components/ProsConsBox.astro, src/components/ReviewCTA.astro, src/components/ReviewJumpNav.astro</files>
  <action>
  Create 4 new components in `src/components/`, following the existing conventions seen in `KeyTakeaways.astro`, `Breadcrumb.astro`, and `ComparisonTable.astro` (Props interface in frontmatter, scoped `<style>` block at bottom, `var(--token)` design tokens: `--brand`, `--text`, `--muted`, `--line`, `--surface`, `--surface-alt`, `--color-mint`, `--space-*`, `--shadow-sm/md/lg`, `--transition-fast`, `--font-serif`, sharp `border-radius: 0px` aesthetic with `box-shadow` offsets).

  **RatingBadge.astro**
  - Props: `{ rating: number; label?: string }`
  - Render a large numeric rating (e.g. "4.7") plus a row of 5 star characters (filled/empty based on `rating`, supporting half-stars via a partial-fill approach using two overlapping spans or simple rounding to nearest 0.5 — keep it simple: round to nearest integer for star count if half-star CSS is too complex, but display the precise numeric rating regardless).
  - If `label` provided, render it as a small caption under/beside the rating (e.g. "Đánh giá tổng thể").
  - Style: bordered box with `box-shadow`, `--brand` accent color for stars, matches the bordered/offset-shadow aesthetic of `.key-takeaways` / `.eyebrow`.

  **ProsConsBox.astro**
  - Props: `{ pros: string[]; cons: string[] }`
  - Render a two-column grid (collapsing to 1 column on mobile via `@media (max-width: 580px)`): left column "Ưu điểm" (pros, green/mint accent), right column "Nhược điểm" (cons, peach/warning accent). Use `--color-mint` and `--color-peach` (consistent with existing eyebrow/ymyl-note usage) for column backgrounds or left-border accents.
  - Each list item gets a `::before` marker dot like `KeyTakeaways.astro`'s `.key-takeaways li::before` pattern.

  **ReviewCTA.astro**
  - Props: `{ label: string; url: string; sticky?: boolean }`
  - Render an `<a>` styled as a prominent button: `background: var(--brand)`, white text, bold, `border: 1px solid var(--line)`, `box-shadow: 2px 2px 0px var(--line)`, hover state translates and removes shadow (matches `.article-trust span:hover` pattern).
  - If `sticky` is true, add a `sticky-cta` class that on mobile (`@media (max-width: 580px)`) fixes the CTA to the bottom of the viewport with `position: fixed; bottom: 0; left: 0; right: 0; z-index: 50;` and full-width button, with a top border and `--shadow-lg`. On desktop the sticky class has no special positioning (renders inline).
  - `target="_blank" rel="noopener sponsored nofollow"` on the anchor (broker affiliate links — nofollow/sponsored per SEO convention for monetized outbound links).

  **ReviewJumpNav.astro**
  - Props: `{ items: { label: string; href: string }[] }`
  - Render a `<nav>` with horizontal (desktop) / horizontally-scrollable (mobile) list of anchor links (`href="#section-id"`), styled as a sticky bar: `position: sticky; top: 0` (or below header height), `background: var(--surface)`, `border-bottom: 1px solid var(--line)`, `box-shadow: var(--shadow-sm)`, `z-index` below header. Each link styled similar to `.breadcrumb a` (bold, brand color on hover, underline offset).
  - This replaces TOC sidebar for review pages — keep it simple, no active-section JS highlighting required (can be added later, not in scope).
  </action>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npx astro check 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 component files exist with valid Astro syntax (Props interface + scoped style), astro check passes with no type errors in these files.</done>
</task>

<task type="auto">
  <name>Task 3: Build review body layouts and ReviewLayout dispatcher</name>
  <files>src/components/review/CompanyReviewBody.astro, src/components/review/ComparisonBody.astro, src/components/review/ListicleBody.astro, src/layouts/ReviewLayout.astro</files>
  <action>
  Create directory `src/components/review/` with 3 body layout components, plus `src/layouts/ReviewLayout.astro`. All body components receive `entry.data` fields as individual props (not a single `entry` blob) so they're testable/composable.

  **CompanyReviewBody.astro** (`src/components/review/CompanyReviewBody.astro`)
  - Props: `{ companyInfo?: { founded?: string; hq?: string; license?: string; website?: string; products: string[] }; accountSteps: { title: string; desc: string }[]; pros: string[]; cons: string[]; rating: number; ctaUrl?: string; ctaLabel?: string }` — note: for `reviewType: company`, the single broker's data comes from the FIRST entry of the `brokers` array (the page using this component should pass `entry.data.brokers?.[0]` fields plus top-level `companyInfo`/`accountSteps`).
  - Layout, in order:
    1. Hero: `<RatingBadge rating={rating} label="Đánh giá tổng thể" />`
    2. "Thông tin nhanh" section (id="thong-tin-nhanh"): a simple definition-list/table of `companyInfo` (founded, hq, license, website) — render only fields that are present.
    3. "Sản phẩm & dịch vụ" section (id="san-pham"): grid/list of `companyInfo.products`.
    4. "Hướng dẫn mở tài khoản" section (id="mo-tai-khoan"): numbered stepper rendering `accountSteps` (title + desc per step, numbered with CSS counters or explicit index).
    5. `<ProsConsBox pros={pros} cons={cons} />`
    6. `<ReviewCTA label={ctaLabel ?? "Mở tài khoản ngay"} url={ctaUrl ?? "#"} sticky={true} />`
  - Import `RatingBadge`, `ProsConsBox`, `ReviewCTA` from `@/components/...`.

  **ComparisonBody.astro** (`src/components/review/ComparisonBody.astro`)
  - Props: `{ brokers: BrokerSchema[]; comparisonCriteria: { label: string; key: string }[]; winnerByCriteria: Record<string,string>; bestForScenarios: { scenario: string; winnerSlug: string; reason: string }[] }`
  - Layout:
    1. "So sánh nhanh" section (id="so-sanh"): build a `<ComparisonTable>` where `columns` = `["Tiêu chí", ...brokers.map(b => b.name)]` and `rows` = one row per `comparisonCriteria` item, with cell values looked up dynamically (since criteria.key won't map directly to typed broker fields, do a best-effort lookup: for known keys like `fee_rate`, `margin_rate`, `app_rating`, `min_deposit`, `rating`, index into the broker object via `(broker as any)[criteria.key]`, falling back to `"-"` if undefined). Highlight the winner cell for each criterion using `winnerByCriteria[criteria.key]` (matches a broker `slug`) — pass a `highlightCol` index or wrap winner cell text, e.g. render winner cells with a `<strong class="winner-cell">` wrapper. Since `ComparisonTable.astro` currently takes plain `rows: (string|number)[][]`, either (a) extend `ComparisonTable.astro` to accept an optional `winnerCols?: number[]` per row for highlighting, or (b) pre-format winner cell values as JSX-safe strings is not possible for styling — prefer (a): add an optional prop `highlightMap?: Record<number, number[]>` (rowIndex -> array of winning column indices) to `ComparisonTable.astro`, and in its template add a `winner-cell` class to matching `<td>` elements with a `--brand`-tinted background. Keep this extension backward-compatible (optional prop, default `{}`).
    2. "Ưu & nhược điểm từng bên" section (id="uu-nhuoc-diem"): for each broker, render `<ProsConsBox pros={broker.pros} cons={broker.cons} />` inside a labeled card with the broker name as heading, laid out side-by-side in a responsive grid (`grid-template-columns: repeat(auto-fit, minmax(280px, 1fr))`).
    3. "Nên chọn ai?" section (id="nen-chon-ai"): render `bestForScenarios` as a list — for each scenario, show `scenario` text, the winning broker's name (look up `brokers.find(b => b.slug === winnerSlug)?.name`), and `reason`. Each scenario item gets a `<ReviewCTA>` linking to the winning broker's `ctaUrl` if present.
  - Import `ComparisonTable`, `ProsConsBox`, `ReviewCTA` from `@/components/...`.

  **ListicleBody.astro** (`src/components/review/ListicleBody.astro`)
  - Props: `{ brokers: BrokerSchema[]; rankings: { slug: string; rank: number; badge: string }[]; comparisonCriteria: { label: string; key: string }[] }`
  - Layout:
    1. "Tổng quan so sánh" section (id="tong-quan"): a `<ComparisonTable>` overview row per broker (columns from `comparisonCriteria` similar to ComparisonBody, but no winner highlighting needed for listicle — or reuse the same extended prop, passing empty `highlightMap`).
    2. "Bảng xếp hạng chi tiết" section (id="xep-hang"): sort `brokers` by matching `rankings` entry's `rank` ascending (brokers without a ranking entry sort last). For each ranked broker render a "RankedCard": rank number badge (large styled numeral, e.g. "#1"), `<RatingBadge rating={broker.rating} />`, broker name as heading, the `badge` string from rankings (e.g. "Tốt nhất cho người mới") as a tag/pill, condensed pros/cons (`<ProsConsBox pros={broker.pros} cons={broker.cons} />`), and `<ReviewCTA label={`Mở tài khoản ${broker.name}`} url={broker.ctaUrl ?? "#"} />`. Each card is a bordered block with `box-shadow`, rank #1 gets a visually distinct accent (e.g. `--brand` border or a "winner" eyebrow tag).
  - Import `ComparisonTable`, `RatingBadge`, `ProsConsBox`, `ReviewCTA` from `@/components/...`.

  **ReviewLayout.astro** (`src/layouts/ReviewLayout.astro`)
  - Mirror `ArticleLayout.astro`'s structure closely: same imports (`BaseLayout`, `AuthorBox`, `RelatedArticles`, `ShareBar`, `Breadcrumb`, `KeyTakeaways`, `CitationBox`, `categories`/`getCategoryPath`/`site` from `@/data/site`, `author` from `@/data/authors`), same Props (`{ entry: any; headings?: {...}[] }`), same breadcrumb logic, same `articleSchema`/`faqSchema`/`breadcrumbSchema` JSON-LD blocks (identical — reuse verbatim), same header block (eyebrow, h1, lead, meta-block, trust badges), same hero image block.
  - DIFFERENCE 1: Replace the `TOC` sidebar with `ReviewJumpNav`. Build `navItems` based on `entry.data.reviewType`:
    - `company`: `[{label: "Thông tin nhanh", href: "#thong-tin-nhanh"}, {label: "Sản phẩm", href: "#san-pham"}, {label: "Mở tài khoản", href: "#mo-tai-khoan"}, {label: "Ưu nhược điểm", href: "#uu-nhuoc-diem"}]`
    - `comparison`: `[{label: "So sánh", href: "#so-sanh"}, {label: "Ưu nhược điểm", href: "#uu-nhuoc-diem"}, {label: "Nên chọn ai", href: "#nen-chon-ai"}]`
    - `listicle`: `[{label: "Tổng quan", href: "#tong-quan"}, {label: "Xếp hạng", href: "#xep-hang"}]`
    - default/fallback (no reviewType): empty array — render `ReviewJumpNav` only if `navItems.length > 0`.
  - DIFFERENCE 2: After the hero image and before/around `<slot />`, render the appropriate body component based on `entry.data.reviewType`:
    - `"company"`: `<CompanyReviewBody companyInfo={entry.data.companyInfo} accountSteps={entry.data.accountSteps ?? []} pros={entry.data.brokers?.[0]?.pros ?? []} cons={entry.data.brokers?.[0]?.cons ?? []} rating={entry.data.brokers?.[0]?.rating ?? 0} ctaUrl={entry.data.brokers?.[0]?.ctaUrl} />`
    - `"comparison"`: `<ComparisonBody brokers={entry.data.brokers ?? []} comparisonCriteria={entry.data.comparisonCriteria ?? []} winnerByCriteria={entry.data.winnerByCriteria ?? {}} bestForScenarios={entry.data.bestForScenarios ?? []} />`
    - `"listicle"`: `<ListicleBody brokers={entry.data.brokers ?? []} rankings={entry.data.rankings ?? []} comparisonCriteria={entry.data.comparisonCriteria ?? []} />`
    - none of the above (reviewType undefined but layoutType === "review"): render nothing extra — fall back to plain `<slot />` only (keeps old-style review articles without reviewType working).
  - Keep `<slot />` (markdown body) rendering AFTER the body component, inside the same `<article class="prose article-content">` wrapper, followed by FAQ section, CitationBox, editorial-review section, AuthorBox, ShareBar, RelatedArticles — identical to ArticleLayout.
  - Re-use the same `<style>` block content as `ArticleLayout.astro` (copy it), since the chrome classes (`.article-header`, `.article-hero`, `.article-body`, `.editorial-review`, etc.) are identical. Add new style rules only for the jump-nav slot replacing `.toc-sidebar` if needed (the `ReviewJumpNav` component is self-styled, so the `.article-body--toc`/`.toc-sidebar` grid layout can be simplified to single-column `.article-body` for review pages — i.e., do NOT use the 2-column TOC grid; render `ReviewJumpNav` as a full-width sticky bar above `.article-body` instead of a sidebar).
  </action>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npx astro check 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 files exist, astro check passes with no type errors, ReviewLayout correctly dispatches to one of the 3 body components based on reviewType and falls back gracefully when reviewType is absent.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire routing dispatch and migrate sample article to reviewType: listicle</name>
  <files>src/pages/[category]/[slug].astro, src/content/articles/review-cong-ty-chung-khoan-cho-nguoi-moi.md</files>
  <action>
  **Routing** (`src/pages/[category]/[slug].astro`):
  - Import `ReviewLayout` from `@/layouts/ReviewLayout.astro`.
  - After computing `{ Content, headings }`, conditionally render: if `entry.data.layoutType === "review"`, render `<ReviewLayout entry={entry} headings={headings}><Content /></ReviewLayout>`; otherwise (default `"article"`), render `<ArticleLayout entry={entry} headings={headings}><Content /></ArticleLayout>` as before. Use a simple ternary or `{condition ? (<A/>) : (<B/>)}` pattern in the template.
  - Check `src/pages/dau-tu/[category]/[slug].astro` (sibling route file) — if it has the same ArticleLayout rendering pattern, apply the identical dispatch there too for consistency, since reviews could theoretically live under either tree (though `reviews` category currently maps to the non-dau-tu tree per `src/data/site.ts` group `"Reviews"`). Confirm via `src/data/site.ts` that `reviews` category's `group` is `"Reviews"` (not `"Đầu tư"` or `"Phân tích"`), meaning it's only generated by `src/pages/[category]/[slug].astro` — if confirmed, skip editing the `dau-tu` variant, but still verify by reading that file's `getStaticPaths` filter logic.

  **Migrate sample article** (`src/content/articles/review-cong-ty-chung-khoan-cho-nguoi-moi.md`):
  - Add `reviewType: listicle` to frontmatter (after `layoutType: review`).
  - Add `rankings:` array mapping the 4 existing brokers to ranks 1-4 based on their `rating` values (already ordered: tcbs 4.7, ssi 4.5, vps 4.2, vndirect 3.9 — so rank 1=tcbs, 2=ssi, 3=vps, 4=vndirect). Give each a short `badge` string derived from their existing `best_for` field, e.g.:
    - `{slug: "tcbs", rank: 1, badge: "Phí thấp nhất"}`
    - `{slug: "ssi", rank: 2, badge: "An toàn & uy tín nhất"}`
    - `{slug: "vps", rank: 3, badge: "Hỗ trợ môi giới tốt nhất"}`
    - `{slug: "vndirect", rank: 4, badge: "Giao diện thân thiện nhất"}`
  - Add `ctaUrl` to each broker entry: tcbs → `https://tcbs.com.vn`, ssi → `https://www.ssi.com.vn`, vps → `https://www.vps.com.vn`, vndirect → `https://www.vndirect.com.vn`.
  - Optionally add a minimal `comparisonCriteria` array (e.g. `[{label: "Phí giao dịch", key: "fee_rate"}, {label: "Lãi suất margin", key: "margin_rate"}, {label: "Đánh giá app", key: "app_rating"}]`) so the "Tổng quan so sánh" ComparisonTable in ListicleBody has real columns to render — this is optional polish but recommended since ListicleBody renders this table.
  - Do not remove or alter the existing markdown body content, FAQ, citations, keyTakeaways, or other fields.

  After edits, run a full build to confirm end-to-end rendering.
  </action>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npx astro check && npx astro build 2>&1 | tail -40</automated>
  </verify>
  <done>astro check && astro build complete with zero errors; the built output for /reviews/review-cong-ty-chung-khoan-cho-nguoi-moi/ includes ListicleBody-rendered markup (ranked cards, RatingBadge, ReviewCTA elements); other existing articles still build via ArticleLayout unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content frontmatter -> Astro build | Markdown/YAML frontmatter authored by content team feeds Zod-validated schema; build-time only, no runtime user input |
| ReviewCTA -> external broker sites | Outbound affiliate/CTA links to third-party broker domains (tcbs.com.vn, ssi.com.vn, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260615-01 | Tampering | content.config.ts schema | accept | Build-time Zod validation only; no runtime user input crosses this boundary, low risk |
| T-quick260615-02 | Information Disclosure | ReviewCTA outbound links | mitigate | Apply `rel="noopener sponsored nofollow"` and `target="_blank"` to prevent referrer leakage and signal monetized links to search engines per SEO convention |
| T-quick260615-03 | Tampering | ComparisonTable highlightMap extension | accept | New optional prop with default `{}`, purely presentational, no injection vector (data sourced from validated frontmatter) |
</threat_model>

<verification>
1. `npx astro check` passes with zero type errors after each task.
2. `npx astro build` completes with zero errors at the end of Task 4.
3. Inspect built HTML for `/reviews/review-cong-ty-chung-khoan-cho-nguoi-moi/` (in `dist/`) to confirm presence of ranked card markup, RatingBadge stars, ProsConsBox columns, and ReviewCTA buttons with broker URLs.
4. Spot-check one existing non-review article (e.g. `co-phieu-la-gi`) still builds with ArticleLayout (no ReviewJumpNav/body components present in its output).
</verification>

<success_criteria>
- Schema extended with all 7 new review fields + `brokerSchema.ctaUrl`, all optional/defaulted, zero breakage to existing 50+ articles.
- 4 shared components (RatingBadge, ProsConsBox, ReviewCTA, ReviewJumpNav) created following existing scoped-style + design-token conventions.
- 3 body layouts (CompanyReviewBody, ComparisonBody, ListicleBody) + ReviewLayout dispatcher created, reusing ArticleLayout's chrome (breadcrumb, FAQ, citations, schemas, author box).
- Routing dispatches `layoutType === "review"` entries to ReviewLayout.
- Sample article migrated to `reviewType: listicle` with rankings, badges, and ctaUrl per broker.
- `astro check && astro build` passes cleanly.
</success_criteria>

<output>
Create `.planning/quick/260615-u57-x-y-d-ng-layout-ri-ng-cho-trang-reviews-/260615-u57-SUMMARY.md` when done
</output>
