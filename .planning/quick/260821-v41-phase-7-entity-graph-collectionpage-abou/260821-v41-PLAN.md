---
phase: quick-260821-v41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/schema.ts
  - src/data/authors.ts
  - src/pages/[category].astro
  - src/pages/dau-tu/[category].astro
  - src/pages/phan-tich/[category].astro
  - src/pages/dau-tu.astro
  - src/pages/phan-tich.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/contact.astro
  - src/pages/author/[slug].astro
  - src/pages/editorial-policy.astro
  - src/pages/corrections-policy.astro
  - src/pages/sources-policy.astro
  - src/pages/disclaimer.astro
  - src/layouts/BaseLayout.astro
  - .planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs
  - .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs
  - .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/baseline.txt
autonomous: true
requirements: []

must_haves:
  truths:
    - "MỌI trang thật trong dist/ (trừ redirect stub) chỉ phát ĐÚNG MỘT khối <script type=\"application/ld+json\"> — không còn trang nào phát 2 khối"
    - "8 trang category listing (dau-tu/co-phieu, dau-tu/etf, dau-tu/trai-phieu, dau-tu/phai-sinh, phan-tich/co-ban, phan-tich/ky-thuat, reviews, nha-dau-tu) có node CollectionPage @id {pageUrl}#webpage nối isPartOf → #website, breadcrumb → #breadcrumb, mainEntity → #itemlist"
    - "BreadcrumbList trên trang listing có @id và được node #webpage trỏ tới — không còn mồ côi như trước"
    - "ItemList liệt kê đúng tập item đang hiển thị trên trang, mỗi ListItem có position + name + url tuyệt đối"
    - "2 trang group (dau-tu/, phan-tich/) có CollectionPage + BreadcrumbList + ItemList liệt kê các category con"
    - "Trang chủ có node WebPage @id {siteUrl}#webpage với isPartOf → #website, about → #organization, mainEntity → ItemList các bài 'Bắt đầu tại đây' đang hiển thị"
    - "about/ có node AboutPage với mainEntity là reference thuần {\"@id\": orgId}; contact/ có node ContactPage"
    - "Node Organization có contactPoint (email + url trang liên hệ) và serialize GIỐNG NHAU trên mọi trang"
    - "author/nguyen-viet-loc/ có node ProfilePage với mainEntity → {\"@id\": personId}; node Person chỉ được định nghĩa theo MỘT bộ thuộc tính duy nhất (không còn bản Person trần từ slot head)"
    - "Node Person có worksFor → {\"@id\": orgId} (chiều ngược Person↔Organization) và mainEntityOfPage là chuỗi URL (không phải reference @id)"
    - "4 trang policy (editorial-policy, corrections-policy, sources-policy, disclaimer) có node WebPage @id {pageUrl}#webpage + isPartOf + breadcrumb"
    - "verify-graph.mjs chạy PASS trên TOÀN BỘ trang non-stub trong dist/ (article + listing + static), exit 0"
    - "Số trang bài viết PASS và tổng số trang non-stub bằng đúng baseline đo trước khi sửa (không mất/không thêm trang)"
    - "npm run build (astro check && astro build) xanh, 0 errors"
  artifacts:
    - path: "src/lib/schema.ts"
      provides: "Builder cho trang listing/tĩnh/profile + enrich Person & Organization"
      exports: ["buildCollectionPageNodes", "buildStaticPageNodes", "buildProfilePageNodes", "ListingItem", "CollectionPageInput", "StaticPageInput", "ProfilePageInput"]
      contains: "buildCollectionPageNodes"
      min_lines: 400
    - path: ".planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs"
      provides: "Gate Phase 7: chạy verify-graph.mjs trên toàn bộ trang non-stub + kiểm 18 trang in-scope có đúng node page-level"
    - path: ".planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/baseline.txt"
      provides: "Số trang bài viết + tổng trang non-stub đo TRƯỚC khi sửa, dùng làm regression gate"
      contains: "articles="
    - path: "src/pages/dau-tu/[category].astro"
      provides: "CollectionPage nodes qua prop schemaNodes, bỏ Fragment slot=head"
      contains: "buildCollectionPageNodes"
    - path: "src/pages/author/[slug].astro"
      provides: "ProfilePage nodes qua prop schemaNodes, bỏ script slot=head chứa Person trần"
      contains: "buildProfilePageNodes"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Bỏ <slot name=\"head\" /> — chặn khối ld+json thứ 2 quay lại"
  key_links:
    - from: "src/pages/[category].astro"
      to: "src/lib/schema.ts"
      via: "buildCollectionPageNodes → schemaNodes prop"
      pattern: "buildCollectionPageNodes"
    - from: "src/pages/index.astro"
      to: "src/lib/schema.ts"
      via: "buildCollectionPageNodes({ pageType: 'WebPage', about: true })"
      pattern: "schemaNodes=\\{"
    - from: "src/pages/about.astro"
      to: "src/lib/schema.ts"
      via: "buildStaticPageNodes({ pageType: 'AboutPage', mainEntityId: orgId(siteUrl) })"
      pattern: "buildStaticPageNodes"
    - from: "src/pages/author/[slug].astro"
      to: "src/lib/schema.ts"
      via: "buildProfilePageNodes → schemaNodes prop"
      pattern: "buildProfilePageNodes"
    - from: ".planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs"
      to: ".planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs"
      via: "spawnSync node verify-graph.mjs <toàn bộ trang non-stub>"
      pattern: "verify-graph\\.mjs"
---

<objective>
Phase 7 Entity Graph: đưa TẤT CẢ trang không phải bài viết (8 category listing, 2 group listing, trang chủ, about, contact, author profile, 4 trang policy) về đúng một `@graph` duy nhất qua prop `schemaNodes` — dùng lại kiến trúc Phase 1+2 đã có.

Purpose: Hiện 9 trang (8 category + author) còn phát khối `ld+json` thứ 2 qua `slot="head"`, trong đó `BreadcrumbList` không có `@id` và không node nào trỏ tới (mồ côi hoàn toàn), còn node `Person` trên trang tác giả bị định nghĩa hai kiểu khác nhau cùng một `@id`. 9 trang còn lại (trang chủ, 2 group, about, contact, 4 policy) hoàn toàn không có node page-level — Google không có entity nào để gắn các trang tín hiệu tin cậy YMYL này vào graph của brand.

Output: 3 builder mới trong `src/lib/schema.ts`, 13 trang được wire lại, `<slot name="head" />` bị gỡ khỏi BaseLayout, và một gate verify chạy trên TOÀN BỘ `dist/`.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/lib/schema.ts
@src/layouts/BaseLayout.astro
@src/data/site.ts
@src/data/authors.ts
@src/pages/[category].astro
@src/pages/dau-tu/[category].astro
@src/pages/author/[slug].astro
@src/pages/index.astro
@.planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/260821-umf-SUMMARY.md
@.planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs
</context>

<decisions>
Các quyết định kiến trúc đã chốt khi lập kế hoạch — executor thực hiện đúng, không tự đổi:

- **D-01 — Trang chủ dùng `WebPage`, KHÔNG dùng `CollectionPage`.** Trang chủ là hub hỗn hợp (hero + khối "Bắt đầu tại đây" + preview theo từng category + EeatBanner), không phải trang liệt kê một collection duy nhất. `WebPage` + `about → #organization` là tín hiệu brand mạnh nhất đặt trên URL gốc; `mainEntity → ItemList` vẫn giữ được danh sách bài nổi bật đang hiển thị.
- **D-02 — `contactPoint` gắn vào node Organization trong `buildSiteNodes` (áp dụng mọi trang), KHÔNG chỉ trên trang contact.** Lý do: cùng một `@id` phải serialize giống hệt nhau ở mọi trang. Định nghĩa hai bộ thuộc tính khác nhau cho cùng `@id` chính là lỗi P0-1 đã phải sửa ở Phase 1+2. Chi phí: ~120 byte/trang.
- **D-03 — `Person.mainEntityOfPage` là CHUỖI URL, không phải `{"@id": ...}`.** Trên trang bài viết không tồn tại node `#webpage` của trang tác giả, nên reference dạng `@id` sẽ dangling và verifier FAIL. Chuỗi URL vẫn hợp lệ schema.org.
- **D-04 — `alumniOf` lấy từ field mới `author.alumniOf` trong `src/data/authors.ts`, KHÔNG parse chuỗi `author.education`.** `education` là câu ghép "Cử nhân Quản trị Kinh doanh Học viện Chính sách và Phát triển"; tách bằng heuristic là mong manh. Bỏ `hasOccupation` (trùng nghĩa `jobTitle`, không thêm entity nào resolve được).
- **D-05 — ProfilePage giữ `mainEntity → Person`; danh sách bài của tác giả treo vào `hasPart → #itemlist`.** Giữ đúng scope 7f, đồng thời ItemList không mồ côi.
- **D-06 — ItemList luôn khớp đúng tập item đang render trong DOM của trang đó**, kèm `numberOfItems`. Trang category render toàn bộ bài của category (featured + rest, phần dư ẩn bằng CSS), trang author render toàn bộ bài (ẩn từ bài thứ 10) → ItemList liệt kê đủ.
- **D-07 — Gỡ `<slot name="head" />` khỏi BaseLayout** sau khi 4 consumer cuối cùng biến mất. Đây là bảo đảm cấu trúc cho bất biến "1 khối ld+json/trang"; grep xác nhận không còn nơi nào dùng.
- **D-08 — Loại trừ redirect stub bằng `grep -rL 'http-equiv="refresh"' dist --include='*.html'`.** Đo tại thời điểm lập kế hoạch: 276 file HTML = 130 stub (toàn bộ `dist/kien-thuc/*`, `dist/co-phieu`, `dist/etf`, `dist/trai-phieu`, `dist/phai-sinh`, `dist/quy-dau-tu`, `dist/dau-tu/quy-dau-tu`) + 146 trang thật (124 bài viết + 22 trang khác).
- **D-09 — `verify-graph.mjs` được siết chặt chứ không nới lỏng.** Bộ 4 node bắt buộc (`#webpage/#breadcrumb/#primaryimage/#article`) chỉ áp dụng cho trang có node `Article` — tức 124 trang bài viết giữ nguyên y hệt gate cũ. Trang không phải bài viết nhận thêm check mới về hình dạng `#webpage`/`#itemlist`/`#breadcrumb`.
- **D-10 — 4 trang non-stub NGOÀI scope Phase 7:** `404.html`, `search/`, `so-do-trang/`, `preview/eeat-components/`. Ba trang đầu là tiện ích/điều hướng, trang cuối là harness nội bộ. Chúng vẫn phải PASS gate cấu trúc (1 khối ld+json, graph liên thông) nhưng không bắt buộc có node `#webpage`. `so-do-trang/` là ứng viên `CollectionPage` hợp lý cho phase sau — ghi nhận, không làm ở đây.
- **D-11 — Breadcrumb JSON-LD vẫn phát trên about/contact/policy/author dù các trang này chưa có breadcrumb hiển thị.** Đúng theo scope 7d/7g; trail 2 cấp khớp với cấu trúc URL và link ở footer. Không thêm UI breadcrumb (ngoài scope).
</decisions>

<source_coverage>
| Nguồn (task scope) | Nội dung | Task | Trạng thái |
|---|---|---|---|
| 7a | CollectionPage + BreadcrumbList + ItemList cho 8 trang category, bỏ Fragment slot=head | T1 (builder) + T2 (wire) | COVERED |
| 7b | CollectionPage cho dau-tu.astro, phan-tich.astro (ItemList = category con) | T1 + T2 | COVERED |
| 7c | WebPage cho trang chủ + about → #organization + mainEntity ItemList | T1 + T2 (D-01) | COVERED |
| 7d | AboutPage + mainEntity reference thuần tới Organization | T1 + T3 | COVERED |
| 7e | ContactPage + quyết định vị trí contactPoint | T1 (D-02) + T3 | COVERED |
| 7f | ProfilePage, bỏ Person trần, thêm worksFor/alumniOf, ItemList bài tác giả | T1 (D-03/D-04/D-05) + T3 | COVERED |
| 7g | WebPage cho 4 trang policy | T1 + T3 | COVERED |
| Ràng buộc verify | Chạy verifier có sẵn trên toàn bộ dist, loại stub, regression 124 bài | T1 (baseline + verifier) + T3 (gate đầy đủ) | COVERED |
| Ngoài scope có chủ ý | Phase 3 (NewsMediaOrganization/policy props), Phase 4, 5, 6; `/dau-tu/quy-dau-tu/` stub; 404/search/so-do-trang/preview | — | EXCLUDED (D-10) |
</source_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Builder cho listing/static/profile + enrich Person & Organization + verifier nhận biết loại trang</name>
  <files>src/lib/schema.ts, src/data/authors.ts, .planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs, .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/baseline.txt</files>
  <action>
**Bước 0 — chốt baseline TRƯỚC khi sửa bất cứ file nào.**
Nếu thiếu `node_modules` trong worktree: chạy `npm ci` (chỉ khôi phục lockfile sẵn có — TUYỆT ĐỐI không `npm install <package>`, plan này không thêm dependency nào). Chạy `npm run build`, xác nhận log có `- 0 errors`. Ghi file `.planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/baseline.txt` gồm đúng 2 dòng dạng `articles=<N>` và `pages=<M>`, với N = `grep -rl '#article"' dist --include='*.html' | wc -l` và M = `grep -rL 'http-equiv="refresh"' dist --include='*.html' | wc -l`. Số đo trên máy planner là N=124, M=146 — dùng số đo THỰC TẾ của worktree, không hardcode.

**Bước 1 — `src/data/authors.ts`.**
Thêm đúng một field mới `alumniOf: "Học viện Chính sách và Phát triển"` ngay dưới `education`. Không đổi `slug`, `avatar`, `socialLinks` hay bất kỳ field nào khác.

**Bước 2 — `src/lib/schema.ts`: thêm type + 3 builder, enrich 2 node sẵn có.**

Type mới (đặt cạnh các interface hiện có, giữ style comment tiếng Anh):
- `ListingItem`: `{ name: string; url: string }` — `url` BẮT BUỘC tuyệt đối (`https://...`).
- `CollectionPageInput`: `{ siteUrl: string; pageUrl: string; title: string; description: string; breadcrumb?: BreadcrumbEntry[]; items?: ListingItem[]; pageType?: "CollectionPage" | "WebPage"; about?: boolean }`.
- `StaticPageInput`: `{ siteUrl: string; pageUrl: string; title: string; description: string; breadcrumb?: BreadcrumbEntry[]; pageType?: "WebPage" | "AboutPage" | "ContactPage"; mainEntityId?: string }`.
- `ProfilePageInput`: `{ siteUrl: string; pageUrl: string; title: string; description: string; breadcrumb?: BreadcrumbEntry[]; items?: ListingItem[] }`.

Helper nội bộ (không export) để 3 builder + `buildArticlePageNodes` dùng chung, KHÔNG copy-paste:
- `breadcrumbNode(pageUrl, entries)` → `{ "@type": "BreadcrumbList", "@id": pageUrl + "#breadcrumb", itemListElement: entries.map((c, i) => ({ "@type": "ListItem", position: i + 1, name: c.name, ...(c.item ? { item: c.item } : {}) })) }`. Refactor `buildArticlePageNodes` để gọi helper này thay cho khối inline hiện tại (output JSON phải giống hệt trước — đây là điều kiện để 124 trang bài viết không đổi byte nào).
- `itemListNode(pageUrl, items)` → `{ "@type": "ItemList", "@id": pageUrl + "#itemlist", numberOfItems: items.length, itemListElement: items.map((it, i) => ({ "@type": "ListItem", position: i + 1, name: it.name, url: it.url })) }`.

`export function buildCollectionPageNodes(input: CollectionPageInput): SchemaNode[]` — trả về mảng theo thứ tự: page node, breadcrumb node (chỉ khi `breadcrumb?.length`), itemlist node (chỉ khi `items?.length`). Page node:
`"@type"` = `pageType ?? "CollectionPage"`; `"@id"` = `` `${pageUrl}#webpage` ``; `url` = pageUrl; `name` = title; `description`; `inLanguage: "vi"`; `isPartOf: { "@id": websiteId(siteUrl) }`; `breadcrumb: { "@id": `${pageUrl}#breadcrumb` }` chỉ khi có breadcrumb; `mainEntity: { "@id": `${pageUrl}#itemlist` }` chỉ khi có items; `about: { "@id": orgId(siteUrl) }` chỉ khi `about === true`. Dùng conditional spread để không bao giờ set key = undefined (đúng style file hiện tại).

`export function buildStaticPageNodes(input: StaticPageInput): SchemaNode[]` — page node giống trên nhưng `"@type"` = `pageType ?? "WebPage"`, không có `about`, không có ItemList, và có `mainEntity: { "@id": mainEntityId }` chỉ khi `mainEntityId` truyền vào. Trả về `[pageNode, breadcrumbNode?]`.

`export function buildProfilePageNodes(input: ProfilePageInput): SchemaNode[]` — trả về `[buildPersonNode(siteUrl), profilePageNode, breadcrumbNode?, itemListNode?]`. `profilePageNode`: `"@type": "ProfilePage"`, `"@id": `${pageUrl}#webpage``, `url`, `name`, `description`, `inLanguage: "vi"`, `isPartOf: { "@id": websiteId(siteUrl) }`, `breadcrumb` (nếu có), `mainEntity: { "@id": personId(siteUrl) }`, `hasPart: { "@id": `${pageUrl}#itemlist` }` (chỉ khi có items).

Enrich `buildPersonNode` — thêm đúng 3 key, không sửa key cũ:
- `worksFor: { "@id": orgId(siteUrl) }` (chiều ngược Person→Organization đang thiếu).
- `alumniOf: { "@type": "EducationalOrganization", name: author.alumniOf }` — chỉ spread khi `author.alumniOf` có giá trị.
- `mainEntityOfPage: `${siteUrl}author/${author.slug}/`` — CHUỖI URL. Viết comment tiếng Anh ngay tại chỗ giải thích: dùng object `{ "@id": ... }` sẽ dangling trên trang bài viết (trang đó không có node `#webpage` của profile) và làm verifier FAIL.

Enrich `buildSiteNodes` — node Organization thêm đúng 1 key `contactPoint: { "@type": "ContactPoint", contactType: "customer support", email: site.email, url: `${siteUrl}contact/`, availableLanguage: ["Vietnamese"] }`. KHÔNG đổi `name`/`url`/`logo`/`founder`/`email`/`areaServed`, KHÔNG thêm lại `sameAs`, KHÔNG thêm `publishingPrinciples`/`correctionsPolicy`/`NewsMediaOrganization` (Phase 3, ngoài scope).

**Bước 3 — `verify-graph.mjs` nhận biết loại trang (siết chặt, không nới).**
- Thêm helper `hasType(node, type)` xử lý `@type` là string HOẶC mảng.
- Tính `const isArticlePage = graph.some(n => isObject(n) && hasType(n, "Article"))`.
- Khối check 5d hiện tại (bắt buộc có `#webpage`, `#breadcrumb`, `#primaryimage`, `#article`) giữ NGUYÊN nội dung, chỉ bọc trong `if (isArticlePage) { ... }`. Không đổi check 1/2/3/4/5a/5b/5c.
- Thêm check mới áp dụng cho MỌI trang: (a) nếu tồn tại node `@id` kết thúc `#webpage` thì node đó phải có `url` là chuỗi bắt đầu `https://`, có `inLanguage`, có `isPartOf`; (b) nếu tồn tại node `@id` kết thúc `#itemlist` thì `itemListElement` phải là mảng không rỗng và MỌI phần tử phải có `position` kiểu number cùng `url` là chuỗi bắt đầu `https://`; (c) nếu tồn tại node `@id` kết thúc `#breadcrumb` thì `itemListElement` phải là mảng không rỗng.
- Cập nhật khối comment đầu file cho khớp hành vi mới (đừng để comment tự mâu thuẫn với code).
  </action>
  <verify>
    <automated>npm run build > /tmp/gsd-v41-build.log 2>&1 && grep -q -- '- 0 errors' /tmp/gsd-v41-build.log && grep -Eq '^export function buildCollectionPageNodes\(' src/lib/schema.ts && grep -Eq '^export function buildStaticPageNodes\(' src/lib/schema.ts && grep -Eq '^export function buildProfilePageNodes\(' src/lib/schema.ts && node .planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs $(grep -rl '#article"' dist --include='*.html') > /tmp/gsd-v41-article.log && test "$(grep -c '^PASS' /tmp/gsd-v41-article.log)" = "$(sed -n 's/^articles=//p' .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/baseline.txt)" && grep -q '"worksFor"' dist/dau-tu/etf/etf-la-gi/index.html && grep -q '"contactPoint"' dist/dau-tu/etf/etf-la-gi/index.html && grep -q '"mainEntityOfPage":"https' dist/dau-tu/etf/etf-la-gi/index.html && echo GATE-T1-OK</automated>
  </verify>
  <done>`baseline.txt` tồn tại với 2 dòng `articles=`/`pages=`; build 0 errors; 3 builder mới được export; verifier PASS đúng bằng số bài baseline (regression Phase 1+2 nguyên vẹn); trang bài viết mẫu có `worksFor`, `contactPoint`, và `mainEntityOfPage` ở dạng chuỗi URL (không phải object `@id`).</done>
</task>

<task type="auto">
  <name>Task 2: Wire 11 trang listing (8 category + 2 group + trang chủ) sang schemaNodes</name>
  <files>src/pages/[category].astro, src/pages/dau-tu/[category].astro, src/pages/phan-tich/[category].astro, src/pages/dau-tu.astro, src/pages/phan-tich.astro, src/pages/index.astro</files>
  <action>
**A — 3 file category (`src/pages/[category].astro`, `src/pages/dau-tu/[category].astro`, `src/pages/phan-tich/[category].astro`).**
Trong mỗi file: xoá hoàn toàn biến `breadcrumbSchema` và khối `<Fragment slot="head"> ... </Fragment>`. Import thêm `buildCollectionPageNodes` từ `@/lib/schema`, và `getArticlePath` (+ `url` với 2 file dau-tu/phan-tich) từ `@/data/site`. Giữ nguyên `breadcrumbItems` đang truyền cho `CategoryListing` (đó là breadcrumb hiển thị) và giữ `categoryUrl`.

Thêm:
`const siteUrl = Astro.site?.toString() ?? "";`
`const schemaNodes = buildCollectionPageNodes({ siteUrl, pageUrl: categoryUrl, title: category.title, description: category.description, breadcrumb: [...], items: articles.map(a => ({ name: a.data.title, url: new URL(getArticlePath(a), Astro.site).toString() })) });`

Mảng `breadcrumb` theo từng file (mỗi hop `{ name, item }`, item tuyệt đối):
- `[category].astro`: `Trang chủ` → `siteUrl`; `category.title` → `categoryUrl`.
- `dau-tu/[category].astro`: `Trang chủ` → `siteUrl`; `Đầu tư` → `new URL(url("/dau-tu/"), Astro.site).toString()`; `category.title` → `categoryUrl`.
- `phan-tich/[category].astro`: `Trang chủ` → `siteUrl`; `Phân tích` → `new URL(url("/phan-tich/"), Astro.site).toString()`; `category.title` → `categoryUrl`.

Lưu ý: code cũ dùng `new URL("/dau-tu/", Astro.site)` bỏ qua `BASE_URL`; chuyển sang bọc qua helper `url()` cho khớp `getCategoryPath` (sửa luôn điểm lệch tiềm ẩn khi build với base khác `/`).

Đổi thẻ mở thành `<BaseLayout title={category.title} description={category.description} schemaNodes={schemaNodes}>`.

**B — `src/pages/dau-tu.astro` và `src/pages/phan-tich.astro`.**
Nâng 2 chuỗi title/description đang viết inline trong props `BaseLayout` lên thành `const pageTitle` / `const pageDescription` trong frontmatter (GIỮ NGUYÊN từng chữ), rồi truyền cho cả `BaseLayout` lẫn builder. Thêm:
`const siteUrl = Astro.site?.toString() ?? "";`
`const pageUrl = new URL(url("/dau-tu/"), Astro.site).toString();` (file `phan-tich.astro` dùng `/phan-tich/`)
`const schemaNodes = buildCollectionPageNodes({ siteUrl, pageUrl, title: pageTitle, description: pageDescription, breadcrumb: [{ name: "Trang chủ", item: siteUrl }, { name: "Đầu tư", item: pageUrl }], items: investing.map(c => ({ name: c.title, url: new URL(getCategoryPath(c), Astro.site).toString() })) });`
(`phan-tich.astro`: hop thứ 2 tên `Phân tích`, items map từ `analysis`.) Breadcrumb JSON-LD phải khớp đúng `<Breadcrumb items={...}>` đang render. Import thêm `url` và `buildCollectionPageNodes`. Truyền `schemaNodes={schemaNodes}` cho `BaseLayout`.

**C — `src/pages/index.astro`.**
Thêm:
`const siteUrl = Astro.site?.toString() ?? "";`
`const schemaNodes = buildCollectionPageNodes({ siteUrl, pageUrl: siteUrl, title: site.name, description: site.description, pageType: "WebPage", about: true, items: startHere.map(a => ({ name: a.data.title, url: new URL(getArticlePath(a), Astro.site).toString() })) });`
KHÔNG truyền `breadcrumb` (D-01: trang chủ không có breadcrumb hiển thị, trail 1 hop vô nghĩa). Import thêm `getArticlePath`, `buildCollectionPageNodes`. Truyền `schemaNodes={schemaNodes}` cho `BaseLayout`.

Không đụng vào bất kỳ markup/CSS nào khác của 6 file này.
  </action>
  <verify>
    <automated>LIST="dist/index.html dist/dau-tu/index.html dist/phan-tich/index.html dist/dau-tu/co-phieu/index.html dist/dau-tu/etf/index.html dist/dau-tu/trai-phieu/index.html dist/dau-tu/phai-sinh/index.html dist/phan-tich/co-ban/index.html dist/phan-tich/ky-thuat/index.html dist/reviews/index.html dist/nha-dau-tu/index.html"; npm run build > /tmp/gsd-v41-build.log 2>&1 && grep -q -- '- 0 errors' /tmp/gsd-v41-build.log && for f in $LIST; do test "$(grep -o 'application/ld+json' "$f" | wc -l | tr -d ' ')" = "1" || { echo "FAIL blocks $f"; exit 1; }; done && node .planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs $LIST && grep -q '"@type":"CollectionPage"' dist/reviews/index.html && grep -q '"@type":"WebPage"' dist/index.html && grep -q '"@id":"https://valueinvesting.com.vn/#webpage"' dist/index.html && grep -q '#itemlist' dist/dau-tu/etf/index.html && test -z "$(grep -l 'slot=\"head\"' 'src/pages/[category].astro' 'src/pages/dau-tu/[category].astro' 'src/pages/phan-tich/[category].astro' 2>/dev/null)" && echo GATE-T2-OK</automated>
  </verify>
  <done>11 trang listing mỗi trang chỉ còn 1 khối `application/ld+json`; verifier PASS cả 11; `dist/reviews/index.html` có node `CollectionPage`, `dist/index.html` có node `WebPage` `@id ...com.vn/#webpage`, trang category có node `#itemlist`; 3 file category không còn `slot="head"`.</done>
</task>

<task type="auto">
  <name>Task 3: Wire 7 trang static/profile, gỡ slot head, viết gate toàn dist</name>
  <files>src/pages/about.astro, src/pages/contact.astro, src/pages/author/[slug].astro, src/pages/editorial-policy.astro, src/pages/corrections-policy.astro, src/pages/sources-policy.astro, src/pages/disclaimer.astro, src/layouts/BaseLayout.astro, .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs</files>
  <action>
**A — `src/pages/about.astro`.**
Nâng title/description inline lên `const pageTitle` / `const pageDescription` (giữ nguyên từng chữ). Thêm `const siteUrl = Astro.site?.toString() ?? "";`, `const pageUrl = new URL(Astro.url.pathname, Astro.site).toString();` và
`const schemaNodes = buildStaticPageNodes({ siteUrl, pageUrl, title: pageTitle, description: pageDescription, pageType: "AboutPage", mainEntityId: orgId(siteUrl), breadcrumb: [{ name: "Trang chủ", item: siteUrl }, { name: "Về Value Investing", item: pageUrl }] });`
Import `buildStaticPageNodes` và `orgId` từ `@/lib/schema`. Truyền `schemaNodes={schemaNodes}`. `mainEntity` là reference THUẦN — không định nghĩa lại Organization ở đây (đã có đủ trong `buildSiteNodes`).

**B — `src/pages/contact.astro`.** Y hệt A nhưng `pageType: "ContactPage"`, breadcrumb hop 2 tên `Liên hệ`. `mainEntityId: orgId(siteUrl)` (thực thể người dùng đang liên hệ). `contactPoint` KHÔNG khai báo ở đây — đã nằm trong node Organization từ Task 1 (D-02).

**C — 4 trang policy** (`editorial-policy`, `corrections-policy`, `sources-policy`, `disclaimer`). Mỗi file: nâng title/description lên const, thêm `siteUrl` + `pageUrl` như trên, gọi `buildStaticPageNodes({ siteUrl, pageUrl, title: pageTitle, description: pageDescription, breadcrumb: [{ name: "Trang chủ", item: siteUrl }, { name: <tên ngắn>, item: pageUrl }] })` — để `pageType` mặc định (`WebPage`), không truyền `mainEntityId`. Tên ngắn hop 2 lần lượt: `Chính sách biên tập`, `Chính sách đính chính`, `Chính sách nguồn tham khảo`, `Miễn trừ trách nhiệm`. KHÔNG thêm `publishingPrinciples`/`correctionsPolicy` vào Organization (Phase 3, ngoài scope).

**D — `src/pages/author/[slug].astro`.**
Xoá hoàn toàn khối `<script slot="head" type="application/ld+json" ...>` (node Person trần trùng `@id` nhưng khác tập thuộc tính). Thêm `siteUrl`, `pageUrl` và
`const schemaNodes = buildProfilePageNodes({ siteUrl, pageUrl, title: profileAuthor.name, description: profileAuthor.bio, breadcrumb: [{ name: "Trang chủ", item: siteUrl }, { name: profileAuthor.name, item: pageUrl }], items: articles.map(a => ({ name: a.data.title, url: new URL(getArticlePath(a), Astro.site).toString() })) });`
Import `buildProfilePageNodes` và `getArticlePath`. Truyền `schemaNodes={schemaNodes}`. Toàn bộ bài đều nằm trong DOM (ẩn từ bài thứ 10 bằng CSS) nên ItemList liệt kê đủ là khớp nội dung (D-06).

**E — `src/layouts/BaseLayout.astro`.** Xoá dòng `<slot name="head" />`. Không đổi gì khác trong file.

**F — Viết `.planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs`** (Node ESM thuần, không dependency, `process.exit(1)` nếu có bất kỳ FAIL nào):
1. Duyệt đệ quy `dist/` lấy mọi `*.html`. Tách stub (nội dung khớp `/http-equiv=["']refresh/i`) khỏi trang thật; in số lượng mỗi loại.
2. `spawnSync("node", [<đường dẫn verify-graph.mjs>, ...trangThật])`, thu stdout; in mọi dòng `FAIL`; FAIL nếu `status !== 0`.
3. Đọc `baseline.txt` cùng thư mục (FAIL kèm hướng dẫn nếu thiếu). Đếm trang có node `Article` → phải bằng `articles=`; số trang thật → phải bằng `pages=`.
4. Lấy `siteUrl` từ node `@id` kết thúc `#website` (đọc key `url`) — không hardcode domain. URL tuyệt đối của một trang = `siteUrl` + đường dẫn tương đối của file sau khi bỏ đuôi `index.html`.
5. Kiểm bảng kỳ vọng 18 trang in-scope; với mỗi dòng: tồn tại node top-level `@id === pageUrl + "#webpage"`, `@type` đúng như cột, có `isPartOf` trỏ `#website`, và có đủ các key ở cột cuối. Với dòng `CollectionPage`/`WebPage` có `mainEntity`: phải tồn tại node `@id === pageUrl + "#itemlist"` với `itemListElement` không rỗng.

| dist path | @type | key bắt buộc |
|---|---|---|
| index.html | WebPage | about, mainEntity |
| dau-tu/index.html | CollectionPage | breadcrumb, mainEntity |
| phan-tich/index.html | CollectionPage | breadcrumb, mainEntity |
| dau-tu/co-phieu/index.html | CollectionPage | breadcrumb, mainEntity |
| dau-tu/etf/index.html | CollectionPage | breadcrumb, mainEntity |
| dau-tu/trai-phieu/index.html | CollectionPage | breadcrumb, mainEntity |
| dau-tu/phai-sinh/index.html | CollectionPage | breadcrumb, mainEntity |
| phan-tich/co-ban/index.html | CollectionPage | breadcrumb, mainEntity |
| phan-tich/ky-thuat/index.html | CollectionPage | breadcrumb, mainEntity |
| reviews/index.html | CollectionPage | breadcrumb, mainEntity |
| nha-dau-tu/index.html | CollectionPage | breadcrumb, mainEntity |
| about/index.html | AboutPage | breadcrumb, mainEntity |
| contact/index.html | ContactPage | breadcrumb, mainEntity |
| author/nguyen-viet-loc/index.html | ProfilePage | breadcrumb, mainEntity, hasPart |
| editorial-policy/index.html | WebPage | breadcrumb |
| corrections-policy/index.html | WebPage | breadcrumb |
| sources-policy/index.html | WebPage | breadcrumb |
| disclaimer/index.html | WebPage | breadcrumb |

6. Kiểm riêng chống-regress: trên trang profile, node top-level `Person` xuất hiện ĐÚNG một lần, `@id` bằng `siteUrl + "author/nguyen-viet-loc/#person"`, và `ProfilePage.mainEntity["@id"]` trỏ đúng `@id` đó. Trên trang profile và trên trang bài viết đầu tiên tìm được: node `Person` phải có `worksFor` trỏ `#organization`, và `mainEntityOfPage` phải là chuỗi (`typeof === "string"`). Trên `contact/index.html`: node `#organization` phải có `contactPoint`.
7. In tổng kết một dòng dạng `PAGES <M> (stub <S> skipped) | ARTICLE <N> | PHASE7 18/18 OK` rồi `exit 0`.

**G — Negative test (bắt buộc chạy, ghi kết quả vào SUMMARY).** Tạo bản sao tạm ngoài repo của một trang listing đã build, cố tình phá (ví dụ đổi `"@type":"CollectionPage"` thành `"@type":"WebPage"`, hoặc xoá node `#itemlist` khiến `mainEntity` dangling), chạy verifier/gate trên bản sao đó và xác nhận báo FAIL + exit 1. Xoá file tạm sau khi thử. Mục đích: chứng minh gate không PASS rỗng.
  </action>
  <verify>
    <automated>npm run build > /tmp/gsd-v41-build.log 2>&1 && grep -q -- '- 0 errors' /tmp/gsd-v41-build.log && ! grep -rq 'slot="head"' src && node .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs && test "$(grep -o 'application/ld+json' dist/author/nguyen-viet-loc/index.html | wc -l | tr -d ' ')" = "1" && grep -q '"@type":"ProfilePage"' dist/author/nguyen-viet-loc/index.html && grep -q '"@type":"AboutPage"' dist/about/index.html && grep -q '"@type":"ContactPage"' dist/contact/index.html && grep -q '"@id":"https://valueinvesting.com.vn/disclaimer/#webpage"' dist/disclaimer/index.html && test -z "$(git status --porcelain -- src/content/articles knowledge .antigravity .agents)" && echo GATE-T3-OK</automated>
  </verify>
  <done>Build 0 errors; không còn `slot="head"` trong `src/`; `verify-pages.mjs` exit 0 với dòng tổng kết `PHASE7 18/18 OK` và số trang/số bài khớp baseline; trang author còn 1 khối ld+json và có `ProfilePage`; about/contact/disclaimer có đúng node tương ứng; negative test đã chạy và verifier báo FAIL đúng như kỳ vọng; `git status --porcelain` trên `src/content/articles`, `knowledge`, `.antigravity`, `.agents` trả về rỗng.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dữ liệu content (`src/content/articles/*.md`, `src/data/*.ts`) → JSON-LD nhúng trong HTML | title/description do tác giả nhập được `JSON.stringify` vào `<script type="application/ld+json">` tại build time |
| Site tĩnh → Google / crawler | Entity graph công khai, quyết định cách Google resolve entity brand + tác giả (YMYL) |
| Repo → tiến trình content chạy song song | Worktree cô lập, nhưng `src/content/articles/` là vùng cấm ghi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v41-01 | Spoofing | Node `Person` bị định nghĩa 2 kiểu cùng `@id` trên trang author (bản trần từ `slot="head"` vs bản đầy đủ từ `buildPersonNode`) làm loãng entity tác giả | mitigate | Task 3 xoá khối `slot="head"`; `verify-pages.mjs` bước 6 FAIL nếu node `Person` top-level xuất hiện khác 1 lần hoặc `@id` lệch |
| T-v41-02 | Tampering | Sửa `verify-graph.mjs` để "làm cho pass" thay vì sửa code | mitigate | D-09: 4 check bắt buộc của trang bài viết giữ nguyên nội dung, chỉ bọc điều kiện `isArticlePage`; Task 1 gate lại 124 trang bài viết đúng bằng baseline; Task 3 bắt buộc negative test chứng minh gate biết FAIL |
| T-v41-03 | Tampering | Wire thiếu trang, gate vẫn xanh vì chỉ chạy trên vài trang mẫu | mitigate | `verify-pages.mjs` tự liệt kê toàn bộ `dist/`, loại stub theo `http-equiv="refresh"`, và đối chiếu số trang với `baseline.txt`; bảng 18 trang in-scope là danh sách cứng |
| T-v41-04 | Information Disclosure | `contactPoint` phơi email brand trên mọi trang | accept | `site.email` đã public sẵn trong `src/data/site.ts` và hiển thị trên trang contact; không thêm dữ liệu cá nhân mới (không đưa email/số điện thoại cá nhân vào Person) |
| T-v41-05 | Denial of Service | ItemList 124 mục trên trang author làm phình HTML | accept | ~16KB, nhỏ hơn search index JSON đã nhúng sẵn ở mọi trang; chỉ ảnh hưởng 1 trang; đổi lại ItemList khớp đúng DOM (D-06) |
| T-v41-SC | Tampering | npm/pip/cargo installs | mitigate | Plan không thêm dependency; chỉ được phép `npm ci` khôi phục lockfile sẵn có. Nếu executor thấy cần cài package mới → DỪNG và hỏi |
</threat_model>

<verification>
1. `npm run build` xanh: `astro check` `- 0 errors` (baseline trước khi sửa cũng là 0 errors / 0 warnings / 14 hints).
2. Số trang non-stub và số trang bài viết sau build khớp đúng `baseline.txt` — không mất, không thêm trang.
3. `node .planning/quick/260821-umf-phase-1-2-entity-graph-schema-refactor/verify-graph.mjs $(grep -rL 'http-equiv="refresh"' dist --include='*.html')` → PASS toàn bộ, exit 0.
4. `node .planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/verify-pages.mjs` → exit 0, in `PHASE7 18/18 OK`.
5. Negative test: bản sao trang listing bị phá có báo FAIL + exit 1 (gate không PASS rỗng).
6. Đếm thủ công `grep -o 'application/ld+json' <file> | wc -l` = 1 trên: 1 trang category, trang chủ, trang author, 1 trang policy.
7. `git status` chỉ hiện thay đổi trong `src/lib/`, `src/pages/`, `src/layouts/`, `src/data/` và 2 thư mục `.planning/quick/260821-*` — KHÔNG được có thay đổi trong `src/content/articles/`, `knowledge/`, `.antigravity/`, `.agents/`.
</verification>

<success_criteria>
- [ ] 8 trang category listing chỉ còn 1 khối ld+json, có `CollectionPage` + `BreadcrumbList` (có `@id`, được trỏ tới) + `ItemList` bài trong category với url tuyệt đối.
- [ ] `dau-tu/` và `phan-tich/` có `CollectionPage` + `BreadcrumbList` + `ItemList` các category con.
- [ ] Trang chủ có `WebPage` `#webpage` với `isPartOf → #website`, `about → #organization`, `mainEntity → #itemlist`.
- [ ] `about/` là `AboutPage` với `mainEntity` reference thuần tới Organization; `contact/` là `ContactPage`; Organization có `contactPoint` đồng nhất trên mọi trang.
- [ ] `author/nguyen-viet-loc/` là `ProfilePage`, 1 khối ld+json, `mainEntity → Person`, Person có `worksFor → #organization` và `alumniOf`; không còn Person định nghĩa hai kiểu.
- [ ] 4 trang policy có node `WebPage` `#webpage` + `isPartOf` + `breadcrumb`.
- [ ] Verifier PASS trên TOÀN BỘ trang non-stub trong `dist/`; redirect stub được loại bằng `grep -rL 'http-equiv="refresh"'`.
- [ ] Regression: số trang bài viết PASS = baseline, `npm run build` 0 errors.
- [ ] Không đổi `src/content.config.ts`, không đổi `@id` Person, không thêm dependency, không đụng `src/content/articles/`.
- [ ] Không làm Phase 3/4/5/6 (không `NewsMediaOrganization`, không `publishingPrinciples`/`correctionsPolicy`, không `DefinedTerm`, không `about/mentions` trên Article, không entity broker).
</success_criteria>

<output>
Create `.planning/quick/260821-v41-phase-7-entity-graph-collectionpage-abou/260821-v41-SUMMARY.md` when done
</output>
