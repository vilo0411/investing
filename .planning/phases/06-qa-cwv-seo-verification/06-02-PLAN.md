---
phase: 06-qa-cwv-seo-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified: []
requirements: [QASE-01, QASE-02, QASE-03, QASE-04]
autonomous: false

must_haves:
  truths:
    - "Automated verification suite executes cleanly and reports zero blocker violations"
    - "Astro build succeeds and generates 62 static routes"
    - "Visual regression audit is complete for both Desktop and Mobile viewports on 7 templates"
  artifacts:
    - path: "dist/verification-report.json"
      provides: "Final consolidated test run results"
  key_links: []
---

<objective>
Execute the automated validation suite on the production build output (`dist/`) to confirm zero regressions in CWV, SEO schema, sitemaps, and URL contracts. Perform manual visual regression inspections of critical pages on desktop and mobile viewports.

Purpose: Certify that the site redesign is ready for production without performance or SEO penalties.
Output: Verification report, browser recordings, and plan summary.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.agent/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-qa-cwv-seo-verification/06-CONTEXT.md
@.planning/phases/06-qa-cwv-seo-verification/06-RESEARCH.md
@.planning/phases/06-qa-cwv-seo-verification/06-VALIDATION.md
@.planning/phases/06-qa-cwv-seo-verification/06-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Execute the automated validation suite on build output (QASE-02, QASE-03, QASE-04)</name>
  <files>dist/verification-report.json</files>
  <read_first>
    scripts/run-verification.mjs (confirm execution logic)
  </read_first>
  <action>
    First, clean and build the website using the production build script (`npm run build`). This verifies compilation and generates the static site in the `dist/` directory.
    
    Next, execute `node scripts/run-verification.mjs` in the zsh terminal.
    
    If the script reports any failures (e.g. images missing width/height, malformed JSON-LD schemas, broken local links, or sitemap diff mismatches), investigate the source of the issue, correct it in the codebase templates or configs, rebuild, and re-run until all checks pass with zero errors.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0 and creates the `dist/` directory
    - `node scripts/run-verification.mjs` exits 0 with zero blocker errors
    - `dist/verification-report.json` is generated and contains `status: "passed"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('dist/verification-report.json', 'utf8'); if(JSON.parse(r).status !== 'passed') throw new Error('Verification failed');"</automated>
  </verify>
  <done>Automated verification script runs on production build output and passes successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Perform visual regression QA on critical templates (QASE-01, D-01, D-02)</name>
  <files></files>
  <read_first>
    .planning/phases/06-qa-cwv-seo-verification/06-CONTEXT.md (D-01/D-02 viewport and route list)
  </read_first>
  <action>
    Start the local preview server (`npm run preview`) to serve the static files in `dist/`.
    
    Spawn the Browser Subagent with instructions to:
    1. Check critical templates on both Desktop (1440x900) and Mobile (375x812) viewports:
       - Homepage (`/`)
       - Category Listing (`/dau-tu/co-phieu/`)
       - Article Page (`/dau-tu/co-phieu/co-phieu-la-gi/`)
       - Author Profile (`/author/nguyen-viet-loc/`)
       - Editorial Policy (`/editorial-policy/`)
       - Disclaimer (`/disclaimer/`)
       - Search Page (`/search/`)
    
    2. Visually verify the layout and style of the redesigned elements and new components:
       - Breadcrumbs, KeyTakeaways, CitationBox, ComparisonTable, and AuthorBox.
       - Verify there are no layout shifts, overlapping text, broken Vietnamese diacritics, or horizontal scrollbars (especially on Mobile).
       - Confirm that hover states on interactive buttons/cards are active and feel premium.
       
    The Browser Subagent will record its actions and save videos of the inspection, returning a detailed visual report.
  </action>
  <acceptance_criteria>
    - Browser Subagent successfully completes navigation to all 7 templates
    - Visual inspection reports zero layout issues, alignment breaks, or overlapping text on either desktop or mobile viewports
  </acceptance_criteria>
  <verify>
    <human-check>Verify the browser recordings and final subagent report show consistent styling across all templates on desktop and mobile viewports.</human-check>
  </verify>
  <done>Visual regression QA audit of all 7 templates on desktop/mobile viewports is completed with clean rendering results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network port boundary | The preview server binds to port 4321 or similar port to allow browser navigation. Connections are only local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Denial of Service (Open Preview Port) | local preview server | mitigate | The server is only run during visual inspection and shut down immediately afterwards; it binds to localhost by default |
</threat_model>

<verification>
Automated script passes and visual QA inspection is logged.
</verification>

<success_criteria>
- Zero errors in automated checks
- Visual inspection checklist completed and verified clean
</success_criteria>

<output>
Create `.planning/phases/06-qa-cwv-seo-verification/06-02-SUMMARY.md` when done
</output>
