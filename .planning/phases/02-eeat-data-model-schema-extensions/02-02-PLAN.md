---
phase: 02-eeat-data-model-schema-extensions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/authors.ts
autonomous: true
requirements: [EEAT-02]

must_haves:
  truths:
    - "src/data/authors.ts exists and exports a single canonical author object with all fields needed by AuthorBox, about.astro, and future Phase 3/4 JSON-LD/profile work"
    - "The exported author data matches the Vietnamese text currently rendered by AuthorBox.astro and about.astro byte-for-byte, so no visual regression occurs when call sites are migrated in the next plan"
  artifacts:
    - path: "src/data/authors.ts"
      provides: "Single source of truth author/reviewer profile data object"
      contains: "export const author"
      min_lines: 15
  key_links:
    - from: "src/data/authors.ts"
      to: "src/components/AuthorBox.astro (consumed in plan 02-03)"
      via: "import { author } from \"@/data/authors\""
      pattern: "export const author"
---

<objective>
Create `src/data/authors.ts` as the single canonical source of author/reviewer profile data, per CONTEXT.md D-01 (single author, no array scaffold) and D-02 (full EEAT field set: name, slug, role, bio, credentials[], experience, expertise[], avatar?, socialLinks?), extended per RESEARCH.md Assumption A1 with `moneyPerspective`, `education`, `publishedIn` (fields currently rendered by `about.astro` that are not in D-02's list but must not be dropped).

Purpose: Establish the single-source-of-truth data module that plan 02-03 will wire `AuthorBox.astro`, `ArticleLayout.astro`, `BaseLayout.astro`, `about.astro`, and `index.astro` to read from (EEAT-02), and that Phase 3 (AuthorBox v2) and Phase 4 (`/author/[slug]`, JSON-LD Person) will consume directly without needing new fields.
Output: New file `src/data/authors.ts` exporting `author` (object) and `type Author`.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-CONTEXT.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-RESEARCH.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/data/authors.ts with full D-02 + A1 field set</name>
  <files>src/data/authors.ts</files>
  <read_first>
    src/data/site.ts
    src/components/AuthorBox.astro
    src/pages/about.astro
  </read_first>
  <action>
    Create new file `src/data/authors.ts`, mirroring the "single config object + named export + co-located type" module pattern of `src/data/site.ts` (e.g. `export const site = {...}`, `export type Category = (typeof categories)[number]`).

    Export `export const author = {...}` (per D-01: single object, NOT an array — this is a solo-blog site with one author) with these exact fields and values:

    - `name: "Nguyễn Viết Lộc"` (currently `site.author`, line 3 of site.ts)
    - `slug: "nguyen-viet-loc"` (new field per D-02, used by Phase 4 `/author/[slug]`)
    - `role: "Người phụ trách nội dung"` (currently `site.authorProfile.role`, line 16 of site.ts — must match exactly, this string is rendered by AuthorBox.astro and about.astro)
    - `bio: "Phụ trách nội dung tại ValueInvesting.com.vn — tập trung giải thích khái niệm đầu tư bằng ngôn ngữ rõ ràng, có nguồn tham khảo, không khuyến nghị mua bán."` (currently hardcoded in `AuthorBox.astro` lines 19-21 — copy verbatim, including the em dash and exact wording, per 02-UI-SPEC.md Copywriting Contract)
    - `credentials: [] as string[]` (new field per D-02, empty per RESEARCH.md Open Question 2 — do not fabricate credentials for a YMYL author)
    - `experience: "Xây dựng nội dung giáo dục tài chính cho nhà đầu tư cá nhân, biên tập theo hướng có nguồn, rõ rủi ro và không khuyến nghị mua bán."` (per D-02: single descriptive string, NOT an array — D-02 literally specifies "experience (string mô tả số năm/nền tảng)". This is a single-string MERGE of the two existing `site.authorProfile.experience` array items: "Xây dựng nội dung giáo dục tài chính cho nhà đầu tư cá nhân" + "Biên tập bài viết theo hướng có nguồn, rõ rủi ro và không khuyến nghị mua bán" — combined into one sentence as shown above)
    - `expertise: ["Đầu tư giá trị", "Phân tích cơ bản", "Sản phẩm đầu tư cho người mới"]` (currently `site.authorProfile.expertise`, line 17 of site.ts — copy verbatim, array order preserved)
    - `avatar: undefined as string | undefined` (new optional field per D-02, no avatar image exists yet)
    - `socialLinks: undefined as { linkedin?: string; twitter?: string; email?: string } | undefined` (new optional field per D-02, none configured yet)
    - `moneyPerspective: "Đầu tư nên bắt đầu từ hiểu doanh nghiệp, hiểu rủi ro và biết giới hạn của chính mình trước khi nhìn vào lợi nhuận kỳ vọng."` (per RESEARCH.md A1, currently `site.authorProfile.moneyPerspective` line 18-19 of site.ts — rendered by about.astro:46, must retain)
    - `education: "Thông tin học vấn và chứng chỉ chuyên môn sẽ được cập nhật khi có hồ sơ xác thực công khai."` (per A1, currently `site.authorProfile.education` line 21 of site.ts — rendered by about.astro:109, must retain)
    - `publishedIn: ["ValueInvesting.com.vn"]` (per A1, currently `site.authorProfile.publishedIn` line 22 of site.ts — rendered by about.astro:115, must retain)

    After the object, export `export type Author = typeof author;`.

    IMPORTANT — `experience` type resolution (additional_notes item 2): D-02 specifies `experience` as a string, but `about.astro:103` currently does `{site.authorProfile.experience.map((item) => <li>{item}</li>)}` expecting an array. Per this plan, `authors.ts.experience` is a STRING per D-02 (the locked decision takes priority over the array shape research flagged). Plan 02-03 (next plan, which migrates `about.astro`) is responsible for changing `about.astro`'s rendering of `experience` from a `.map()`-over-array to a single `<p>{author.experience}</p>` — do not change that here, this task only creates `authors.ts`. Add a brief comment above the `experience` field in `authors.ts` noting: `// String per D-02 (not string[] — about.astro rendering updated in plan 02-03 to render this as a single paragraph)`.

    Do not modify `src/data/site.ts`, `AuthorBox.astro`, `about.astro`, or any other file in this task — this task ONLY creates the new `authors.ts` file. Call-site migration happens in plan 02-03.
  </action>
  <acceptance_criteria>
    - File `src/data/authors.ts` exists.
    - Contains `export const author = {` with all 11 fields listed in the action (name, slug, role, bio, credentials, experience, expertise, avatar, socialLinks, moneyPerspective, education, publishedIn).
    - `experience` field value is a single string (not an array), matching D-02.
    - `bio` field value is byte-identical to the current hardcoded string in `AuthorBox.astro` lines 19-21 (verify via `grep -A2 "author-bio" src/components/AuthorBox.astro` comparison).
    - `role`, `expertise`, `moneyPerspective`, `education`, `publishedIn` values are byte-identical to the corresponding `site.authorProfile.*` values in `src/data/site.ts`.
    - Contains `export type Author = typeof author;`.
    - `npx astro check` reports 0 errors (new file type-checks cleanly even though nothing imports it yet).
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>src/data/authors.ts exists, exports a single `author` object with the full D-02 + A1 field set and matching `Author` type, all string values match current rendered output exactly, and astro check passes with 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | `src/data/authors.ts` is a static, build-time-only TypeScript module with no external input — same trust level as existing `src/data/site.ts` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | `src/data/authors.ts` `socialLinks`/`avatar` (currently `undefined`) | accept | No PII placed in this phase — `socialLinks: undefined`, `avatar: undefined`, `credentials: []` per RESEARCH.md Open Question 2, avoiding fabricated YMYL author credentials. Future phases that populate these fields with real data are responsible for their own review. |
</threat_model>

<verification>
- `npx astro check` — 0 errors (new file compiles, even though unreferenced)
- Manual diff: `author.bio`, `author.role`, `author.expertise`, `author.moneyPerspective`, `author.education`, `author.publishedIn` match the current values in `src/data/site.ts` / `src/components/AuthorBox.astro` exactly (no wording changes)
</verification>

<success_criteria>
- EEAT-02 (partial — data module half): `src/data/authors.ts` exists as the single source of author/reviewer profile data with the full D-01/D-02/A1 field set, ready for plan 02-03 to wire all 5 call sites to it without needing further field additions
</success_criteria>

## Artifacts this phase produces

- `src/data/authors.ts` (NEW FILE): exports `author` (object: `name`, `slug`, `role`, `bio`, `credentials: string[]`, `experience: string`, `expertise: string[]`, `avatar?: string`, `socialLinks?: { linkedin?, twitter?, email? }`, `moneyPerspective`, `education`, `publishedIn: string[]`) and `type Author = typeof author`

<output>
Create `.planning/phases/02-eeat-data-model-schema-extensions/02-02-SUMMARY.md` when done
</output>
