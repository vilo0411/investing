---
phase: 02-eeat-data-model-schema-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content.config.ts
autonomous: true
requirements: [EEAT-01]

must_haves:
  truths:
    - "All 21 existing articles in src/content/articles/*.md still build successfully without supplying citations, keyTakeaways, or factCheckedDate"
    - "New articles can optionally set citations (structured source objects), keyTakeaways (string list), and factCheckedDate"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Extended Zod schema with citations, keyTakeaways, factCheckedDate fields"
      contains: "citations: z.array(z.object("
  key_links:
    - from: "src/content.config.ts"
      to: "astro:content articles collection"
      via: "defineCollection schema z.object"
      pattern: "citations:\\s*z\\.array\\(z\\.object"
---

<objective>
Extend the `articles` content collection schema in `src/content.config.ts` with three new additive, optional fields per CONTEXT.md D-03/D-04/D-05: `citations` (structured source objects), `keyTakeaways` (string array), and `factCheckedDate` (optional coerced date). All fields must default or resolve to `undefined` so the 21 existing articles continue to validate and build without any frontmatter changes.

Purpose: Lay the schema foundation EEAT-01 requires so Phase 3 components (Key Takeaways, Citation/Fact-check box) and Phase 4 backfill (EEAT-10) have fields to read from, without touching any existing article content or breaking the build.
Output: Updated `src/content.config.ts` with the new Zod fields; `npx astro check && npx astro build` green for all 21 articles.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-CONTEXT.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add citations, keyTakeaways, factCheckedDate fields to articles schema</name>
  <files>src/content.config.ts</files>
  <read_first>
    src/content.config.ts
  </read_first>
  <action>
    In the `z.object({...})` schema passed to `defineCollection` for `articles` (currently lines 6-20), add three new fields after the existing `sources: z.array(z.string()).default([])` field (line 19), following the exact additive pattern already used for `tags`/`faq`/`sources`:

    1. `citations` — per D-03: `citations: z.array(z.object({ title: z.string(), url: z.string().optional(), publisher: z.string().optional(), date: z.string().optional() })).default([])`. This is a NEW field, additive to (not replacing) the existing `sources: z.array(z.string()).default([])` field, which stays exactly as-is.

    2. `keyTakeaways` — per D-04: `keyTakeaways: z.array(z.string()).default([])`.

    3. `factCheckedDate` — per D-05: `factCheckedDate: z.coerce.date().optional()`. This is the first field in the schema that is `.optional()` WITHOUT `.default()` — it resolves to `undefined` when absent (not a sentinel date), mirroring the `z.coerce.date()` usage of `publishDate`/`updatedDate` (required, no default) but staying optional. Add an inline comment directly above this field: `// Optional, no default — resolves to undefined when not set. Type is Date | undefined; Phase 3 consumers must handle the undefined case explicitly.`

    Do not modify any other field in the schema. Do not touch `src/content/articles/*.md` files — none require changes (that is the point of `.default()`/`.optional()`).
  </action>
  <acceptance_criteria>
    - `src/content.config.ts` contains the literal substring `citations: z.array(z.object({` with nested `title: z.string()`, `url: z.string().optional()`, `publisher: z.string().optional()`, `date: z.string().optional()`, ending `.default([])`.
    - `src/content.config.ts` contains `keyTakeaways: z.array(z.string()).default([])`.
    - `src/content.config.ts` contains `factCheckedDate: z.coerce.date().optional()` preceded by an explanatory comment about `Date | undefined`.
    - The existing `sources: z.array(z.string()).default([])` field is unchanged and still present.
    - `npx astro check` reports 0 errors, 0 warnings (same as pre-change baseline of 0 errors/0 warnings/8 hints).
    - `npx astro build` completes successfully (exit code 0) without requiring any of the 21 existing `src/content/articles/*.md` files to be modified.
  </acceptance_criteria>
  <verify>
    <automated>npx astro check && npx astro build</automated>
  </verify>
  <done>content.config.ts has the three new fields added per D-03/D-04/D-05, astro check is clean, and astro build succeeds for all 21 existing articles with zero frontmatter changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Article frontmatter (.md) -> Zod schema | Untrusted-shape input (markdown frontmatter written by content pipeline) validated at build time by `src/content.config.ts` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `citations[].url` (new Zod field) | accept | D-03 specifies `z.string().optional()` (not `.url()`) for `citations[].url`. This phase does not render `citations` anywhere (no UI surface yet — confirmed by 02-UI-SPEC.md), so there is no immediate XSS/injection surface. Phase 3, which renders `citations[].url` as an `href`, inherits responsibility for safe rendering (Astro auto-escapes `<a href>` by default; `set:html` must not be used for citation URLs). Stricter `.url()` validation was considered (RESEARCH.md) but D-03 is a locked decision and not silently overridden here — flagged for Phase 3 planning instead. |
| T-02-02 | Tampering | `factCheckedDate` (new Zod field, `.optional()` no default) | accept | Build-time-only Zod date coercion; malformed date strings fail `astro build` (loud failure), not a runtime/security issue. No PII or executable content. |
| T-02-03 | Information Disclosure | `keyTakeaways[]` (new Zod field, string array) | accept | Plain strings rendered as text content (Phase 3) via Astro's default auto-escaping — no `set:html` usage planned. No new injection surface introduced by this phase, which adds the schema field only. |
</threat_model>

<verification>
- `npx astro check` — 0 errors, 0 warnings (matches baseline)
- `npx astro build` — exit code 0, all 21 articles in `src/content/articles/*.md` build without modification
- Manual: open `src/content.config.ts` and confirm `citations`, `keyTakeaways`, `factCheckedDate` fields exist with the exact shapes specified in D-03/D-04/D-05
</verification>

<success_criteria>
- EEAT-01 satisfied: `src/content.config.ts` accepts `citations`, `factCheckedDate`, `keyTakeaways` as new optional/defaulted fields
- All 21 existing articles build successfully without supplying any of the three new fields
- `sources` field remains unchanged (additive only, per D-03)
</success_criteria>

## Artifacts this phase produces

- `src/content.config.ts`: new schema fields `citations` (z.array of `{ title, url?, publisher?, date? }`, default `[]`), `keyTakeaways` (z.array(z.string()), default `[]`), `factCheckedDate` (z.coerce.date().optional(), no default — type `Date | undefined`)

<output>
Create `.planning/phases/02-eeat-data-model-schema-extensions/02-01-SUMMARY.md` when done
</output>
