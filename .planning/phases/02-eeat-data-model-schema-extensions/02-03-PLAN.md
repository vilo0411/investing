---
phase: 02-eeat-data-model-schema-extensions
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/components/AuthorBox.astro
  - src/layouts/ArticleLayout.astro
  - src/layouts/BaseLayout.astro
  - src/pages/about.astro
  - src/pages/index.astro
  - src/data/site.ts
autonomous: true
requirements: [EEAT-02]

must_haves:
  truths:
    - "AuthorBox.astro renders identical markup, styles, and text to before the refactor, now sourced from src/data/authors.ts (D-06)"
    - "ArticleLayout.astro and BaseLayout.astro JSON-LD Person/author fields resolve from src/data/authors.ts"
    - "about.astro and index.astro render author name/role/expertise/experience/moneyPerspective/education/publishedIn from src/data/authors.ts with no visual change"
    - "No file in src/ references site.author or site.authorProfile after the refactor (grep returns 0 matches, or only a documented derived alias)"
    - "site.editorialReviewer remains untouched in src/data/site.ts (out of scope per Pitfall 2)"
  artifacts:
    - path: "src/data/site.ts"
      provides: "site config without duplicated author data"
      contains: "editorialReviewer"
  key_links:
    - from: "src/components/AuthorBox.astro"
      to: "src/data/authors.ts"
      via: "import { author } from \"@/data/authors\""
      pattern: "import \\{ author \\} from \"@/data/authors\""
    - from: "src/layouts/ArticleLayout.astro"
      to: "src/data/authors.ts"
      via: "JSON-LD Person name + byline use author.name"
      pattern: "author\\.name"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/data/authors.ts"
      via: "Organization founder JSON-LD + author meta tag use author.name"
      pattern: "author\\.name"
    - from: "src/pages/about.astro"
      to: "src/data/authors.ts"
      via: "full author profile fields rendered from author object"
      pattern: "import \\{ author \\} from \"@/data/authors\""
    - from: "src/pages/index.astro"
      to: "src/data/authors.ts"
      via: "homepage author section heading uses author.name"
      pattern: "author\\.name"
---

<objective>
Migrate all 5 call sites that currently read `site.author` / `site.authorProfile.*` from `src/data/site.ts` to instead read from `src/data/authors.ts` (created in plan 02-02), per CONTEXT.md D-06. Then remove the now-duplicated `author` and `authorProfile` fields from `src/data/site.ts`, leaving `site.editorialReviewer` and all other `site.ts` fields untouched. `AuthorBox.astro` markup/styles must remain byte-identical (D-06) — only its data source changes.

Purpose: Establish `src/data/authors.ts` as the single source of truth for author data (EEAT-02) with zero duplication, completing the data-model foundation that Phase 3 (AuthorBox v2) and Phase 4 (`/author/[slug]`, full JSON-LD Person) build on without further data-source changes.
Output: `AuthorBox.astro`, `ArticleLayout.astro`, `BaseLayout.astro`, `about.astro`, `index.astro` import and read from `@/data/authors`; `src/data/site.ts` no longer has `author`/`authorProfile` fields; `grep -rn "site\.author\|authorProfile" src/` returns 0 matches.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-CONTEXT.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-RESEARCH.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-PATTERNS.md
@/Users/nguyenvietloc/Documents/investing/.planning/phases/02-eeat-data-model-schema-extensions/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate AuthorBox.astro and the two layouts (ArticleLayout, BaseLayout) to authors.ts</name>
  <files>src/components/AuthorBox.astro, src/layouts/ArticleLayout.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    src/components/AuthorBox.astro
    src/layouts/ArticleLayout.astro
    src/layouts/BaseLayout.astro
    src/data/authors.ts
  </read_first>
  <action>
    **AuthorBox.astro** (D-06: markup/styles frozen, data-source-only swap):
    - Replace `import { site } from "@/data/site"` (line 2) with `import { author } from "@/data/authors"`.
    - Line 7: `{site.author.split(" ").pop()?.charAt(0)}` -> `{author.name.split(" ").pop()?.charAt(0)}`.
    - Line 13: `<a class="author-name" href="/about/">{site.author}</a>` -> `<a class="author-name" href="/about/">{author.name}</a>`.
    - Line 17: `<p class="author-role">{site.authorProfile.role}</p>` -> `<p class="author-role">{author.role}</p>`.
    - Lines 18-21: replace the hardcoded `.author-bio` paragraph text with `<p class="author-bio">{author.bio}</p>` (single line, since `author.bio` in `src/data/authors.ts` is byte-identical to the current hardcoded text).
    - Line 23: `{site.authorProfile.expertise.map((item) => <span>{item}</span>)}` -> `{author.expertise.map((item) => <span>{item}</span>)}`.
    - The `.author-note` paragraph (lines 25-27) is static copy not sourced from `site`/`authors` — leave its text unchanged.
    - The entire `<style>` block (lines 31-153) — leave completely untouched (D-06).

    **ArticleLayout.astro**:
    - Add `import { author } from "@/data/authors"` alongside the existing `import { categories, getCategoryPath, site } from "@/data/site"` (line 7) — keep the `site` import (still needed for `site.editorialReviewer`, `site.disclosure`).
    - Line 45 (`articleSchema.author.name`): `"name": site.author,` -> `"name": author.name,`.
    - Line 50 (`articleSchema.reviewedBy.name`): `"name": site.editorialReviewer,` — LEAVE UNCHANGED (Pitfall 2: this is the Organization reviewer field, a different concept from author data, out of scope for this refactor).
    - Line 98 (`articleMeta.author` prop passed to BaseLayout): `author: site.author,` -> `author: author.name,`.
    - Line 126 (byline link): `<a href="/about/">{site.author}</a>` -> `<a href="/about/">{author.name}</a>`.

    **BaseLayout.astro**:
    - Add `import { author } from "@/data/authors"` alongside the existing `import { categories, site } from "@/data/site"` (line 3) — keep the `site` import (still needed for `site.name`, `site.description`, `site.email`, `site.disclosure`, navigation, etc.).
    - Line 79 (Organization JSON-LD `founder.name`): `"name": site.author,` -> `"name": author.name,`.
    - Line 138 (meta tag `{ name: "author", content: site.author }`): `content: site.author` -> `content: author.name`.
  </action>
  <acceptance_criteria>
    - `src/components/AuthorBox.astro` imports `{ author }` from `"@/data/authors"` and contains zero references to `site.author` or `site.authorProfile`.
    - `src/components/AuthorBox.astro`'s `<style>` block is unchanged (diff shows zero changes within `<style>...</style>`).
    - `src/layouts/ArticleLayout.astro` contains `import { author } from "@/data/authors"`, `"name": author.name,` (twice — once in `articleSchema.author`, once is the byline reference via `{author.name}`), `author: author.name,` in the `articleMeta` prop, and still contains `"name": site.editorialReviewer,` unchanged for `reviewedBy`.
    - `src/layouts/BaseLayout.astro` contains `import { author } from "@/data/authors"`, `"name": author.name,` in the Organization founder block, and `content: author.name` in the author meta tag.
    - `grep -rn "site\.author\b" src/components/AuthorBox.astro src/layouts/ArticleLayout.astro src/layouts/BaseLayout.astro` returns 0 matches.
    - `grep -rn "authorProfile" src/components/AuthorBox.astro src/layouts/ArticleLayout.astro src/layouts/BaseLayout.astro` returns 0 matches.
    - `npx astro check` reports 0 errors, 0 warnings.
  </acceptance_criteria>
  <verify>
    <automated>npx astro check && grep -rn "site\.author\|authorProfile" src/components/AuthorBox.astro src/layouts/ArticleLayout.astro src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>AuthorBox.astro, ArticleLayout.astro, and BaseLayout.astro all import author from @/data/authors, no longer reference site.author/authorProfile, AuthorBox.astro's style block is byte-identical to before, site.editorialReviewer is untouched, and astro check is clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate about.astro and index.astro to authors.ts, then remove author/authorProfile from site.ts</name>
  <files>src/pages/about.astro, src/pages/index.astro, src/data/site.ts</files>
  <read_first>
    src/pages/about.astro
    src/pages/index.astro
    src/data/site.ts
    src/data/authors.ts
  </read_first>
  <action>
    **about.astro**:
    - Add `import { author } from "@/data/authors"` alongside the existing `import { categories, getArticlePath, site } from "@/data/site"` (line 4) — keep the `site` import (still needed for `site.email`, `site.name`).
    - Line 13: `site.author.split(" ").pop()?.charAt(0) ?? site.author.charAt(0)` -> `author.name.split(" ").pop()?.charAt(0) ?? author.name.charAt(0)`.
    - Line 27: `<h1>{site.author}</h1>` -> `<h1>{author.name}</h1>`.
    - Line 28: `<p class="author-role">{site.authorProfile.role}</p>` -> `<p class="author-role">{author.role}</p>`.
    - Line 35: `{site.authorProfile.expertise.map((item) => <li>{item}</li>)}` -> `{author.expertise.map((item) => <li>{item}</li>)}`.
    - Line 46: `<blockquote>"{site.authorProfile.moneyPerspective}"</blockquote>` -> `<blockquote>"{author.moneyPerspective}"</blockquote>`.
    - Line 52: `{site.author} phụ trách nội dung tại {site.name}, ...` -> `{author.name} phụ trách nội dung tại {site.name}, ...` (keep `{site.name}` unchanged).
    - Line 66: `Mới nhất từ {site.author.split(" ").pop()}` -> `Mới nhất từ {author.name.split(" ").pop()}`.
    - Line 103: REPLACE the array-mapping rendering with a single-paragraph rendering, since `author.experience` in `src/data/authors.ts` is a STRING per D-02 (not `string[]`). Change `<ul>{site.authorProfile.experience.map((item) => <li>{item}</li>)}</ul>` to `<p>{author.experience}</p>`. The surrounding `.profile-card` wrapper (`<div class="profile-card"><h2>Kinh nghiệm nội dung</h2>...</div>`) stays — only the inner `<ul>`/`.map()` becomes a `<p>`. Note: the `.profile-card p` CSS rule already exists (selector list includes `.profile-card p, .profile-card li` at line ~360-365) so no new styles are needed — `<p>` picks up the existing `.profile-card p` styling.
    - Line 109: `{site.authorProfile.education}` -> `{author.education}`.
    - Line 115: `{site.authorProfile.publishedIn.map((item) => <span>{item}</span>)}` -> `{author.publishedIn.map((item) => <span>{item}</span>)}`.

    **index.astro**:
    - Add `import { author } from "@/data/authors"` alongside the existing `import { categories, categoryMeta, getCategoryPath, getArticlePath, site } from "@/data/site"` (line 5) — keep the `site` import (still needed for `site.name`, `site.description`, `site.trustStats`).
    - Line 176: `<h2 class="section-title">{site.author}</h2>` -> `<h2 class="section-title">{author.name}</h2>`.

    **src/data/site.ts** (do this LAST, after both files above are confirmed migrated):
    - Re-run `grep -rn "site\.author\|authorProfile" src/` to confirm only `src/data/site.ts` itself still defines these fields (per RESEARCH.md Open Question 1 — confirm no other consumer was missed since research was written).
    - Remove the `author: "Nguyễn Viết Lộc",` field (line 3) and the entire `authorProfile: {...}` block (lines 15-23) from the `site` object.
    - Leave `editorialReviewer` (line 7) and every other field in `site` untouched.
    - Do NOT add a derived `author: author.name` alias to `site.ts` — per the grep confirmation in this task, all 5 known consumers have been migrated to import `author` from `@/data/authors` directly, so no alias is needed (per RESEARCH.md Open Question 1 recommendation: only add an alias if a remaining non-author-data consumer needs a plain string, which the re-grep should show does not exist).
  </action>
  <acceptance_criteria>
    - `src/pages/about.astro` imports `{ author }` from `"@/data/authors"` and contains zero references to `site.author` or `site.authorProfile`.
    - `src/pages/about.astro` line corresponding to "Kinh nghiệm nội dung" renders `<p>{author.experience}</p>` (not a `.map()`/`<ul>`/`<li>`).
    - `src/pages/index.astro` imports `{ author }` from `"@/data/authors"` and line 176's section title renders `{author.name}`.
    - `src/data/site.ts` does not contain the substring `authorProfile` and does not contain a top-level `author:` field on the `site` object.
    - `src/data/site.ts` still contains `editorialReviewer: "Ban biên tập ValueInvesting.com.vn",` unchanged.
    - `grep -rn "site\.author\b\|authorProfile" src/` returns 0 matches across the entire `src/` tree.
    - `npx astro check && npx astro build` completes with exit code 0 (0 errors, 0 warnings — matching baseline).
  </acceptance_criteria>
  <verify>
    <automated>npx astro check && npx astro build && grep -rn "site\.author\|authorProfile" src/ | wc -l</automated>
  </verify>
  <done>about.astro and index.astro read all author data from @/data/authors (with about.astro's experience section rendered as a single paragraph per D-02's string type), site.ts no longer defines author/authorProfile while editorialReviewer remains untouched, grep for site.author|authorProfile across src/ returns 0, and astro check + astro build are both green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | All changes are build-time static data wiring between TypeScript modules and Astro templates — same trust level as existing `site.ts` consumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | JSON-LD `author`/`founder` fields in `ArticleLayout.astro`/`BaseLayout.astro` now sourced from `authors.ts` instead of `site.ts` | accept | Both source modules are static, build-time-only, developer-authored data — no change in trust level. Value (`author.name`) is identical string before and after migration. |
| T-02-06 | Repudiation/Spoofing | `site.editorialReviewer` (Organization reviewedBy) accidentally merged into author refactor | mitigate | Explicitly excluded in Task 1 action (line 50 of ArticleLayout.astro left unchanged); acceptance criteria assert `"name": site.editorialReviewer,` remains present and unchanged. |
</threat_model>

<verification>
- `npx astro check && npx astro build` — 0 errors, 0 warnings, all 21 articles build (matches pre-Phase-2 baseline)
- `grep -rn "site\.author\|authorProfile" src/` — 0 matches (manual verification per 02-VALIDATION.md)
- Manual visual spot-check (per 02-UI-SPEC.md Verification Checklist): one article page (AuthorBox + JSON-LD), `/about/`, and `/` (homepage) render identical text/markup before and after — author name, role, bio, expertise tags, moneyPerspective quote, experience paragraph, education line, publishedIn tags all match prior wording
</verification>

<success_criteria>
- EEAT-02 fully satisfied: `src/data/authors.ts` is the single source of author/reviewer profile data; `AuthorBox.astro`, `ArticleLayout.astro`, `BaseLayout.astro`, `about.astro`, `index.astro` all resolve author data from it with zero duplication in `site.ts`
- `AuthorBox.astro` markup/styles unchanged (D-06) — verified via the 02-UI-SPEC.md "No Visual Change" regression checklist
- `site.editorialReviewer` untouched (Pitfall 2 boundary respected)
</success_criteria>

## Artifacts this phase produces

- `src/components/AuthorBox.astro` (MODIFIED): now imports `{ author }` from `@/data/authors`; `.author-bio` text now sourced from `author.bio` (value unchanged)
- `src/layouts/ArticleLayout.astro` (MODIFIED): adds `import { author } from "@/data/authors"`; JSON-LD `articleSchema.author.name`, `articleMeta.author`, and byline now use `author.name`
- `src/layouts/BaseLayout.astro` (MODIFIED): adds `import { author } from "@/data/authors"`; Organization `founder.name` and `<meta name="author">` now use `author.name`
- `src/pages/about.astro` (MODIFIED): adds `import { author } from "@/data/authors"`; all author-profile fields (name, role, expertise, moneyPerspective, experience, education, publishedIn) now sourced from `author`; "Kinh nghiệm nội dung" section now renders `<p>{author.experience}</p>` instead of a `<ul>`/`.map()` over an array
- `src/pages/index.astro` (MODIFIED): adds `import { author } from "@/data/authors"`; homepage author section heading uses `author.name`
- `src/data/site.ts` (MODIFIED): `author` and `authorProfile` fields removed from the `site` object; `editorialReviewer` and all other fields unchanged

<output>
Create `.planning/phases/02-eeat-data-model-schema-extensions/02-03-SUMMARY.md` when done
</output>
