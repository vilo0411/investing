---
phase: 03-new-eeat-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/KeyTakeaways.astro
  - src/components/CitationBox.astro
  - src/components/AuthorBox.astro
autonomous: true
requirements: [EEAT-03, EEAT-04, EEAT-05]

must_haves:
  truths:
    - "KeyTakeaways renders a bordered box with checklist items when items is non-empty, and renders nothing when items=[]"
    - "CitationBox renders a sourced-list box with a last-checked/last-updated date line, using citations when present and falling back to sources when not"
    - "AuthorBox shows a Chứng chỉ (credentials) section only when author.credentials is non-empty, always shows experience text, and links to /author/{slug} instead of /about/"
  artifacts:
    - path: "src/components/KeyTakeaways.astro"
      provides: "EEAT-03 Key Takeaways box component"
      contains: "interface Props"
    - path: "src/components/CitationBox.astro"
      provides: "EEAT-04 Citation/Fact-check box component"
      contains: "interface Props"
    - path: "src/components/AuthorBox.astro"
      provides: "EEAT-05 AuthorBox v2 with credentials/experience/profile link"
      contains: "author.credentials"
  key_links:
    - from: "src/components/AuthorBox.astro"
      to: "src/data/authors.ts"
      via: "import { author } from \"@/data/authors\""
      pattern: "author\\.(credentials|experience|slug)"
    - from: "src/components/CitationBox.astro"
      to: "src/content.config.ts"
      via: "props shaped after citations/sources/factCheckedDate/updatedDate schema"
      pattern: "factCheckedDate|updatedDate"
---

<objective>
Build two new standalone EEAT presentational components (KeyTakeaways, CitationBox) and upgrade the existing AuthorBox to v2 (credentials, experience, /author/{slug} link), all following the existing AuthorBox border-left box pattern and Phase 1 design tokens. No wiring into ArticleLayout or new routes — components are validated in isolation (Plan 03 builds the preview page).

Purpose: Deliver the data-driven trust/summary components required by EEAT-03, EEAT-04, EEAT-05, ready for Phase 4 layout wiring.
Output: `src/components/KeyTakeaways.astro` (new), `src/components/CitationBox.astro` (new), `src/components/AuthorBox.astro` (modified in place).
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `src/components/KeyTakeaways.astro` (NEW) — exports default Astro component, `interface Props { items: string[] }`, CSS classes `.key-takeaways`, `.kt-label`
- `src/components/CitationBox.astro` (NEW) — exports default Astro component, `interface Props { sources?: string[]; citations?: { title: string; url?: string; publisher?: string; date?: string }[]; factCheckedDate?: Date; updatedDate: Date }`, CSS classes `.citation-box`, `.citation-label`
- `src/components/AuthorBox.astro` (MODIFIED, additive) — new markup: `.author-credentials` block (conditional on `author.credentials.length > 0`), `<p class="author-experience">`; new CSS classes `.author-sublabel`, `.author-credentials ul`, `.author-experience`; link targets changed from `/about/` to `` `/author/${author.slug}` ``
</artifacts_produced>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-new-eeat-components/03-CONTEXT.md
@.planning/phases/03-new-eeat-components/03-RESEARCH.md
@.planning/phases/03-new-eeat-components/03-PATTERNS.md
@.planning/phases/03-new-eeat-components/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create KeyTakeaways.astro (EEAT-03, per D-01/D-02/D-03)</name>
  <files>src/components/KeyTakeaways.astro</files>
  <read_first>
    src/components/AuthorBox.astro
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/KeyTakeaways.astro` as a new Astro component. Frontmatter: `interface Props { items: string[]; }`, destructure `const { items } = Astro.props;`. Per D-03, if `items.length === 0`, render nothing (wrap the entire `<aside>` in `{items.length > 0 && (...)}` — do not return a placeholder or empty box).

    When `items.length > 0`, render `<aside class="key-takeaways" aria-label="Tóm tắt nội dung chính">` containing:
    - `<p class="kt-label">Tóm tắt nội dung chính</p>` (exact copy per UI-SPEC copywriting contract)
    - `<ul>` with one `<li>✓ {item}</li>` per item in `items`

    Scoped `<style>` block at end of file, mirroring `.author-box` box pattern from AuthorBox.astro:
    - `.key-takeaways`: `padding: var(--space-6); border: 1px solid var(--line); border-radius: var(--radius-md); background: var(--surface); border-left: 4px solid var(--color-brand-900);`
    - `.kt-label`: `margin: 0 0 var(--space-2); font-size: 0.78rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-accent);` (copy `.author-label` styling)
    - `.key-takeaways ul`: `margin: 0; padding-left: 0; list-style: none; display: grid; gap: var(--space-2);`
    - `.key-takeaways li`: `font-size: 0.9rem; color: var(--muted); line-height: 1.6;` (match `.author-bio`/`.author-note` body text style)

    Use only existing design tokens (`--space-*`, `--color-brand-900`, `--color-accent`, `--surface`, `--line`, `--radius-md`, `--muted`) — no hardcoded hex values, no new tokens.
  </action>
  <acceptance_criteria>
    - File `src/components/KeyTakeaways.astro` exists with `interface Props { items: string[]; }`
    - Source contains `items.length > 0 &&` (or equivalent conditional) guarding the `<aside>` — confirms D-03 empty-state behavior
    - Source contains the exact string `Tóm tắt nội dung chính`
    - Source contains `✓` checkmark prefix on list items
    - `npx astro check` exits 0 with no errors referencing KeyTakeaways.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>KeyTakeaways.astro exists, accepts `items: string[]`, renders a border-left box with checklist when populated, renders nothing when `items=[]`, uses only design tokens, and passes `astro check`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CitationBox.astro (EEAT-04, per D-04/D-05/D-06)</name>
  <files>src/components/CitationBox.astro</files>
  <read_first>
    src/components/AuthorBox.astro
    src/layouts/ArticleLayout.astro
    src/content.config.ts
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/CitationBox.astro`. Frontmatter `interface Props`:
    - `sources?: string[]` (legacy 21-article field)
    - `citations?: { title: string; url?: string; publisher?: string; date?: string }[]` (Phase 2 field, matches `content.config.ts` shape exactly)
    - `factCheckedDate?: Date` (matches `z.coerce.date().optional()` — type is `Date`, NOT string, per Pitfall 2)
    - `updatedDate: Date` (required, fallback source)

    Destructure with defaults: `const { sources = [], citations = [], factCheckedDate, updatedDate } = Astro.props;`. Compute `const hasAny = sources.length > 0 || citations.length > 0;`. If `!hasAny`, render nothing (wrap `<aside>` in `{hasAny && (...)}`).

    When `hasAny`, render `<aside class="citation-box" aria-label="Nguồn tham khảo và kiểm tra">` containing:
    - `<p class="citation-label">` with date line: if `factCheckedDate` is set, text is `` `Kiểm tra nguồn lần cuối: ${factCheckedDate.toLocaleDateString("vi-VN")}` `` (D-05); else fallback `` `Cập nhật lần cuối: ${updatedDate.toLocaleDateString("vi-VN")}` ``. Use `.toLocaleDateString("vi-VN")` exactly as `ArticleLayout.astro` line 127 does for `updatedDate` (Pitfall 2 — never render raw `Date`).
    - `<h2>` or similar heading with exact text "Nguồn tham khảo" (per UI-SPEC section heading)
    - `<ul>`: if `citations.length > 0`, render one `<li>` per citation — title as `<a href={c.url} target="_blank" rel="noopener noreferrer">{c.title}</a>` when `c.url` present else plain `{c.title}`, append ` — {c.publisher}` if `c.publisher` present, append ` ({c.date})` if `c.date` present. Else (citations empty), render one `<li>{s}</li>` per string in `sources` (no link/publisher/date — plain text per D-04).

    Scoped `<style>` block: reuse the same box pattern as `.author-box`/`.key-takeaways` — `.citation-box`: `padding: var(--space-6); border: 1px solid var(--line); border-radius: var(--radius-md); background: var(--surface); border-left: 4px solid var(--color-brand-900);` (D-06: box, not thin horizontal strip). `.citation-label`: same eyebrow style as `.kt-label`/`.author-label` (`font-size: 0.78rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--color-accent);`). List items: `font-size: 0.9rem; color: var(--muted); line-height: 1.6;`. Citation links: `color: var(--brand);`.

    Use `set:html` NOWHERE in this component — use `{expression}` interpolation for all citation title/url/publisher/date values (Astro auto-escapes), per security guidance in RESEARCH.md (T-3-01).
  </action>
  <acceptance_criteria>
    - File `src/components/CitationBox.astro` exists with `interface Props` containing `sources?: string[]`, `citations?: { title: string; url?: string; publisher?: string; date?: string }[]`, `factCheckedDate?: Date`, `updatedDate: Date`
    - Source contains `toLocaleDateString("vi-VN")` applied to both `factCheckedDate` and `updatedDate` branches
    - Source contains the exact string `Kiểm tra nguồn lần cuối:` and `Cập nhật lần cuối:`
    - Source contains `Nguồn tham khảo` heading text
    - Source contains `target="_blank" rel="noopener noreferrer"` on citation links and does NOT contain `set:html`
    - `npx astro check` exits 0 with no errors referencing CitationBox.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>CitationBox.astro exists, accepts sources/citations/factCheckedDate/updatedDate props matching content.config.ts shapes, renders fallback date logic per D-05, renders citations-or-sources list per D-04, uses box styling per D-06, and passes `astro check`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Upgrade AuthorBox.astro to v2 (EEAT-05, per D-07/D-08/D-09)</name>
  <files>src/components/AuthorBox.astro</files>
  <read_first>
    src/components/AuthorBox.astro
    src/data/authors.ts
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
    .planning/phases/03-new-eeat-components/03-PATTERNS.md
  </read_first>
  <action>
    Modify `src/components/AuthorBox.astro` in place (existing import `import { author } from "@/data/authors";` stays unchanged). Make these additive changes only — preserve avatar, name, role, bio, expertise tags, author-note unchanged:

    1. Change both profile link targets from `/about/` to `` `/author/${author.slug}` ``: the `.author-name` link (currently `href="/about/"` on the name `<a>`) and the `.author-profile-link` "Xem hồ sơ" link (currently `href="/about/"`). `author.slug` is `"nguyen-viet-loc"` per `src/data/authors.ts`. Accept the temporary 404 on `/author/nguyen-viet-loc` until Phase 4 builds that route (per D-09 — this is expected and acceptable).

    2. After the existing `.author-expertise` `<div>` block, insert a conditional credentials block (D-07/D-08): `{author.credentials.length > 0 && (<div class="author-credentials" aria-label="Chứng chỉ chuyên môn"><p class="author-sublabel">Chứng chỉ</p><ul>{author.credentials.map((c) => <li>{c}</li>)}</ul></div>)}`. Since `author.credentials` is currently `[]` in `src/data/authors.ts`, this block will not render on the live site today — that is correct per D-08 (no fallback text, section hidden entirely).

    3. Immediately after the credentials block, add `<p class="author-experience">{author.experience}</p>` — always rendered (unconditional), since `author.experience` is already a non-empty string in `src/data/authors.ts`.

    4. Add new scoped styles: `.author-sublabel` — same eyebrow shape as `.author-label` but `color: var(--muted)` (per UI-SPEC, to differentiate from the primary gold `.author-label`): `margin: 0 0 var(--space-1); font-size: 0.78rem; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: var(--muted);`. `.author-credentials ul`: `margin: 0; padding-left: 0; list-style: none; display: grid; gap: var(--space-1);`. `.author-experience`: copy `.author-bio`/`.author-note` styling — `margin: var(--space-1) 0 0; font-size: 0.9rem; color: var(--muted); line-height: 1.6;` (can be grouped with the existing `.author-bio, .author-note` selector by adding `.author-experience` to that selector list, or as its own rule with identical declarations).

    Do not change `.author-box`, `.author-avatar`, `.author-heading`, `.author-label`, `.author-name`, `.author-profile-link`, `.author-role`, `.author-expertise` rules, and do not change the existing `@media (max-width: 560px)` block (credentials/experience inherit the column layout automatically).
  </action>
  <acceptance_criteria>
    - `src/components/AuthorBox.astro` contains `` href={`/author/${author.slug}`} `` for both the author name link and the "Xem hồ sơ" link, and contains zero remaining occurrences of `href="/about/"`
    - Source contains `author.credentials.length > 0 &&` guarding an `<div class="author-credentials"` block with `aria-label="Chứng chỉ chuyên môn"` and sub-label text `Chứng chỉ`
    - Source contains unconditional `<p class="author-experience">{author.experience}</p>`
    - Source still contains `.author-box`, `.author-avatar`, `.author-expertise` rules unchanged (verify by `grep -c "author-box\|author-avatar\|author-expertise" src/components/AuthorBox.astro` returns >= 3)
    - `npx astro check` exits 0 with no errors referencing AuthorBox.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>AuthorBox.astro renders credentials section only when non-empty, always shows experience text, links both name and "Xem hồ sơ" to /author/{slug}, preserves all v1 elements, and passes `astro check`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content-author frontmatter -> rendered HTML | `citations[].title/url/publisher/date`, `sources[]`, `author.credentials`/`experience` originate from build-time markdown/TS files (content authors), rendered into article pages seen by anonymous visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering / Information Disclosure | CitationBox.astro citation links | mitigate | Use `{expression}` interpolation (Astro auto-escape) for all citation title/url/publisher/date; never `set:html`. External links get `target="_blank" rel="noopener noreferrer"` to prevent `window.opener` tabnabbing. |
| T-3-02 | Tampering | KeyTakeaways.astro items list | mitigate | `{item}` interpolation only, Astro auto-escapes string content — no `set:html` |
| T-3-03 | Information Disclosure | AuthorBox.astro `/author/{slug}` link to not-yet-built route | accept | Temporary 404 until Phase 4 ships `/author/[slug]`; static site, no sensitive data exposed via 404; explicitly accepted per D-09 |
</threat_model>

<verification>
Run `npx astro check` after each task (per Validation Strategy sampling rate). Run `npm run build` after all three tasks complete to confirm the modified AuthorBox.astro and two new components compile cleanly (full build will fail only if a consuming page references them with mismatched props — Plan 03's preview page is the first such consumer, so this plan's build check covers compile/type errors in isolation via `astro check` on the component files themselves and the unchanged existing pages that import AuthorBox).
</verification>

<success_criteria>
- `src/components/KeyTakeaways.astro` exists, accepts `items: string[]`, empty array renders nothing
- `src/components/CitationBox.astro` exists, accepts `sources?`/`citations?`/`factCheckedDate?`/`updatedDate`, correct date fallback and citation/source rendering
- `src/components/AuthorBox.astro` modified in place: credentials conditional, experience always shown, both profile links point to `/author/{slug}`
- `npx astro check` passes with zero errors across all three files
- All 21 existing articles still build (AuthorBox is wired into ArticleLayout and ships to every article — `npm run build` must remain green)
</success_criteria>

<output>
Create `.planning/phases/03-new-eeat-components/03-01-SUMMARY.md` when done
</output>
</content>
