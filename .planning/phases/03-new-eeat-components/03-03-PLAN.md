---
phase: 03-new-eeat-components
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/pages/_preview/eeat-components.astro
  - astro.config.mjs
autonomous: false
requirements: [EEAT-03, EEAT-04, EEAT-05, EEAT-07, EEAT-08, EEAT-09]

must_haves:
  truths:
    - "Visiting /_preview/eeat-components/ in npm run dev renders all 6 EEAT components, each with a populated-data variant and (where applicable) an empty/fallback-data variant"
    - "npm run build succeeds (astro check + astro build) with the preview page importing all 6 components"
    - "/_preview/ route is excluded from sitemap-index.xml, consistent with the existing kien-thuc exclusion precedent"
  artifacts:
    - path: "src/pages/_preview/eeat-components.astro"
      provides: "Isolated test harness rendering all 6 Phase 3 components in populated + empty/fallback states"
      contains: "import KeyTakeaways"
    - path: "astro.config.mjs"
      provides: "Sitemap filter excluding /_preview/ route"
      contains: "_preview"
  key_links:
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/KeyTakeaways.astro"
      via: "import KeyTakeaways from \"@/components/KeyTakeaways.astro\""
      pattern: "import KeyTakeaways"
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/CitationBox.astro"
      via: "import CitationBox from \"@/components/CitationBox.astro\""
      pattern: "import CitationBox"
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/AuthorBox.astro"
      via: "import AuthorBox from \"@/components/AuthorBox.astro\""
      pattern: "import AuthorBox"
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/Breadcrumb.astro"
      via: "import Breadcrumb from \"@/components/Breadcrumb.astro\""
      pattern: "import Breadcrumb"
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/Disclaimer.astro"
      via: "import Disclaimer from \"@/components/Disclaimer.astro\""
      pattern: "import Disclaimer"
    - from: "src/pages/_preview/eeat-components.astro"
      to: "src/components/ComparisonTable.astro"
      via: "import ComparisonTable from \"@/components/ComparisonTable.astro\""
      pattern: "import ComparisonTable"
    - from: "astro.config.mjs"
      to: "sitemap integration filter function"
      via: "filter excludes pages containing /_preview/"
      pattern: "_preview"
---

<objective>
Build the Wave-0/integration test harness: a single isolated preview page that imports and renders all 6 Phase 3 EEAT components (KeyTakeaways, CitationBox, AuthorBox, Breadcrumb, Disclaimer, ComparisonTable) with both populated and empty/fallback prop sets, confirm `npm run build` is green, exclude the route from the sitemap, and perform the manual visual verification checkpoint required by the Validation Strategy.

Purpose: This page IS the test harness for Phase 3 (no test framework exists) — it proves all 6 components render correctly in isolation per EEAT-03/04/05/07/08/09 and the UI Design Contract, satisfying ROADMAP Phase 3 Success Criteria.
Output: `src/pages/_preview/eeat-components.astro` (new), `astro.config.mjs` (sitemap filter updated).
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `src/pages/_preview/eeat-components.astro` (NEW) — static Astro page, no `getStaticPaths`, route `/_preview/eeat-components/`; imports and renders `KeyTakeaways`, `CitationBox`, `AuthorBox`, `Breadcrumb`, `Disclaimer`, `ComparisonTable` from `@/components/*.astro` inside `BaseLayout`
- `astro.config.mjs` (MODIFIED, additive) — sitemap `filter` function gains an additional `&& !page.includes("/_preview/")` condition alongside the existing `/kien-thuc/` and legacy-URL exclusions
</artifacts_produced>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-new-eeat-components/03-CONTEXT.md
@.planning/phases/03-new-eeat-components/03-RESEARCH.md
@.planning/phases/03-new-eeat-components/03-UI-SPEC.md
@.planning/phases/03-new-eeat-components/03-VALIDATION.md

# Components created by 03-01 and 03-02 (read their exact Props before writing the preview page)
@src/components/KeyTakeaways.astro
@src/components/CitationBox.astro
@src/components/AuthorBox.astro
@src/components/Breadcrumb.astro
@src/components/Disclaimer.astro
@src/components/ComparisonTable.astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the isolated preview page rendering all 6 components</name>
  <files>src/pages/_preview/eeat-components.astro</files>
  <read_first>
    src/pages/about.astro
    src/layouts/BaseLayout.astro
    src/data/authors.ts
    src/content.config.ts
    src/components/KeyTakeaways.astro
    src/components/CitationBox.astro
    src/components/AuthorBox.astro
    src/components/Breadcrumb.astro
    src/components/Disclaimer.astro
    src/components/ComparisonTable.astro
  </read_first>
  <action>
    Create `src/pages/_preview/eeat-components.astro`, a static page (no `getStaticPaths`) following the `src/pages/about.astro` structure: import `BaseLayout` from `@/layouts/BaseLayout.astro` and wrap content in `<BaseLayout title="EEAT Components Preview" description="Isolated preview of Phase 3 EEAT components for visual verification.">`.

    Import all 6 components with the `@/*` alias: `KeyTakeaways`, `CitationBox`, `AuthorBox`, `Breadcrumb`, `Disclaimer`, `ComparisonTable` from `@/components/*.astro`.

    For each component, render a labeled section (e.g. `<h2>KeyTakeaways — populated</h2>` followed by the component, then `<h2>KeyTakeaways — empty</h2>` followed by the empty variant) so each state is visually distinguishable during manual review. Render in this order, matching the variants below:

    1. **KeyTakeaways**: populated with `items={["Đa dạng hóa giúp giảm rủi ro tổng thể", "Chi phí thấp là lợi thế dài hạn của ETF", "Không có chiến lược đầu tư nào đảm bảo lợi nhuận"]}`; empty with `items={[]}` (per D-03 this renders nothing — add a text note like "(no output expected below this line)" in the page copy, not inside the component).

    2. **CitationBox**: variant A — `citations` populated (e.g. 2 entries with `title`, `url`, `publisher`, `date`) and `factCheckedDate={new Date("2026-06-10")}`, `updatedDate={new Date("2026-06-01")}` (exercises the `factCheckedDate` branch and the `citations` branch). Variant B — `sources={["Ủy ban Chứng khoán Nhà nước", "Sở Giao dịch Chứng khoán TP.HCM"]}`, no `citations`, no `factCheckedDate`, `updatedDate={new Date("2026-05-15")}` (exercises the fallback-to-`updatedDate` branch and the `sources`-only branch). Variant C — both `sources={[]}` and `citations={[]}`, `updatedDate={new Date()}` (renders nothing per the `hasAny` guard).

    3. **AuthorBox**: render once — it reads from `src/data/authors.ts` directly (no props), so it always shows the current `author.credentials=[]` (hidden block) + `author.experience` (shown) + `/author/{slug}` links. Add a text note explaining the credentials block is hidden because `authors.ts` currently has `credentials: []` (D-08 path), and that the populated-credentials path is exercised structurally by the conditional logic itself (no separate mock-author fixture needed — do not create one).

    4. **Breadcrumb**: variant A (article-style) — `items={[{label: "Trang chủ", href: "/"}, {label: "Đầu tư", href: "/dau-tu/"}, {label: "ETF", href: "/dau-tu/etf/"}, {label: "ETF là gì?"}]}`. Variant B (category-style) — `items={[{label: "Trang chủ", href: "/"}, {label: "Đầu tư", href: "/dau-tu/"}, {label: "Cổ phiếu"}]}`.

    5. **Disclaimer**: variant A — no props (uses default `site.disclosure`). Variant B — `text="Đây là nội dung minh họa cho mục đích xem trước component."` (custom text override).

    6. **ComparisonTable**: populated with realistic financial comparison data, e.g. `columns={["Loại quỹ", "Phí quản lý", "Mức độ rủi ro", "Phù hợp với"]}` and `rows={[["ETF VN30", "0.5%/năm", "Trung bình", "Nhà đầu tư dài hạn"], ["Quỹ trái phiếu", "1.0%/năm", "Thấp", "Nhà đầu tư bảo toàn vốn"], ["Quỹ cổ phiếu chủ động", "1.8%/năm", "Cao", "Nhà đầu tư chấp nhận rủi ro"]]}` and `caption="So sánh các loại quỹ đầu tư phổ biến"`. (ComparisonTable has required props only — no empty variant needed per UI-SPEC.)

    Match every prop name and type EXACTLY to each component's `interface Props` as written in 03-01/03-02 (read each component file first — do not guess prop names). `factCheckedDate`/`updatedDate`/`publishDate`-style date props must be passed as `Date` objects (`new Date("YYYY-MM-DD")`), matching `z.coerce.date()` typing.
  </action>
  <acceptance_criteria>
    - File `src/pages/_preview/eeat-components.astro` exists, imports all 6 components via `@/components/*.astro` paths, and wraps content in `BaseLayout`
    - Source contains at least 2 distinct prop-set invocations of `KeyTakeaways` (populated `items` and `items={[]}`)
    - Source contains at least 3 distinct prop-set invocations of `CitationBox` covering citations+factCheckedDate, sources-only+updatedDate fallback, and empty/both-empty
    - Source contains at least 2 distinct prop-set invocations of `Breadcrumb` (article-style and category-style `items`)
    - Source contains at least 2 distinct prop-set invocations of `Disclaimer` (default and custom `text`)
    - Source contains one `ComparisonTable` invocation with `columns`, `rows`, and `caption` props
    - `npx astro check` exits 0 with no errors referencing `src/pages/_preview/eeat-components.astro`
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>The preview page exists, imports and renders all 6 components with the prop variants specified above, type-checks cleanly via `astro check`, and is ready for `npm run build` + manual visual review.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Exclude /_preview/ from sitemap and run full build</name>
  <files>astro.config.mjs</files>
  <read_first>
    astro.config.mjs
  </read_first>
  <action>
    Edit `astro.config.mjs`: extend the existing `sitemap({ filter: (page) => ... })` function's condition to also exclude any page URL containing `/_preview/`. The current filter is:

    `filter: (page) => !page.includes("/kien-thuc/") && ![...4 legacy URLs].includes(page)`

    Add a third condition using the same `&&` chain: `&& !page.includes("/_preview/")`. Do not change the existing `/kien-thuc/` exclusion or the legacy-URL array — this is an additive condition only.

    After editing, run `npm run build` (= `astro check && astro build`) from the project root. This is the full-suite verification for Plan 03 and the integration check for Plans 01/02/03 combined — it must complete with exit code 0 and produce `dist/sitemap-index.xml`/`dist/sitemap-0.xml` (or equivalent) that does NOT contain any `/_preview/` URL.
  </action>
  <acceptance_criteria>
    - `astro.config.mjs` sitemap `filter` function contains `!page.includes("/_preview/")` chained with `&&` alongside the existing `/kien-thuc/` and legacy-URL conditions
    - `npm run build` exits 0
    - After build, `grep -r "_preview" dist/sitemap*.xml` (or `dist/**/sitemap*.xml`) returns no matches (or sitemap files contain zero `_preview` entries)
    - `dist/_preview/eeat-components/index.html` (or equivalent build output path) exists — confirms the page IS built, just excluded from the sitemap
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>

  <done>astro.config.mjs sitemap filter excludes /_preview/ pages; npm run build succeeds; the preview page is present in dist/ output but absent from generated sitemap files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual visual verification of all 6 components in preview page</name>
  <files>src/pages/_preview/eeat-components.astro</files>
  <action>Pause for human review of the rendered preview page per the steps in how-to-verify below. No code changes in this task.</action>
  <what-built>
    All 6 Phase 3 EEAT components (KeyTakeaways, CitationBox, AuthorBox v2, Breadcrumb, Disclaimer, ComparisonTable) plus an isolated preview page at `/_preview/eeat-components/` rendering each in populated and empty/fallback states. `npm run build` passes and the route is excluded from the sitemap.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit `http://localhost:4321/_preview/eeat-components/` (port per project default).
    2. KeyTakeaways: confirm the populated variant shows a border-left accent box titled "Tóm tắt nội dung chính" with 3 items, each prefixed "✓"; confirm the empty variant renders nothing (no box, no placeholder).
    3. CitationBox: confirm variant A shows "Kiểm tra nguồn lần cuối: 10/6/2026" with 2 linked citation entries (publisher/date shown, links open in new tab); confirm variant B shows "Cập nhật lần cuối: 15/5/2026" with 2 plain-text source lines (no links); confirm variant C (both empty) renders nothing.
    4. AuthorBox: confirm "Xem hồ sơ" and the author name link both point to `/author/nguyen-viet-loc` (will 404 if clicked — expected per D-09); confirm the credentials section is absent (since `authors.ts` has `credentials: []`); confirm the experience paragraph is visible below the expertise tags.
    5. Breadcrumb: confirm both variants render "Trang chủ › Đầu tư › ... › {last item}" with all but the last item as clickable links, and the last item as plain (non-link) current-page text.
    6. Disclaimer: confirm both variants show a border-left accent box headed "Lưu ý quan trọng:" with the respective body text.
    7. ComparisonTable: confirm the table renders with a dark-blue header row (white text), zebra-striped body rows, and a caption below/above the table; resize the browser to mobile width (or use devtools responsive mode) and confirm the table scrolls horizontally within its wrapper without breaking the page layout.
    8. Confirm `npm run build` (already run in Task 2) completed with exit 0 and no `astro check` errors.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 components render correctly in both states per the steps above, or describe any visual/behavioral issues found.</resume-signal>
  <verify>
    <automated>MISSING — manual-only checkpoint, no automated check (preview page validated via astro check and npm run build in Tasks 1-2)</automated>
  </verify>

  <done>User confirms "approved" after reviewing all 6 components in populated and empty/fallback states at /_preview/eeat-components/, or reports issues to be addressed before phase completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preview page route -> public build output | `/_preview/eeat-components/` is built and reachable by direct URL even though excluded from sitemap/nav — low-risk static informational page, no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Information Disclosure | `/_preview/eeat-components/` reachable orphan page | accept | Static informational preview page with no sensitive/PII data (only mock citation/comparison data already public); excluded from sitemap per astro.config.mjs filter — same accepted pattern as `kien-thuc` legacy routes |
</threat_model>

<verification>
- `npx astro check` after Task 1 (per Validation Strategy sampling rate)
- `npm run build` after Task 2 — full suite, must be green (per Validation Strategy "After every plan wave")
- Manual visual check via `npm run dev` at `/_preview/eeat-components/` per the checkpoint task — covers all Manual-Only Verifications rows in 03-VALIDATION.md
</verification>

<success_criteria>
- `src/pages/_preview/eeat-components.astro` exists, imports and renders all 6 components with populated + empty/fallback variants matching each component's exact `interface Props`
- `astro.config.mjs` sitemap filter excludes `/_preview/`
- `npm run build` passes (astro check + astro build green)
- Sitemap output contains zero `/_preview/` entries; `dist/_preview/eeat-components/` build output exists
- Human checkpoint confirms all 6 components render correctly in both populated and empty/fallback states, including ComparisonTable mobile horizontal scroll
</success_criteria>

<output>
Create `.planning/phases/03-new-eeat-components/03-03-SUMMARY.md` when done
</output>
</content>
