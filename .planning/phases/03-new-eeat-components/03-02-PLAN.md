---
phase: 03-new-eeat-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Breadcrumb.astro
  - src/components/Disclaimer.astro
  - src/components/ComparisonTable.astro
autonomous: true
requirements: [EEAT-07, EEAT-08, EEAT-09]

must_haves:
  truths:
    - "Breadcrumb renders a navigable trail from a generic items array, with the last item shown as plain current-page text, working identically for article-style and category-style item lists"
    - "Disclaimer renders a token-styled YMYL risk-disclosure box, defaulting to site.disclosure text"
    - "ComparisonTable renders a horizontally-scrollable table with a brand-colored header row and zebra-striped body rows from columns/rows props"
  artifacts:
    - path: "src/components/Breadcrumb.astro"
      provides: "EEAT-07 generic breadcrumb navigation component"
      contains: "interface Props"
    - path: "src/components/Disclaimer.astro"
      provides: "EEAT-08 disclaimer/risk-disclosure component"
      contains: "site.disclosure"
    - path: "src/components/ComparisonTable.astro"
      provides: "EEAT-09 comparison table component"
      contains: "overflow-x: auto"
  key_links:
    - from: "src/components/Disclaimer.astro"
      to: "src/data/site.ts"
      via: "import { site } from \"@/data/site\""
      pattern: "site\\.disclosure"
    - from: "src/components/Breadcrumb.astro"
      to: "caller (Plan 03 preview page)"
      via: "generic items: { label: string; href?: string }[] prop, decoupled from entry/categories"
      pattern: "items:\\s*\\{\\s*label"
---

<objective>
Build three new standalone EEAT presentational components (Breadcrumb, Disclaimer, ComparisonTable), following Phase 1 design tokens and the existing AuthorBox/ArticleLayout patterns. No wiring into ArticleLayout or new routes — components are validated in isolation (Plan 03 builds the preview page).

Purpose: Deliver the navigation, risk-disclosure, and data-comparison components required by EEAT-07, EEAT-08, EEAT-09, ready for Phase 4/5 layout wiring.
Output: `src/components/Breadcrumb.astro` (new), `src/components/Disclaimer.astro` (new), `src/components/ComparisonTable.astro` (new).
</objective>

<artifacts_produced>
## Artifacts this phase produces

- `src/components/Breadcrumb.astro` (NEW) — exports default Astro component, `interface Props { items: { label: string; href?: string }[] }`, CSS classes `.breadcrumb`, `.separator`
- `src/components/Disclaimer.astro` (NEW) — exports default Astro component, `interface Props { text?: string }` (defaults to `site.disclosure`), CSS class `.disclaimer`
- `src/components/ComparisonTable.astro` (NEW) — exports default Astro component, `interface Props { columns: string[]; rows: (string | number)[][]; caption?: string }`, CSS classes `.comparison-table-wrap`, `.comparison-table`
</artifacts_produced>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-new-eeat-components/03-CONTEXT.md
@.planning/phases/03-new-eeat-components/03-RESEARCH.md
@.planning/phases/03-new-eeat-components/03-PATTERNS.md
@.planning/phases/03-new-eeat-components/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Breadcrumb.astro (EEAT-07, generic items prop)</name>
  <files>src/components/Breadcrumb.astro</files>
  <read_first>
    src/layouts/ArticleLayout.astro
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
    .planning/phases/03-new-eeat-components/03-PATTERNS.md
  </read_first>
  <action>
    Create `src/components/Breadcrumb.astro`. Frontmatter: `interface Props { items: { label: string; href?: string }[]; }`, destructure `const { items } = Astro.props;`.

    Render `<nav class="breadcrumb" aria-label="Breadcrumb">` containing an `<ol>`. For each item except the last: `<li><a href={item.href}>{item.label}</a><span class="separator" aria-hidden="true">›</span></li>`. For the last item: `<li><span aria-current="page">{item.label}</span></li>` — plain text, no link, even if `item.href` is set (per UI-SPEC: "Last item: plain text (current page), aria-current=page, no link even if href provided").

    This component must NOT import or reference `entry`, `categories`, `site.ts`, or any article/category-specific data — it is purely generic, decoupled per CONTEXT.md Claude's Discretion and RESEARCH.md Anti-Patterns (do not duplicate the JSON-LD BreadcrumbList logic from ArticleLayout.astro lines 74-89, and do not couple to `isInvestingCategory`/`categoryGroupPath`). The caller (Plan 03 preview page, and Phase 4 ArticleLayout/category pages later) builds the `items` array.

    Scoped `<style>` block:
    - `.breadcrumb`: no border/box wrapper (flush, not a callout) — `font-size: 0.85rem;`
    - `.breadcrumb ol`: `display: flex; align-items: center; flex-wrap: wrap; gap: var(--space-2); margin: 0; padding: 0; list-style: none;`
    - `.breadcrumb li`: `display: flex; align-items: center; gap: var(--space-2);`
    - `.breadcrumb a`: `color: var(--brand); text-decoration: none;`
    - `.breadcrumb a:hover`: `text-decoration: underline;`
    - `.breadcrumb .separator`: `color: var(--muted);`
    - `.breadcrumb [aria-current="page"]`: `color: var(--color-neutral-400);` (matches existing inline breadcrumb's current-page color)

    Verify `var(--color-neutral-400)` exists in `src/styles/tokens/colors.css` before using it; if not present, use `var(--muted)` for the current-page color instead and note the substitution is intentional (token-only constraint takes priority over exact color match).
  </action>
  <acceptance_criteria>
    - File `src/components/Breadcrumb.astro` exists with `interface Props { items: { label: string; href?: string }[]; }`
    - Source contains `aria-label="Breadcrumb"` and `aria-current="page"`
    - Source does NOT import from `@/data/site` or `astro:content`, and does NOT reference `entry`, `categories`, `isInvestingCategory`
    - Source renders the last item without an `<a>` tag (plain text/span only) regardless of `href`
    - `npx astro check` exits 0 with no errors referencing Breadcrumb.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>Breadcrumb.astro exists, accepts generic `items: {label, href?}[]`, renders all-but-last as links separated by "›", renders last item as plain `aria-current="page"` text, uses only design tokens, has no box wrapper, and passes `astro check`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create Disclaimer.astro (EEAT-08, per Claude's Discretion + site.disclosure)</name>
  <files>src/components/Disclaimer.astro</files>
  <read_first>
    src/data/site.ts
    src/layouts/ArticleLayout.astro
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/Disclaimer.astro`. Frontmatter: `import { site } from "@/data/site";` and `interface Props { text?: string; }`, destructure `const { text = site.disclosure } = Astro.props;`. `site.disclosure` already contains the confirmed YMYL-appropriate string ("ValueInvesting.com.vn không cung cấp dịch vụ tư vấn đầu tư cá nhân...") — reuse it verbatim as the default, do not write new copy.

    Render `<aside class="disclaimer" aria-label="Lưu ý tài chính">` containing `<strong>Lưu ý quan trọng:</strong>` followed by `<span>{text}</span>` (exact heading text "Lưu ý quan trọng:" per UI-SPEC — this differs intentionally from the existing inline ArticleLayout wording "Lưu ý trước khi đọc:"; use the new UI-SPEC wording).

    Scoped `<style>` block, box pattern matching KeyTakeaways/CitationBox:
    - `.disclaimer`: `display: grid; gap: var(--space-1); padding: var(--space-6); border: 1px solid var(--line); border-radius: var(--radius-md); background: var(--surface); border-left: 4px solid var(--color-brand-900);`
    - `.disclaimer strong`: `color: var(--color-brand-900);`
    - `.disclaimer span`: `color: var(--muted); font-size: 0.96rem; line-height: 1.65;`
  </action>
  <acceptance_criteria>
    - File `src/components/Disclaimer.astro` exists with `import { site } from "@/data/site";` and `interface Props { text?: string; }` defaulting to `site.disclosure`
    - Source contains the exact string `Lưu ý quan trọng:` and `aria-label="Lưu ý tài chính"`
    - Source uses `var(--color-brand-900)`, `var(--surface)`, `var(--line)`, `var(--radius-md)`, `var(--muted)` — no hardcoded hex colors
    - `npx astro check` exits 0 with no errors referencing Disclaimer.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>Disclaimer.astro exists, defaults `text` to `site.disclosure`, renders a border-left box with "Lưu ý quan trọng:" heading per UI-SPEC, uses only design tokens, and passes `astro check`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create ComparisonTable.astro (EEAT-09, per D-10/D-11/D-12)</name>
  <files>src/components/ComparisonTable.astro</files>
  <read_first>
    src/components/AuthorBox.astro
    .planning/phases/03-new-eeat-components/03-RESEARCH.md
    .planning/phases/03-new-eeat-components/03-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/ComparisonTable.astro`. Frontmatter: `interface Props { columns: string[]; rows: (string | number)[][]; caption?: string; }`, destructure `const { columns, rows, caption } = Astro.props;` (D-10: structured props, not a markdown-table style wrapper).

    Render `<div class="comparison-table-wrap">` wrapping a `<table class="comparison-table">`. If `caption` is provided, render `<caption>{caption}</caption>` as the first child of `<table>`. `<thead><tr>{columns.map((col) => <th>{col}</th>)}</tr></thead>`. `<tbody>{rows.map((row) => <tr>{row.map((cell) => <td>{cell}</td>)}</tr>)}</tbody>`.

    Scoped `<style>` block per D-11/D-12:
    - `.comparison-table-wrap`: `overflow-x: auto; margin: var(--space-6) 0; border: 1px solid var(--line); border-radius: var(--radius-md);` (D-11 — horizontal scroll wrapper, table structure stays intact, no card-stacking)
    - `.comparison-table`: `width: 100%; border-collapse: collapse; font-size: 0.92rem;`
    - `.comparison-table th`: `background: var(--color-brand-900); color: #fff; text-align: left; padding: var(--space-3) var(--space-4); white-space: nowrap;` (D-12 — brand-color header, white text; `#fff` matches the existing accepted exception used for `.author-avatar` text color in AuthorBox.astro)
    - `.comparison-table td`: `padding: var(--space-3) var(--space-4); border-bottom: 1px solid var(--line);`
    - `.comparison-table tbody tr:nth-child(even)`: `background: var(--surface-alt);` (D-12 zebra striping — `--surface-alt` is defined in `src/styles/tokens/colors.css`)
  </action>
  <acceptance_criteria>
    - File `src/components/ComparisonTable.astro` exists with `interface Props { columns: string[]; rows: (string | number)[][]; caption?: string; }`
    - Source contains `overflow-x: auto` on the wrapper element
    - Source contains `background: var(--color-brand-900)` and `color: #fff` on the `th` rule
    - Source contains `tbody tr:nth-child(even)` with `background: var(--surface-alt)`
    - `npx astro check` exits 0 with no errors referencing ComparisonTable.astro
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>

  <done>ComparisonTable.astro exists, accepts columns/rows/caption structured props, wraps the table in an overflow-x:auto container, styles the header row with brand background + white text, applies zebra striping via --surface-alt, and passes `astro check`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content-author data -> rendered HTML | `Breadcrumb` items, `Disclaimer` text override, `ComparisonTable` columns/rows/caption all originate from build-time data (component callers), rendered to anonymous visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Tampering | Breadcrumb.astro item labels/hrefs | mitigate | `{expression}` interpolation only for `label`/`href`, Astro auto-escapes — no `set:html` |
| T-3-05 | Tampering | ComparisonTable.astro cell content | mitigate | `{cell}` interpolation only, Astro auto-escapes string/number cells — no `set:html`, no raw HTML table generation from strings |
| T-3-06 | Information Disclosure | Disclaimer.astro custom `text` prop | accept | Build-time only prop, no runtime user input; default sourced from existing `site.disclosure` constant |
</threat_model>

<verification>
Run `npx astro check` after each task (per Validation Strategy sampling rate). Since these are new files with no existing consumers, `astro check` on the files themselves (no Props mismatch possible until Plan 03's preview page imports them) is the primary per-task gate; Plan 03's preview page + `npm run build` is the integration verification.
</verification>

<success_criteria>
- `src/components/Breadcrumb.astro` exists, accepts generic `items: {label, href?}[]`, works for both article-style and category-style trails, last item is plain current-page text
- `src/components/Disclaimer.astro` exists, defaults to `site.disclosure`, renders YMYL box per UI-SPEC
- `src/components/ComparisonTable.astro` exists, accepts `columns`/`rows`/`caption`, renders scrollable table with brand header + zebra rows
- `npx astro check` passes with zero errors across all three new files
</success_criteria>

<output>
Create `.planning/phases/03-new-eeat-components/03-02-SUMMARY.md` when done
</output>
</content>
