---
phase: 04-article-layout-wiring-author-profile-pages
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/layouts/ArticleLayout.astro
autonomous: true
requirements: [ARTL-01, ARTL-02, EEAT-10]

must_haves:
  truths:
    - "Article pages render, top to bottom: Breadcrumb -> Key Takeaways -> TOC/Content -> Citation box -> AuthorBox v2 -> Share/Related (per ROADMAP Phase 4 success criterion 1)"
    - "JSON-LD articleSchema.author on article pages is a full Person object sourced from src/data/authors.ts (jobTitle, description, knowsAbout, url pointing to /author/{slug}/, sameAs only if socialLinks defined), matching what AuthorBox v2 displays"
    - "All 21 articles display their backfilled Key Takeaways (from Plan 01) and Citation box content (from entry.data.sources) without duplicated 'Nguồn tham khảo' or 'Tóm tắt nội dung chính' sections"
    - "No dead CSS remains from the removed inline breadcrumb (.breadcrumb) or removed .source-section after the swap"
  artifacts:
    - path: "src/layouts/ArticleLayout.astro"
      provides: "Rewired article template: Breadcrumb component, KeyTakeaways inside .article-content before slot, CitationBox replacing .source-section, expanded JSON-LD author Person object"
      contains: "Breadcrumb items={breadcrumbItems}"
  key_links:
    - from: "src/layouts/ArticleLayout.astro"
      to: "src/components/Breadcrumb.astro"
      via: "import + <Breadcrumb items={breadcrumbItems} />"
      pattern: "import Breadcrumb from \"@/components/Breadcrumb.astro\""
    - from: "src/layouts/ArticleLayout.astro"
      to: "src/components/KeyTakeaways.astro"
      via: "<KeyTakeaways items={entry.data.keyTakeaways} /> as first child of .article-content"
      pattern: "<KeyTakeaways items={entry\\.data\\.keyTakeaways}"
    - from: "src/layouts/ArticleLayout.astro"
      to: "src/components/CitationBox.astro"
      via: "<CitationBox sources={entry.data.sources} updatedDate={updatedDate} /> replacing .source-section"
      pattern: "<CitationBox sources={entry\\.data\\.sources}"
    - from: "src/layouts/ArticleLayout.astro articleSchema.author"
      to: "src/data/authors.ts"
      via: "author.role, author.bio, author.expertise, author.socialLinks fields read into Person object"
      pattern: "\"jobTitle\": author\\.role"
---

<objective>
Rewire `src/layouts/ArticleLayout.astro` to compose the Phase 3 EEAT components in the order Breadcrumb -> Key Takeaways -> TOC/Content -> Citation box -> AuthorBox v2 -> Share/Related, and expand the `articleSchema.author` JSON-LD object to a full Person sourced from `src/data/authors.ts`, matching AuthorBox v2's display data.

Purpose: Implements ARTL-01 (article layout composition order) and ARTL-02 (JSON-LD Person/Article from authors.ts), and is the consumer-facing wiring that makes EEAT-10's backfilled `keyTakeaways` (Plan 01) and existing `sources` data visible to readers via `<KeyTakeaways>` and `<CitationBox>`.
Output: Modified `src/layouts/ArticleLayout.astro` — new imports (Breadcrumb, KeyTakeaways, CitationBox), `breadcrumbItems` array, expanded `articleSchema.author`, reordered article body markup, removed dead CSS (`.breadcrumb`, `.source-section` rules split from `.editorial-review`).
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-CONTEXT.md
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-PATTERNS.md
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-UI-SPEC.md
@src/components/Breadcrumb.astro
@src/components/KeyTakeaways.astro
@src/components/CitationBox.astro
@src/data/authors.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add imports, build breadcrumbItems, expand JSON-LD articleSchema.author (D-08, D-10)</name>
  <files>src/layouts/ArticleLayout.astro</files>
  <read_first>
    - src/layouts/ArticleLayout.astro (current frontmatter: lines 1-90 — existing imports, articleSchema construction lines 29-57, categoryMeta/categoryTitle/categoryPath/isInvestingCategory/categoryGroupPath already computed at lines 18-22)
    - src/components/Breadcrumb.astro (Props: items: { label: string; href?: string }[]; last item with no href renders aria-current="page")
    - src/data/authors.ts (author.role, author.bio, author.expertise: string[], author.socialLinks?: { linkedin?: string; twitter?: string; email?: string })
    - .planning/phases/04-article-layout-wiring-author-profile-pages/04-PATTERNS.md (Pattern: JSON-LD author expansion code, breadcrumbItems code)
  </read_first>
  <action>
    Add three new imports at the top of the frontmatter (after the existing `import { author } from "@/data/authors";` at line 8):
    - `import Breadcrumb from "@/components/Breadcrumb.astro";`
    - `import KeyTakeaways from "@/components/KeyTakeaways.astro";`
    - `import CitationBox from "@/components/CitationBox.astro";`

    After the existing `categoryGroupPath`/`isInvestingCategory` computation (around line 21-22), add a `breadcrumbItems` const built with the same conditional structure as the current inline `<nav class="breadcrumb">` (lines 110-122) and `breadcrumbSchema.itemListElement` (lines 77-88):
    - If `isInvestingCategory && categoryGroupPath`: `[{ label: "Trang chủ", href: "/" }, { label: "Đầu tư", href: categoryGroupPath }, { label: categoryTitle, href: categoryPath }, { label: title }]`
    - Else: `[{ label: "Trang chủ", href: "/" }, { label: categoryTitle, href: categoryPath }, { label: title }]`
    (Last item has no `href`, matching Breadcrumb.astro's `aria-current="page"` convention for the final item.)

    Before the `articleSchema` const (line 29), compute `sameAs`:
    - `const sameAs = author.socialLinks ? [author.socialLinks.linkedin, author.socialLinks.twitter].filter((v): v is string => Boolean(v)) : [];`

    In `articleSchema.author` (currently lines 44-48: `{ "@type": "Person", "name": author.name, "url": new URL("/about/", Astro.site).toString() }`), replace with:
    - `"@type": "Person"`
    - `"name": author.name`
    - `"url": new URL(\`/author/${author.slug}/\`, Astro.site).toString()` (was `/about/`)
    - `"jobTitle": author.role` (new)
    - `"description": author.bio` (new)
    - `"knowsAbout": author.expertise` (new)
    - conditionally spread `"sameAs": sameAs` only if `sameAs.length > 0` (currently `sameAs` will be `[]` since `author.socialLinks` is `undefined`, so this key is omitted entirely — do not invent fields beyond what authors.ts provides)

    Do not modify `breadcrumbSchema` (lines 74-89) — D-10 confirms it is a separate, already-correct concern; `breadcrumbItems` is a new const, independent of `breadcrumbSchema.itemListElement` (duplication between the two is acceptable per D-10).
  </action>
  <acceptance_criteria>
    - `src/layouts/ArticleLayout.astro` frontmatter imports `Breadcrumb`, `KeyTakeaways`, `CitationBox` from `@/components/*.astro`
    - A `breadcrumbItems` const exists with the two-branch structure (investing category vs. other), matching the existing inline breadcrumb's label/href logic
    - `articleSchema.author` contains `"jobTitle": author.role`, `"description": author.bio`, `"knowsAbout": author.expertise`, and `"url"` pointing to `/author/${author.slug}/` (not `/about/`)
    - `articleSchema.author` does NOT contain a `"sameAs"` key when `author.socialLinks` is `undefined` (current state in `src/data/authors.ts`)
    - `npm run build` succeeds (type-checks the new const/object shapes)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ArticleLayout.astro frontmatter has the three new imports, breadcrumbItems const, and expanded articleSchema.author with jobTitle/description/knowsAbout/url-to-/author/ and conditional sameAs; npm run build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reorder article body markup and remove dead CSS (D-04, D-05, D-09, D-10, D-11)</name>
  <files>src/layouts/ArticleLayout.astro</files>
  <read_first>
    - src/layouts/ArticleLayout.astro (current template: header lines 108-135 with inline <nav class="breadcrumb">; article body lines 137-187; <style> block lines 190-354 — specifically .breadcrumb rules 191-214, .ymyl-note/.source-section/.editorial-review shared rules 260-292 and 318-319)
    - .planning/phases/04-article-layout-wiring-author-profile-pages/04-UI-SPEC.md (Component-Specific Visual Contracts section 1 — exact render order and CSS removal notes)
    - .planning/phases/04-article-layout-wiring-author-profile-pages/04-RESEARCH.md (Pitfalls 4 and 5 — dead CSS after Breadcrumb/CitationBox swap)
  </read_first>
  <action>
    Template changes inside `<header class="article-header">` (lines 108-135):
    - Replace the entire `<nav class="breadcrumb" aria-label="Breadcrumb">...</nav>` block (lines 110-122) with `<Breadcrumb items={breadcrumbItems} />`.

    Template changes inside `<article class="prose article-content">` (lines 138-180), new order:
    - Keep `<aside class="ymyl-note" ...>...</aside>` as-is (lines 139-142) — per Claude's discretion (04-CONTEXT.md), skip the `.ymyl-note` -> `<Disclaimer />` consolidation since it would change the heading copy from "Lưu ý trước khi đọc:" to "Lưu ý quan trọng:" (Pitfall 6); preserving exact existing copy is the safer choice and `.ymyl-note` already uses the same box pattern.
    - Insert `<KeyTakeaways items={entry.data.keyTakeaways} />` immediately after `.ymyl-note` and before `<slot />`.
    - Keep `<slot />` (article body content, with `## Key takeaways` sections already removed by Plan 01).
    - Keep the FAQ section (lines 144-156) unchanged.
    - Remove the entire `{sources?.length ? (<section class="source-section">...</section>) : null}` block (lines 157-168). Replace with `<CitationBox sources={entry.data.sources} updatedDate={updatedDate} />` placed at the same position (after FAQ, before `.editorial-review`). Per D-04, do NOT pass `citations` (the structured field stays unbackfilled for these 21 articles). Per D-05, do NOT pass `factCheckedDate` — `CitationBox` falls back to displaying `updatedDate` ("Cập nhật lần cuối: ...").
    - Keep `<section class="editorial-review">...</section>` (lines 169-176) unchanged.
    - Reorder the remaining elements to: `<CitationBox ...>` -> `<section class="editorial-review">` -> `<AuthorBox />` -> `<ShareBar title={title} url={articleUrl} />` -> `<RelatedArticles category={entry.data.category} currentSlug={entry.slug} />`. (Current order has `<ShareBar>` before `<AuthorBox>` at lines 177-178; D-11 only fixes the position of CitationBox relative to slot/AuthorBox — keep ShareBar/AuthorBox/RelatedArticles in their existing relative order: AuthorBox, then ShareBar, then RelatedArticles, i.e. do not reorder lines 177-179 relative to each other, only insert CitationBox before them.)

    `<style>` block changes:
    - Delete the `.breadcrumb`, `.breadcrumb a`, `.breadcrumb a:hover`, `.breadcrumb span[aria-current]` rules (current lines ~191-214) — dead after the `<Breadcrumb>` swap (Pitfall 4); `Breadcrumb.astro` ships its own scoped `<nav><ol><li>` styles.
    - For the `.ymyl-note, .editorial-review, .source-section { border: 1px solid var(--line); border-radius: var(--radius-md); background: var(--surface); padding: 18px 20px; }` shared rule (current lines ~262-267): remove `.source-section` from the selector list, keep `.ymyl-note, .editorial-review`.
    - For `.ymyl-note span, .source-section p, .editorial-review p { color: var(--muted); font-size: 0.96rem; line-height: 1.65; }` (current lines ~280-286): remove `.source-section p` from the selector list, keep `.ymyl-note span, .editorial-review p`.
    - For `.source-section, .editorial-review, .faq-section { margin-top: 42px; }` (current lines ~288-292): remove `.source-section` from the selector list, keep `.editorial-review, .faq-section`.
    - For `.source-section h2, .editorial-review h2 { margin-top: 0; }` (current lines ~318-319): remove `.source-section h2`, keep `.editorial-review h2`.
    - The dropped `.source-section` paragraph text "Bài viết ưu tiên nguồn gốc và tài liệu có thể kiểm chứng. Xem thêm chính sách nguồn tham khảo." (with link to `/sources-policy/`) is an accepted minor content reduction per D-11 — `CitationBox` has no slot for it, and `/sources-policy/` remains linked from `/editorial-policy/` and the site footer (per RESEARCH.md Pitfall 5 / Open Question 1 recommendation).
  </action>
  <acceptance_criteria>
    - `<Breadcrumb items={breadcrumbItems} />` appears inside `<header class="article-header">`, and the old `<nav class="breadcrumb">` markup is gone
    - `<KeyTakeaways items={entry.data.keyTakeaways} />` appears as the first element inside `<article class="prose article-content">` after `.ymyl-note` and before `<slot />`
    - `<CitationBox sources={entry.data.sources} updatedDate={updatedDate} />` appears after the FAQ section and before `<section class="editorial-review">`; the old `<section class="source-section">` markup is gone
    - The `<style>` block no longer contains `.breadcrumb` selector rules
    - The `<style>` block's `.editorial-review` rules (border/background/padding, margin-top, h2 margin-top) remain intact and are not broken by the `.source-section` selector removal
    - `npm run build` succeeds
    - Manual: `npm run dev`, visit `/co-phieu/co-phieu-la-gi/` (or any article with TOC) and a non-TOC article — visual order top-to-bottom is Breadcrumb -> header (title/lead/meta) -> [ymyl-note] -> Key Takeaways -> Content -> FAQ (if present) -> Citation box -> editorial-review -> AuthorBox -> ShareBar -> RelatedArticles; on `benjamin-graham.md` specifically confirm Key Takeaways box shows 3 items including the colon-containing bullet, and no duplicate "Nguồn tham khảo" heading appears
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ArticleLayout.astro renders Breadcrumb -> header -> KeyTakeaways -> slot -> FAQ -> CitationBox -> editorial-review -> AuthorBox -> ShareBar -> RelatedArticles; dead .breadcrumb and .source-section-only CSS removed without breaking .editorial-review; npm run build passes; manual visual check on a TOC and non-TOC article confirms correct order and no duplicate headings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time template composition | `ArticleLayout.astro` is a static-site template; all data (`entry.data.*`, `author` from `authors.ts`) is repo-committed, no runtime user input |
| JSON-LD output | `articleSchema` (including expanded `author` Person object) is emitted via `JSON.stringify()` into `<script type="application/ld+json">` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | JSON-LD `articleSchema.author` (expanded Person object) | accept | All source fields (`author.role`, `author.bio`, `author.expertise`, `author.socialLinks`) come from repo-committed `src/data/authors.ts`, not user input; existing `JSON.stringify()` pattern (unchanged) correctly escapes for `<script type="application/ld+json">` context — no new injection surface introduced |
| T-04-03 | Information Disclosure | `articleSchema.author.url` now points to `/author/{slug}/` instead of `/about/` | accept | `/author/{slug}/` is a new public page (Plan 03) intentionally designed to expose author credibility info (bio, expertise, credentials) for E-E-A-T — this is the intended disclosure, not a leak |

This phase is content/layout wiring on a static site with no auth, no user input, no server-side data handling (per RESEARCH.md Security Domain) — ASVS V2/V3/V4/V6 categories are not applicable. V5 (Input Validation) is satisfied by the existing Zod schema in `src/content.config.ts`, unchanged by this plan.
</threat_model>

<verification>
1. `npm run build` — succeeds after Task 1 (imports/breadcrumbItems/articleSchema.author changes) and after Task 2 (template reorder + CSS cleanup)
2. Manual: `npm run dev`, visit an article with TOC (e.g. `/co-phieu/co-phieu-la-gi/`) — confirm Breadcrumb renders via `<ol>/<li>` (Breadcrumb.astro markup, not old `<nav>` flat spans), Key Takeaways box appears before body content inside the content column (not full-width), Citation box appears after content/FAQ with "Nguồn tham khảo" heading (only one), AuthorBox links to `/author/nguyen-viet-loc` (will 404 until Plan 03 lands — expected at this point)
3. Manual: visit `benjamin-graham.md`'s page — Key Takeaways box shows 3 bullets including the colon-containing one, verbatim
4. Manual: view page source on any article — `<script type="application/ld+json">` for `articleSchema` contains `"author":{"@type":"Person","name":"...","url":"https://valueinvesting.com.vn/author/nguyen-viet-loc/","jobTitle":"...","description":"...","knowsAbout":[...]}` with no `"sameAs"` key (since `author.socialLinks` is `undefined`)
</verification>

<success_criteria>
- `npm run build` passes with 0 errors after both tasks
- ArticleLayout.astro renders components in order: Breadcrumb -> header -> [ymyl-note] -> KeyTakeaways -> slot -> FAQ -> CitationBox -> editorial-review -> AuthorBox -> ShareBar -> RelatedArticles
- JSON-LD `articleSchema.author` is a full Person object with `jobTitle`, `description`, `knowsAbout`, `url` to `/author/{slug}/`, matching `src/data/authors.ts` and AuthorBox v2 display
- No dead `.breadcrumb` or orphaned `.source-section`-only CSS remains; `.editorial-review` styling intact
</success_criteria>

<output>
Create `.planning/phases/04-article-layout-wiring-author-profile-pages/04-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 02)

- `breadcrumbItems` const — new local variable in `ArticleLayout.astro` frontmatter, array of `{ label: string; href?: string }`
- `sameAs` const — new local variable in `ArticleLayout.astro` frontmatter, `string[]`
- Expanded `articleSchema.author` object — new fields `jobTitle`, `description`, `knowsAbout`, conditional `sameAs`; `url` now points to `/author/{slug}/`
- New imports in `ArticleLayout.astro`: `Breadcrumb`, `KeyTakeaways`, `CitationBox` from `src/components/`
- Template now renders `<Breadcrumb items={breadcrumbItems} />`, `<KeyTakeaways items={entry.data.keyTakeaways} />`, `<CitationBox sources={entry.data.sources} updatedDate={updatedDate} />`
</content>
