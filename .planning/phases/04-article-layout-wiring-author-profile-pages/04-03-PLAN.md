---
phase: 04-article-layout-wiring-author-profile-pages
plan: 03
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/pages/author/[slug].astro
autonomous: true
requirements: [EEAT-06]

must_haves:
  truths:
    - "Visiting /author/nguyen-viet-loc/ shows a profile page with the author's bio, role, experience, expertise, credentials, education, moneyPerspective, socialLinks (if defined), and publishedIn from src/data/authors.ts"
    - "The page includes a full list of all 21 articles (D-07), distinct from /about/'s 6-item 'latest' list"
    - "AuthorBox v2's 'Xem hồ sơ' link (/author/{slug}) now resolves to a real page instead of 404"
    - "/author/[slug] reads as a distinct credibility/E-E-A-T profile, not a near-duplicate of /about/"
  artifacts:
    - path: "src/pages/author/[slug].astro"
      provides: "Static /author/[slug] profile page, getStaticPaths() generates one path per author (currently single author, array-friendly)"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/author/[slug].astro"
      to: "src/data/authors.ts"
      via: "import { author } from \"@/data/authors\"; getStaticPaths returns { params: { slug: author.slug }, props: { author } }"
      pattern: "getStaticPaths"
    - from: "src/pages/author/[slug].astro"
      to: "src/components/ArticleList.astro"
      via: "<ArticleList articles={articles} /> with all 21 articles sorted by updatedDate desc"
      pattern: "<ArticleList articles={articles}"
    - from: "src/components/AuthorBox.astro"
      to: "src/pages/author/[slug].astro"
      via: "AuthorBox's 'Xem hồ sơ' link href={`/author/${author.slug}`} now resolves"
      pattern: "author\\.slug"
---

<objective>
Create `src/pages/author/[slug].astro` — a new static page that renders a full E-E-A-T credibility profile for the author (bio, role, experience, expertise, credentials, education, moneyPerspective, socialLinks, publishedIn) plus a complete list of all 21 articles, sourced entirely from `src/data/authors.ts` and `getCollection("articles")`.

Purpose: Implements EEAT-06 — resolves the previously-dangling `/author/{slug}` link from `AuthorBox.astro` (D-06/D-07), giving readers and Google a dedicated author-credibility page distinct from the brand/mission-focused `/about/` page.
Output: New file `src/pages/author/[slug].astro` with `getStaticPaths()`, profile sections following the established box pattern (border-left 4px `var(--color-brand-900)` + `var(--surface)` + `var(--radius-md)`), and `<ArticleList articles={articles} />` showing all 21 articles.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-CONTEXT.md
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-UI-SPEC.md
@.planning/phases/04-article-layout-wiring-author-profile-pages/04-PATTERNS.md
@src/pages/about.astro
@src/data/authors.ts
@src/components/ArticleList.astro
@src/pages/[category].astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/author/[slug].astro with getStaticPaths, bio/credibility sections, and full article list</name>
  <files>src/pages/author/[slug].astro</files>
  <read_first>
    - src/pages/about.astro (full file — primary visual/markup analog: hero section with avatar/identity/expertise-panel, author-body two-column grid with quote-block/bio-section/latest-section in main column and stat-stack/profile-card sidebar; reuse this exact CSS block, adapted)
    - src/data/authors.ts (full author object: name, slug, role, bio, credentials: string[], experience: string, expertise: string[], avatar?, socialLinks?: {linkedin?, twitter?, email?}, moneyPerspective, education, publishedIn: string[])
    - src/components/ArticleList.astro (Props: { articles: CollectionEntry<"articles">[] })
    - src/pages/[category].astro (getStaticPaths + getCollection pattern reference, lines 1-22)
    - .planning/phases/04-article-layout-wiring-author-profile-pages/04-UI-SPEC.md (Component-Specific Visual Contracts section 2 — full copy/layout contract for this page)
  </read_first>
  <action>
    Create `src/pages/author/[slug].astro` with this structure:

    Frontmatter:
    - Imports: `getCollection` from `astro:content`; `BaseLayout` from `@/layouts/BaseLayout.astro`; `ArticleList` from `@/components/ArticleList.astro`; `author` from `@/data/authors`
    - `export async function getStaticPaths()` returns `[{ params: { slug: author.slug }, props: { author } }]` — single-element array now, written so a future `authors: Author[]` export in `authors.ts` could be mapped over without restructuring (per D-06 array-friendly note); add a one-line comment documenting this
    - `const { author: profileAuthor } = Astro.props;`
    - `const articles = (await getCollection("articles")).toSorted((a, b) => b.data.updatedDate.getTime() - a.data.updatedDate.getTime());` (D-07: single author = all 21 articles, no filter needed)

    Page wrapped in `<BaseLayout title={profileAuthor.name} description={profileAuthor.bio}>`.

    Sections, in order (per UI-SPEC Component 2, adapted from `about.astro`):
    1. **Hero** (`.author-hero`, reuse `about.astro`'s `.author-hero`/`.author-hero-grid`/`.author-identity`/`.author-avatar-large` classes and markup): avatar initial (`profileAuthor.name.split(" ").pop()?.charAt(0) ?? profileAuthor.name.charAt(0)`), H1 `{profileAuthor.name}`, `.author-role` showing `{profileAuthor.role}`, expertise panel with `.panel-label` "Chuyên môn" + `<ul>` of `profileAuthor.expertise`
    2. **Body** (`.author-body` / `.author-layout` two-column grid, reuse `about.astro` classes):
       - Main column (`.author-main`):
         - Quote block (`.quote-block`): `.panel-label` "Quan điểm về đầu tư" + `<blockquote>"{profileAuthor.moneyPerspective}"</blockquote>`
         - Bio section (`.bio-section`): H2 "Tiểu sử" + paragraph(s) using `profileAuthor.bio` and `profileAuthor.experience` (experience is a string per `authors.ts` comment, render as `<p>{profileAuthor.experience}</p>`)
         - Conditional "Chứng chỉ" sub-section: only render if `profileAuthor.credentials.length > 0` — render as `<h3>Chứng chỉ</h3><ul>{profileAuthor.credentials.map(c => <li>{c}</li>)}</ul>` inside `.bio-section` (currently `credentials: []`, so this sub-section is omitted — confirm conditional guard works)
         - "Bài viết mới" section (`.latest-section`, reuse class names): `.panel-label` "Bài viết mới" + H2, then `<ArticleList articles={articles} />` (full 21-article grid, NOT a 6-item slice — this is the key difference from `about.astro`'s `.latest-author-list`)
       - Sidebar (`.author-sidebar`, `aria-label="Thông tin hồ sơ"`):
         - `.profile-card` for "Kinh nghiệm nội dung": `<p>{profileAuthor.experience}</p>` — NOTE: if experience is already shown in bio-section above, avoid exact duplication; either show experience once in bio-section OR once in sidebar profile-card (not both) — prefer sidebar profile-card per `about.astro`'s existing pattern, and use bio-section for `profileAuthor.bio` only
         - `.profile-card` for "Học vấn": `<p>{profileAuthor.education}</p>` (label "Học vấn" per copywriting contract)
         - Conditional `.profile-card` for social links (`.profile-links`): only render if `profileAuthor.socialLinks` is defined; within it, conditionally render LinkedIn/Twitter/email links only for keys that are non-empty strings. Entire block omitted if `socialLinks === undefined` (currently the case)
         - `.profile-card` "Xuất hiện trên" (`.published-list`): `{profileAuthor.publishedIn.map((item) => <span>{item}</span>)}`

    Copy a `<style>` block adapted from `about.astro`'s existing styles (`.author-hero`, `.author-hero-grid`, `.author-identity`, `.author-avatar-large`, `.profile-links`, `.expertise-panel`, `.panel-label`, `.author-body`, `.author-layout`, `.author-main`, `.quote-block`, `.bio-section`, `.latest-section`, `.author-sidebar`, `.profile-card`, `.published-list`, and the `@media (max-width: 900px)` / `@media (max-width: 640px)` responsive rules) — adjust selectors/markup only as needed for the conditional credentials/socialLinks sections and the full `<ArticleList>` (which has its own scoped styles and does not need `.latest-author-item`/`.latest-author-list` rules copied, since this page uses `ArticleList.astro`'s `.card-grid` instead).

    No new design tokens, colors, or spacing values — every value referenced must already exist in `about.astro`'s `<style>` block or Phase 1 tokens (per UI-SPEC "No new CSS values").
  </action>
  <acceptance_criteria>
    - `src/pages/author/[slug].astro` exists, exports `getStaticPaths()` returning a single-element array with `params: { slug: author.slug }` and `props: { author }`
    - Page renders `<BaseLayout title={profileAuthor.name} description={profileAuthor.bio}>`
    - Page includes: H1 with author name, role subtitle, expertise list, moneyPerspective quote block, bio text, conditional credentials sub-section (hidden when `credentials.length === 0`), conditional socialLinks block (hidden when `socialLinks === undefined`), education profile-card, publishedIn profile-card, and `<ArticleList articles={articles} />` rendering all 21 articles (not a 6-item slice)
    - `npm run build` succeeds and generates `/author/nguyen-viet-loc/` in the build output (check `dist/author/nguyen-viet-loc/index.html` exists after build)
    - Manual: `npm run dev`, visit `/author/nguyen-viet-loc/` — page renders without errors, shows all sections with real data from `src/data/authors.ts`, and the article grid shows 21 cards
    - Manual: on any article page, AuthorBox's "Xem hồ sơ" link now navigates to `/author/nguyen-viet-loc/` successfully (no 404)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/author/nguyen-viet-loc/index.html</automated>
  </verify>
  <done>src/pages/author/[slug].astro exists, npm run build succeeds and produces dist/author/nguyen-viet-loc/index.html, and manual visit to /author/nguyen-viet-loc/ shows the full credibility profile (bio, role, experience, expertise, conditional credentials/socialLinks, education, publishedIn) plus all 21 articles via ArticleList.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time static page generation | `/author/[slug].astro` is generated via `getStaticPaths()` at build time from repo-committed `src/data/authors.ts` data — no runtime input, no dynamic routes beyond the single known author slug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | `/author/[slug]` exposes `author.bio`, `experience`, `expertise`, `education`, `moneyPerspective`, `publishedIn`, conditional `socialLinks`/`credentials` | accept | This is the intentional purpose of the page (E-E-A-T credibility signals); all fields are repo-committed, non-sensitive professional/editorial data the author has chosen to publish — no PII beyond what's already in `authors.ts` and `about.astro` |
| T-04-05 | Spoofing | `getStaticPaths()` route collision with existing `/author/*` paths | accept | RESEARCH.md confirmed (via `ls src/pages/`) no existing `src/pages/author/` directory prior to this phase — clean new route, no collision; sitemap/redirect config in `astro.config.mjs` has no `/author/*` filter pattern that would conflict |

This phase is content/layout wiring on a static site with no auth, no user input, no server-side data handling (per RESEARCH.md Security Domain) — ASVS V2/V3/V4/V6 categories are not applicable. V5 (Input Validation) is satisfied by Zod schema validation of `getCollection("articles")` data, unchanged by this plan.
</threat_model>

<verification>
1. `npm run build` — succeeds, generates `dist/author/nguyen-viet-loc/index.html`
2. Manual: `npm run dev`, visit `/author/nguyen-viet-loc/` — all sections render with data from `src/data/authors.ts`: name, role, bio, experience, expertise list, moneyPerspective quote, education, publishedIn tags, and full 21-article `<ArticleList>` grid
3. Manual: confirm conditional sections behave correctly with current data — `credentials: []` hides the "Chứng chỉ" sub-section, `socialLinks: undefined` hides the social-links profile-card
4. Manual: from any article page, click AuthorBox's "Xem hồ sơ" link — lands on `/author/nguyen-viet-loc/` (no 404)
5. Manual: visually compare `/author/nguyen-viet-loc/` against `/about/` — confirm the two pages read as distinct (credibility dossier vs. brand/mission page), not near-duplicates, per D-06
</verification>

<success_criteria>
- `npm run build` succeeds and produces `dist/author/nguyen-viet-loc/index.html`
- `/author/nguyen-viet-loc/` renders bio, role, experience, expertise, credentials (conditional), education, moneyPerspective, socialLinks (conditional), publishedIn, and a full 21-article list
- AuthorBox's "Xem hồ sơ" link resolves successfully (previously 404)
- No new CSS values introduced beyond what exists in `about.astro`/Phase 1 tokens
</success_criteria>

<output>
Create `.planning/phases/04-article-layout-wiring-author-profile-pages/04-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (Plan 03)

- `src/pages/author/[slug].astro` — new file, static page with `getStaticPaths()` generating `/author/nguyen-viet-loc/`
- `getStaticPaths()` export — new function in this file, returns `[{ params: { slug: author.slug }, props: { author } }]`
</content>
