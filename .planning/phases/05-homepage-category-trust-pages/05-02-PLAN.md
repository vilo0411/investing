---
phase: 05-homepage-category-trust-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CategoryListing.astro
  - src/pages/[category].astro
  - src/pages/dau-tu/[category].astro
autonomous: true
requirements: [CATG-01]

must_haves:
  truths:
    - "Visiting /co-phieu/ and /dau-tu/co-phieu/ both render a page-hero (with breadcrumb), Learning path list, and latest-articles sidebar via the same shared component"
    - "Category breadcrumbs use the accessible <Breadcrumb /> component instead of inline <nav class=\"breadcrumb\">"
    - "Latest-articles sidebar links resolve to the correct route-tree-prefixed article URL for both [category].astro and dau-tu/[category].astro"
    - "getStaticPaths() filtering by category.group remains unchanged for both route trees"
  artifacts:
    - path: "src/components/CategoryListing.astro"
      provides: "Shared category page body: page-hero (Breadcrumb + eyebrow + h1 + lead + article-count badge) + Learning path/latest-panel or empty state"
      exports: ["default"]
    - path: "src/pages/[category].astro"
      provides: "Thin route file: getStaticPaths (group !== Đầu tư) + article fetch + <CategoryListing />"
      contains: "CategoryListing"
    - path: "src/pages/dau-tu/[category].astro"
      provides: "Thin route file: getStaticPaths (group === Đầu tư) + article fetch + <CategoryListing /> with 3-level breadcrumb"
      contains: "CategoryListing"
  key_links:
    - from: "src/pages/[category].astro"
      to: "src/components/CategoryListing.astro"
      via: "import + JSX usage with category/articles/breadcrumbItems props"
      pattern: "<CategoryListing\\s+category={category}\\s+articles={articles}\\s+breadcrumbItems={breadcrumbItems}\\s*/>"
    - from: "src/pages/dau-tu/[category].astro"
      to: "src/components/CategoryListing.astro"
      via: "import + JSX usage with category/articles/breadcrumbItems props"
      pattern: "<CategoryListing\\s+category={category}\\s+articles={articles}\\s+breadcrumbItems={breadcrumbItems}\\s*/>"
    - from: "src/components/CategoryListing.astro"
      to: "src/components/Breadcrumb.astro"
      via: "<Breadcrumb items={breadcrumbItems} />"
      pattern: "<Breadcrumb items={breadcrumbItems}\\s*/>"
    - from: "src/components/CategoryListing.astro"
      to: "src/data/site.ts"
      via: "getArticlePath(article) for .latest-panel hrefs"
      pattern: "getArticlePath\\(article\\)"
---

<objective>
Extract a single shared `src/components/CategoryListing.astro` (CATG-01, D-07) that renders the full category page body — page-hero (with `<Breadcrumb />`, D-08), Learning path grid, and latest-articles sidebar (or empty state) — taking `category`, `articles`, and `breadcrumbItems` as props (D-09: route files keep their own `getStaticPaths()` filtering and breadcrumb-depth construction). Reduce `src/pages/[category].astro` and `src/pages/dau-tu/[category].astro` to thin route files that fetch data and render `<CategoryListing />`. Fix the route-tree-specific `.latest-panel` href bug (Pitfall 2) by using `getArticlePath(article)` inside the shared component.

Purpose: Guarantee both category route trees render visually identical bodies from one source of truth, with accessible breadcrumb markup.
Output: New `src/components/CategoryListing.astro`; both category route files reduced to ~30-40 lines each.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-category-trust-pages/05-CONTEXT.md
@.planning/phases/05-homepage-category-trust-pages/05-PATTERNS.md
@.planning/phases/05-homepage-category-trust-pages/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/components/CategoryListing.astro (CATG-01, D-07, D-08)</name>
  <files>src/components/CategoryListing.astro</files>
  <read_first>
    src/pages/[category].astro (current full file — source of body markup lines ~51-107 and styles ~109-224 to extract)
    src/pages/dau-tu/[category].astro (current full file — superset `.category-meta`/`.category-meta-hint` styles lines 145-159, and the hardcoded `/dau-tu/${category.slug}/${article.slug}/` href bug to fix)
    src/components/Breadcrumb.astro (full file — prop contract `{ items: { label: string; href?: string }[] }`, renders its own `<nav class="breadcrumb">` with scoped styles)
    src/data/site.ts (Category type, getArticlePath signature — `getArticlePath(article: { slug: string; data: { category: string } })`)
    src/components/ArticleList.astro (confirm it already uses getArticlePath for the Learning path grid — no change needed there)
  </read_first>
  <action>
    Create `src/components/CategoryListing.astro` with frontmatter imports: `ArticleList` from `@/components/ArticleList.astro`, `Breadcrumb` from `@/components/Breadcrumb.astro`, type `CollectionEntry` from `astro:content`, type `Category` and `getArticlePath` from `@/data/site`. Define `interface Props { category: Category; articles: CollectionEntry<"articles">[]; breadcrumbItems: { label: string; href?: string }[] }`. Destructure `{ category, articles, breadcrumbItems } = Astro.props`. Compute `latestArticles` the same way as the current files: `articles.toSorted((a, b) => b.data.updatedDate.getTime() - a.data.updatedDate.getTime()).slice(0, 3)`.

    Render structure:
    1. `<section class="page-hero"><div class="container">` containing `<Breadcrumb items={breadcrumbItems} />` (replaces both files' inline `<nav class="breadcrumb">`), then `<div class="eyebrow">{category.group}</div>`, `<h1>{category.title}</h1>`, `<p class="lead">{category.description}</p>`, then `<div class="category-meta"><span class="article-count-badge">{articles.length} bài viết</span>{articles.length > 0 && <span class="category-meta-hint">Đọc theo thứ tự để xây dựng nền tảng vững chắc</span>}</div>` — use `dau-tu/[category].astro`'s superset `.category-meta`/`.category-meta-hint` markup+styles (the hint span) since it's a strict superset of `[category].astro`'s version.
    2. `<section class="section"><div class="container">` containing the `articles.length` conditional: non-empty branch renders `.category-layout` with `<ArticleList articles={articles} />` (Learning path heading "Learning path" / sub "Đọc theo thứ tự để xây dựng nền tảng vững chắc.") and `<aside class="latest-panel" aria-label="Bài viết mới nhất">` with `<h2>Mới nhất</h2>` and `{latestArticles.map((article) => <a href={getArticlePath(article)}><strong>{article.data.title}</strong><span>{article.data.readingTime}</span></a>)}` — CRITICAL: use `getArticlePath(article)`, NOT a hardcoded `/${category.slug}/${article.slug}/` or `/dau-tu/${category.slug}/${article.slug}/` template (Pitfall 2 fix). Empty branch renders the existing `.card.empty-state` with eyebrow "Sắp ra mắt", `<h3>Nội dung đang được biên tập</h3>`, body text, and `<a href="/" class="empty-state-link">← Về trang chủ</a>` — copy verbatim from `[category].astro`.

    Styles `<style>` block: retain `.category-meta`, `.category-meta-hint`, `.article-count-badge`, `.empty-state*`, `.category-layout`, `.latest-panel*`, and the `@media (max-width: 920px)` block from the existing files. Do NOT include `.breadcrumb*` rules — `<Breadcrumb />` has its own scoped styles already.
  </action>
  <acceptance_criteria>
    - src/components/CategoryListing.astro exists, exports a default Astro component with `interface Props { category: Category; articles: CollectionEntry<"articles">[]; breadcrumbItems: { label: string; href?: string }[] }`
    - File contains `<Breadcrumb items={breadcrumbItems} />`
    - File contains `getArticlePath(article)` inside the `.latest-panel` map — NOT a hardcoded `/${category.slug}/` or `/dau-tu/${category.slug}/` template literal
    - File contains `.category-layout`, `.latest-panel`, `.empty-state`, `.article-count-badge`, `.category-meta-hint` style rules
    - File does NOT contain `.breadcrumb` style rules (moved to Breadcrumb.astro already)
    - `npx astro check` exits 0 (props may show unused until Task 2 wires consumers — acceptable interim state, but no syntax errors)
  </acceptance_criteria>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>src/components/CategoryListing.astro exists with the documented Props contract, renders page-hero via Breadcrumb + Learning path/latest-panel/empty-state, uses getArticlePath for sidebar links, and astro check exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reduce [category].astro and dau-tu/[category].astro to thin route files (CATG-01, D-09)</name>
  <files>src/pages/[category].astro, src/pages/dau-tu/[category].astro</files>
  <read_first>
    src/pages/[category].astro (current full file — getStaticPaths, breadcrumbSchema JSON-LD lines 26-43 stay unchanged)
    src/pages/dau-tu/[category].astro (current full file — getStaticPaths, 3-level breadcrumbSchema JSON-LD lines 26-49 stay unchanged)
    src/components/CategoryListing.astro (just created — Props contract: category, articles, breadcrumbItems)
  </read_first>
  <action>
    For `src/pages/[category].astro`: keep `getStaticPaths()` exactly as-is (filter `category.group !== "Đầu tư"`, D-09 — no change to this filtering logic). Keep the article fetch/sort (`getCollection("articles")` filter by `category.slug`, sort by `order`). Keep `categoryUrl` and `breadcrumbSchema` JSON-LD construction unchanged (route-specific schema generation per research). Remove `latestArticles` computation (moves into CategoryListing) and the entire `<style>` block (moves into CategoryListing). Remove `import ArticleList from "@/components/ArticleList.astro"` (unused now — moved into CategoryListing). Add `import CategoryListing from "@/components/CategoryListing.astro"`. Add `const breadcrumbItems = [{ label: "Trang chủ", href: "/" }, { label: category.title }]` (2-level, matches the existing 2-item breadcrumbSchema). Replace the entire `<section class="page-hero">...</section>` and `<section class="section">...</section>` body with a single `<CategoryListing category={category} articles={articles} breadcrumbItems={breadcrumbItems} />` inside `<BaseLayout>`.

    For `src/pages/dau-tu/[category].astro`: same pattern, but keep `getStaticPaths()` filtering `category.group === "Đầu tư"` unchanged (D-09). Keep the 3-level `breadcrumbSchema` JSON-LD unchanged. Add `const breadcrumbItems = [{ label: "Trang chủ", href: "/" }, { label: "Đầu tư", href: "/dau-tu/" }, { label: category.title }]` (3-level, matches the existing 3-item breadcrumbSchema). Same removals (ArticleList import, latestArticles, `<style>` block) and same `<CategoryListing category={category} articles={articles} breadcrumbItems={breadcrumbItems} />` usage.

    Confirm `import { categories, getCategoryPath } from "@/data/site"` stays in both route files — `getCategoryPath` is still used for `categoryUrl` in the JSON-LD schema (route-specific, per Pitfall 4).
  </action>
  <acceptance_criteria>
    - src/pages/[category].astro does NOT contain `import ArticleList` or a `<style>` block
    - src/pages/[category].astro contains `<CategoryListing category={category} articles={articles} breadcrumbItems={breadcrumbItems} />`
    - src/pages/[category].astro breadcrumbItems array has exactly 2 entries: `{ label: "Trang chủ", href: "/" }` and `{ label: category.title }`
    - src/pages/dau-tu/[category].astro contains `<CategoryListing category={category} articles={articles} breadcrumbItems={breadcrumbItems} />`
    - src/pages/dau-tu/[category].astro breadcrumbItems array has exactly 3 entries including `{ label: "Đầu tư", href: "/dau-tu/" }`
    - getStaticPaths() filter conditions (`category.group !== "Đầu tư"` / `category.group === "Đầu tư"`) unchanged in both files
    - `npx astro check` exits 0
    - `npm run build` exits 0
    - `dist/co-phieu/index.html` and `dist/dau-tu/co-phieu/index.html` both exist and both contain `class="latest-panel"` and `class="article-count-badge"`
    - `dist/co-phieu/index.html` "Mới nhất" sidebar links point to `/co-phieu/{slug}/` (not `/dau-tu/co-phieu/{slug}/`); `dist/dau-tu/co-phieu/index.html` "Mới nhất" sidebar links point to `/dau-tu/co-phieu/{slug}/`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/co-phieu/index.html && test -f dist/dau-tu/co-phieu/index.html</automated>
  </verify>
  <done>Both [category].astro and dau-tu/[category].astro are thin route files rendering <CategoryListing /> with correct breadcrumbItems depth and route-tree-correct latest-panel links; npm run build exits 0 and both dist category pages exist.</done>
</task>

</tasks>

## Artifacts this plan produces

- New file `src/components/CategoryListing.astro` exporting a default Astro component with `Props = { category: Category; articles: CollectionEntry<"articles">[]; breadcrumbItems: { label: string; href?: string }[] }`
- New CSS classes scoped to `CategoryListing.astro`: `.category-meta`, `.category-meta-hint`, `.article-count-badge`, `.empty-state`, `.empty-state-link`, `.category-layout`, `.latest-panel`
- New local variable `breadcrumbItems` in both `src/pages/[category].astro` and `src/pages/dau-tu/[category].astro`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure refactor of static-page composition. No new user input, auth, or data sources — `category`/`articles` come from existing `getCollection`/`getStaticPaths` build-time data, same as before. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering (broken links / route collisions) | src/components/CategoryListing.astro, src/pages/[category].astro, src/pages/dau-tu/[category].astro | mitigate | `getArticlePath(article)` (existing, tested helper in `src/data/site.ts`) replaces hardcoded per-route href templates, eliminating the route-prefix divergence bug (Pitfall 2); `npm run build` fails on `getStaticPaths()` route collisions, providing a build-time safety net |
| T-05-04 | Information Disclosure (XSS via set:html in breadcrumbSchema JSON-LD) | src/pages/[category].astro, src/pages/dau-tu/[category].astro | accept | Pre-existing `set:html={JSON.stringify(breadcrumbSchema)}` blocks are unmodified by this plan — schema generation stays in route files per Pattern 1; `JSON.stringify` on build-time-constructed objects (category title/url from `src/data/site.ts`, not user input) |
</threat_model>

<verification>
After Task 1: `npx astro check` (component compiles, props typed correctly once consumed). After Task 2: `npm run build` — must exit 0. Inspect `dist/co-phieu/index.html` vs `dist/dau-tu/co-phieu/index.html` for structural parity (breadcrumb depth differs by design, rest identical). Manually click "Mới nhất" sidebar links on both `/co-phieu/` and `/dau-tu/co-phieu/` to confirm correct route-prefixed hrefs.
</verification>

<success_criteria>
- `npm run build` exits 0
- Both `[category].astro` and `dau-tu/[category].astro` render via `<CategoryListing />` with correct breadcrumb depth and route-tree-correct `.latest-panel` links
- `getStaticPaths()` filtering logic unchanged in both route files
</success_criteria>

<output>
Create `.planning/phases/05-homepage-category-trust-pages/05-02-SUMMARY.md` when done
</output>
