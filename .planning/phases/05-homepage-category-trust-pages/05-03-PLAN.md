---
phase: 05-homepage-category-trust-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/about.astro
autonomous: true
requirements: [TRST-01]

must_haves:
  truths:
    - "Visiting /about/ shows a mission/audience statement explaining why the site exists and who it's for, distinct from /author/[slug]/"
    - "/about/ shows a 2-3 step editorial process summary with a link to /editorial-policy/"
    - "/about/ shows a small author preview card (avatar initial, name, role, 1-line bio excerpt) with a 'Xem hồ sơ đầy đủ' CTA linking to /author/{slug}/"
    - "/about/ shows the existing site stats reframed as 'Quy mô nội dung' (article count, category count), without the 'Năm cập nhật hồ sơ' stat"
    - "/about/ no longer renders the full author dossier (.author-hero, .author-layout, credentials list, expertise list, moneyPerspective quote, education, publishedIn)"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "Brand/mission story page: mission hero, editorial process summary, author preview card, reframed stat-stack"
      contains: "Xem hồ sơ đầy đủ"
  key_links:
    - from: "src/pages/about.astro"
      to: "/author/{author.slug}/"
      via: "author preview card CTA"
      pattern: "href={`/author/\\$\\{author\\.slug\\}/`}"
    - from: "src/pages/about.astro"
      to: "/editorial-policy/"
      via: "editorial process summary CTA"
      pattern: "href=\"/editorial-policy/\""
    - from: "src/pages/about.astro"
      to: "src/data/authors.ts"
      via: "import { author } from \"@/data/authors\""
      pattern: "import \\{ author \\} from \"@/data/authors\""
---

<objective>
Rewrite `src/pages/about.astro` (TRST-01, D-01–D-03) from its current near-duplicate of `/author/[slug]/` into a distinct brand/mission story page: mission/audience statement, 2-3 step editorial process summary (linking to `/editorial-policy/`), a small author preview card (linking to `/author/{slug}/`), and a reframed site-stats section ("Quy mô nội dung"). Strictly avoid re-creating dossier-level content (credentials, expertise list, moneyPerspective quote, education, publishedIn) — those remain exclusive to `/author/[slug]/` (Pitfall 3).

Purpose: Eliminate duplicate-content risk between `/about/` and `/author/[slug]/`, and give `/about/` a distinct EEAT purpose ("why this site exists").
Output: Rewritten `src/pages/about.astro` (~mission hero + process steps + preview card + stats, much smaller `<style>` block than current 420-line file).
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-category-trust-pages/05-CONTEXT.md
@.planning/phases/05-homepage-category-trust-pages/05-PATTERNS.md
@.planning/phases/05-homepage-category-trust-pages/05-UI-SPEC.md
@.antigravity/rules/content-anti-ai.md
@.antigravity/memory/instincts.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite src/pages/about.astro mission/process/preview-card/stats (TRST-01, D-01, D-02, D-03)</name>
  <files>src/pages/about.astro</files>
  <read_first>
    src/pages/about.astro (current full 420-line file — identify what to DELETE: `.author-hero`/`.author-hero-grid`/`.author-identity`/`.author-avatar-large`/`.expertise-panel`/`.author-body`/`.author-layout`/`.quote-block`/`.bio-section`/`.latest-section`/`.latest-author-*`/`.author-sidebar`/`.profile-card`/`.published-list` markup AND styles — all of this now lives at src/pages/author/[slug].astro)
    src/pages/author/[slug].astro (full file — confirms the full dossier is already live there; do NOT duplicate its content/credentials/expertise/moneyPerspective/education/publishedIn here)
    src/data/authors.ts (author object shape: name, slug, role, bio, expertise[], credentials[], experience, moneyPerspective, education, publishedIn[], avatar=undefined — for /about/ ONLY use name, slug, role, bio)
    src/pages/index.astro (`.start-item`/`.start-number`/`.start-list` markup+styles lines 92-105, 327-349 — analog for the 2-3 step editorial process list; `.page-hero` and `.section`/`.section-header`/`.section-title`/`.eyebrow` patterns)
    .antigravity/rules/content-anti-ai.md and .antigravity/memory/instincts.md (already read this session — apply: no "Trong kỷ nguyên số" openings, no emphatic quote-marks around metaphors, direct YMYL-safe phrasing)
  </read_first>
  <action>
    Replace the entire file. Keep the `getCollection("articles")` import and `articleCount`/`categoryCount` computation (`const articleCount = articles.length; const categoryCount = new Set(articles.map((article) => article.data.category)).size;`) from the current file — these remain valid for the reframed stat-stack. Import `author` from `@/data/authors` (already imported in current file) and `categories`/`getCategoryPath` if needed for any cross-links (not required for stats, which use raw counts).

    Page structure (in order):
    1. **Mission hero** — `<section class="page-hero"><div class="container">`: `<div class="eyebrow">Giới thiệu</div>`, `<h1>Vì sao ValueInvesting.com.vn ra đời</h1>`, `<p class="lead">` containing 1-2 sentences covering: who the site is for (người mới bắt đầu đầu tư tại Việt Nam), what makes it different (nội dung giáo dục, có nguồn tham khảo, không khuyến nghị mua/bán, không cung cấp tín hiệu giao dịch). Follow anti-ai-rules: no "Trong kỷ nguyên số/thế giới hiện đại" openings, no emphatic quote marks around ordinary terms, direct and specific.
    2. **Editorial process summary** — `<section class="section">` with `.section-header` (eyebrow + `<h2 class="section-title">Quy trình biên tập tóm tắt</h2>` or similar), then a `.start-list`-style grid (reuse `.start-item`/`.start-number` pattern from index.astro, collapsed to 2-3 columns since only 2-3 steps) listing 2-3 short steps (e.g., "01 Nghiên cứu & viết theo nguồn", "02 Rà soát nội dung & rủi ro", "03 Cập nhật định kỳ") — each step is a `<div class="start-item">` (not a link, since no per-step target) with `<span class="start-number">` + `<strong>`/`<small>`. After the step grid, add `<a href="/editorial-policy/">Xem chính sách biên tập đầy đủ</a>` (D-02.2 — summary only, full detail lives at `/editorial-policy/`, do not duplicate the full policy text here).
    3. **Author preview card** — `<section class="section">` with a `.profile-card`-style or new `.author-preview-card` box (border `1px solid var(--line)`, `border-radius: var(--radius-md)`, `background: var(--surface)`, `padding: var(--space-6)`, grid `64px minmax(0,1fr)` columns) containing: 64px avatar-initial circle (`.author-avatar-small`, scaled-down version of `.author-avatar-large`'s radial-gradient, `font-size: 1.6rem`), `<strong>{author.name}</strong>`, `<span class="author-role-small">{author.role}</span>`, `<p class="author-bio-excerpt">{author.bio}</p>` with `-webkit-line-clamp: 2`, and `<a class="preview-cta" href={`/author/${author.slug}/`}>Xem hồ sơ đầy đủ</a>`. STRICTLY exclude: `author.expertise`, `author.credentials`, `author.experience`, `author.moneyPerspective`, `author.education`, `author.publishedIn` — none of these fields appear anywhere in this file (Pitfall 3).
    4. **Site stats** — `<section class="section">` (or merge with preview card section) titled "Quy mô nội dung" (eyebrow + h2), containing `.stat-stack` markup with exactly 2 entries: `<strong>{articleCount}</strong><span>Bài viết đã xuất bản</span>` and `<strong>{categoryCount}</strong><span>Nhóm chủ đề đầu tư</span>` — drop the "Năm cập nhật hồ sơ" / "2026" stat entirely (it was profile-specific).

    `<style>` block: include ONLY new/needed rules — `.start-list`/`.start-item`/`.start-number` (collapsed column count for 2-3 items, e.g. `grid-template-columns: repeat(auto-fit, minmax(180px, 1fr))` or explicit `repeat(3, minmax(0,1fr))`), `.author-preview-card`, `.author-avatar-small`, `.author-role-small`, `.author-bio-excerpt`, `.preview-cta`, `.stat-stack` (retain existing stat-stack CSS from current file, lines ~321-349, since it's still needed). Do NOT carry over `.author-hero*`, `.author-identity`, `.author-avatar-large`, `.expertise-panel`, `.quote-block`, `.bio-section`, `.latest-section`, `.latest-author-*`, `.author-sidebar`, `.profile-card` (old dossier version), `.published-list`, `.profile-links` — delete all of these (Anti-Pattern from research: don't let dead CSS balloon the file).
  </action>
  <acceptance_criteria>
    - src/pages/about.astro does NOT contain the strings `.author-hero`, `.author-layout`, `.author-sidebar`, `.expertise-panel`, `.quote-block`, `.moneyPerspective`, `.publishedIn`, `author.credentials`, `author.expertise`, `author.education`
    - src/pages/about.astro contains `<h1>Vì sao ValueInvesting.com.vn ra đời</h1>` (or equivalent mission headline using this exact phrase per UI-SPEC copy table)
    - src/pages/about.astro contains `href={`/author/${author.slug}/`}` and the text "Xem hồ sơ đầy đủ"
    - src/pages/about.astro contains `href="/editorial-policy/"` and the text "Xem chính sách biên tập đầy đủ"
    - src/pages/about.astro contains a `.stat-stack` with exactly 2 `<div>` children (articleCount, categoryCount) — grep -c '<strong>{' on the stat-stack block equals 2, and the string "Năm cập nhật hồ sơ" is absent
    - src/pages/about.astro contains `-webkit-line-clamp: 2` for the author bio excerpt
    - `npx astro check` exits 0
    - `npm run build` exits 0 and `dist/about/index.html` exists, contains "Xem hồ sơ đầy đủ" and does not contain any of "Chứng chỉ", "Học vấn", "Đã xuất bản tại", "Quan điểm về đầu tư"
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q 'Xem hồ sơ đầy đủ' dist/about/index.html && ! grep -qE 'Chứng chỉ|Học vấn|Đã xuất bản tại|Quan điểm về đầu tư' dist/about/index.html</automated>
  </verify>
  <done>src/pages/about.astro renders a mission hero ("Vì sao ValueInvesting.com.vn ra đời"), a 2-3 step editorial process summary linking to /editorial-policy/, an author preview card (name/role/1-line bio, "Xem hồ sơ đầy đủ" CTA to /author/{slug}/), and a 2-stat "Quy mô nội dung" stack with no dossier-level author fields; npm run build exits 0.</done>
</task>

</tasks>

## Artifacts this plan produces

- Rewritten `src/pages/about.astro` page structure: mission hero (`.page-hero`), editorial process summary section (`.start-list`/`.start-item`/`.start-number`), author preview card section (`.author-preview-card`, `.author-avatar-small`, `.author-role-small`, `.author-bio-excerpt`, `.preview-cta`), reframed `.stat-stack` ("Quy mô nội dung", 2 stats)
- New CSS classes: `.author-preview-card`, `.author-avatar-small`, `.author-role-small`, `.author-bio-excerpt`, `.preview-cta`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static content page rewrite. `author` object comes from `src/data/authors.ts` (build-time, no user input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure (duplicate-content / thin-content SEO) | src/pages/about.astro | mitigate | Strictly limit author preview card to name/role/1-line bio excerpt per D-02.3 and Pitfall 3 — full dossier (credentials, expertise, moneyPerspective, education, publishedIn) remains exclusive to `/author/[slug]/`, preventing near-duplicate pages |
| T-05-06 | YMYL misleading-advice copy (mission statement, process summary) | src/pages/about.astro | mitigate | Mission/process copy must follow `.antigravity/rules/content-anti-ai.md` and avoid personalized-advice framing — phrasing emphasizes "giáo dục", "có nguồn", "không khuyến nghị mua/bán" per locked decisions D-02.1 |
</threat_model>

<verification>
`npx astro check` after the task. `npm run build` — must exit 0, `dist/about/index.html` must exist and not contain dossier-only strings ("Chứng chỉ", "Học vấn", "Đã xuất bản tại", "Quan điểm về đầu tư"). Manually load `/about/` and `/author/nguyen-viet-loc/` side by side to confirm they are visibly distinct pages (mission story vs. full dossier).
</verification>

<success_criteria>
- `npm run build` exits 0
- `/about/` renders mission hero + 2-3 step process summary + author preview card + 2-stat "Quy mô nội dung" section
- `/about/` contains zero dossier-level author fields (credentials, expertise, moneyPerspective, education, publishedIn)
</success_criteria>

<output>
Create `.planning/phases/05-homepage-category-trust-pages/05-03-SUMMARY.md` when done
</output>
