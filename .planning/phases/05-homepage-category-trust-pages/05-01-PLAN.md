---
phase: 05-homepage-category-trust-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
autonomous: true
requirements: [HOME-01, HOME-02]

must_haves:
  truths:
    - "Visitor on homepage sees a new trust-strip section after the hero, before the topic grid"
    - "Trust strip links to all three of /about/, /editorial-policy/, /disclaimer/"
    - "Homepage hero, topic grid, and featured/latest sections continue to render unchanged (HOME-01 already satisfied)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with new .trust-strip-section between .home-hero and .topic-section"
      contains: "trust-strip-section"
  key_links:
    - from: "src/pages/index.astro"
      to: "/about/, /editorial-policy/, /disclaimer/"
      via: "trust-card anchor hrefs"
      pattern: "href=\"/(about|editorial-policy|disclaimer)/\""
---

<objective>
Add a new homepage "trust strip" section (HOME-02) immediately after the hero and before the topic grid, linking to `/about/`, `/editorial-policy/`, and `/disclaimer/` — forming the closed EEAT link loop required by D-04/D-05/D-06. HOME-01 (hero, topic grid, featured/latest) is already implemented and confirmed by research; this plan makes no changes to those sections beyond the insertion point.

Purpose: Surface credibility signals on the homepage and connect visitors to the three trust pages built/expanded elsewhere in this phase.
Output: `src/pages/index.astro` with new `.trust-strip-section` (markup + scoped styles).
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-category-trust-pages/05-CONTEXT.md
@.planning/phases/05-homepage-category-trust-pages/05-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert homepage trust-strip section (HOME-02, D-04, D-05, D-06)</name>
  <files>src/pages/index.astro</files>
  <read_first>
    src/pages/index.astro (current full file — confirm exact line where `.home-hero` section closes and `.topic-section` begins, and existing `.topic-grid`/`.topic-card`/`.section-header`/`.section-title`/`.eyebrow` CSS to copy the pattern from)
    .planning/phases/05-homepage-category-trust-pages/05-UI-SPEC.md (Component & Layout Notes #1 — exact copy for trust-strip cards, Copywriting Contract table for all four card titles/bodies and section eyebrow/heading)
  </read_first>
  <action>
    Insert a new `<section class="section trust-strip-section">` immediately after the `</section>` that closes `.home-hero` (the section containing `.home-hero-grid`) and before `<section class="section topic-section">`. Per D-04, this is a NEW additive section — do not modify `.home-hero`, `.hero-trust-panel`, or `site.trustStats` usage anywhere in the file.

    Markup structure inside the new section: `.container` > `.section-header` containing eyebrow "Vì sao tin chúng tôi" and `<h2 class="section-title">Nguyên tắc xây dựng nội dung</h2>` (do not reuse `.trust-band`'s existing heading "Thông tin đầu tư cần minh bạch trước khi hữu ích." — give the new section its own distinct heading per UI-SPEC). Below that, a `.trust-strip` grid of 4 `<a class="trust-card">` elements with `<h3>` title + `<p>` body, using the exact copy from UI-SPEC Copywriting Contract:
    - Card 1 → href="/editorial-policy/": "Quy trình biên tập rõ ràng" / "Mỗi bài viết được rà soát về nguồn, tính cập nhật và rủi ro trước khi xuất bản."
    - Card 2 → href="/disclaimer/": "Không khuyến nghị mua/bán" / "Nội dung mang tính giáo dục, không thay thế tư vấn tài chính cá nhân hóa."
    - Card 3 → href="/about/": "Có nguồn, có tác giả rõ ràng" / "Mỗi bài viết ghi rõ tác giả, nguồn tham khảo và ngày cập nhật cuối."
    - Card 4 → href="/editorial-policy/": "Cập nhật thường xuyên" / "Nội dung được rà soát và cập nhật định kỳ khi có thay đổi về số liệu hoặc quy định."

    D-05 requires all three pages (/about/, /editorial-policy/, /disclaimer/) to appear at least once across the four cards — the above mapping satisfies that (about: card 3, editorial-policy: cards 1 and 4, disclaimer: card 2).

    Add scoped `<style>` rules for `.trust-strip-section`, `.trust-strip` (grid: `display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: var(--space-4);`), `.trust-card` (border `1px solid var(--line)`, `border-radius: var(--radius-md)`, `background: var(--surface)`, `padding: var(--space-6)`, `text-decoration: none`), `.trust-card:hover` (`border-color: var(--color-brand-100); box-shadow: var(--shadow-sm); text-decoration: none;`), `.trust-card h3` (0.92rem, weight 700, line-height 1.4, `color: var(--text)`, `margin: 0 0 var(--space-2)`), `.trust-card p` (0.85rem, weight 400, `color: var(--muted)`, line-height 1.55, `margin: 0`). Add responsive collapse inside the existing `@media (max-width: 860px)` block (`.trust-strip { grid-template-columns: repeat(2, 1fr); }`) and `@media (max-width: 640px)` block (`.trust-strip { grid-template-columns: 1fr; }`) — append to those existing media query blocks rather than creating new ones.

    Verify HOME-01 is unaffected: do not touch `.home-hero`, `.topic-section`, `.start-section`, `.top-section`, `.trust-band`, `.author-home`, or the per-category `{byCategory.map(...)}` sections — these already satisfy HOME-01 per research confirmation.
  </action>
  <acceptance_criteria>
    - src/pages/index.astro contains a `<section class="section trust-strip-section">` placed between the `.home-hero` closing `</section>` and the `<section class="section topic-section">` opening tag
    - The new section contains exactly 4 `<a class="trust-card">` elements
    - grep -o 'href="/about/"' src/pages/index.astro returns at least 1 match
    - grep -o 'href="/editorial-policy/"' src/pages/index.astro returns at least 2 matches (cards 1 and 4)
    - grep -o 'href="/disclaimer/"' src/pages/index.astro returns at least 1 match
    - `.trust-strip`, `.trust-card`, `.trust-card:hover`, `.trust-card h3`, `.trust-card p` rules exist in the `<style>` block
    - `npx astro check` exits 0
    - `npm run build` exits 0 and `dist/index.html` contains `trust-strip-section`
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -c 'trust-card' dist/index.html | grep -qv '^0$'</automated>
  </verify>
  <done>Homepage renders a new .trust-strip-section with 4 trust-card links between the hero and topic grid, linking to /about/, /editorial-policy/ (x2), and /disclaimer/; npm run build exits 0.</done>
</task>

</tasks>

<!-- Artifacts this phase produces -->
## Artifacts this plan produces

- New CSS classes in `src/pages/index.astro`: `.trust-strip-section`, `.trust-strip`, `.trust-card`, `.trust-card h3`, `.trust-card p`
- New markup section in `src/pages/index.astro`: trust strip with 4 cards linking to `/about/`, `/editorial-policy/` (x2), `/disclaimer/`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan adds static anchor links and text content to a pre-rendered static page. No user input, no auth, no session, no forms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure (XSS via set:html) | src/pages/index.astro | accept | Not applicable — this plan adds no new `set:html` usage; existing JSON-LD blocks elsewhere in the codebase are unmodified by this plan |
| T-05-02 | YMYL misleading-advice copy | src/pages/index.astro (trust-strip card copy) | mitigate | Trust-strip copy is taken verbatim from 05-UI-SPEC.md Copywriting Contract, which was authored per `.antigravity/rules/content-anti-ai.md` and explicitly avoids buy/sell recommendation framing (e.g., "Không khuyến nghị mua/bán") |
</threat_model>

<verification>
Run `npx astro check` after the task (fast type-check). Run `npm run build` at the end of the wave — must exit 0. Manually load `/` in `npm run dev` or inspect `dist/index.html` to confirm the trust strip renders between hero and topic grid with 4 cards.
</verification>

<success_criteria>
- `npm run build` exits 0
- Homepage trust strip renders with 4 cards, linking to `/about/`, `/editorial-policy/` (x2), `/disclaimer/`
- No existing homepage section (hero, topic grid, start list, featured/latest, trust band, author-home, category sections) is altered
</success_criteria>

<output>
Create `.planning/phases/05-homepage-category-trust-pages/05-01-SUMMARY.md` when done
</output>
