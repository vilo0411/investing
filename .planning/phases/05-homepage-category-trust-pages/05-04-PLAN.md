---
phase: 05-homepage-category-trust-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/disclaimer.astro
  - src/components/Disclaimer.astro
  - src/pages/editorial-policy.astro
autonomous: true
requirements: [TRST-02, TRST-03]

must_haves:
  truths:
    - "Visiting /disclaimer/ shows a standalone page styled like /corrections-policy/, expanding on educational-only scope, no personalized advice, investment risk, and contact"
    - "The inline <Disclaimer /> component (used on article pages etc.) retains its short site.disclosure text AND adds a 'Tìm hiểu thêm' link to /disclaimer/"
    - "/editorial-policy/ has two new sections: detailed fact-check process, and a description of what the CitationBox shows on articles (sources list + Cập nhật lần cuối date)"
  artifacts:
    - path: "src/pages/disclaimer.astro"
      provides: "Standalone disclaimer page (.article-header + .article-container.prose pattern)"
      contains: "Miễn trừ trách nhiệm"
    - path: "src/components/Disclaimer.astro"
      provides: "Inline disclaimer aside with Tìm hiểu thêm link to /disclaimer/, text prop contract unchanged"
      contains: "Tìm hiểu thêm"
    - path: "src/pages/editorial-policy.astro"
      provides: "Editorial policy with fact-check detail + CitationBox cross-reference sections appended"
      contains: "Hộp nguồn tham khảo trên từng bài viết"
  key_links:
    - from: "src/components/Disclaimer.astro"
      to: "/disclaimer/"
      via: "a.disclaimer-link href"
      pattern: "href=\"/disclaimer/\""
    - from: "src/pages/editorial-policy.astro"
      to: "src/components/CitationBox.astro"
      via: "textual description of CitationBox rendered output (no new component instance)"
      pattern: "Cập nhật lần cuối|Kiểm tra nguồn lần cuối"
---

<objective>
Create the standalone `/disclaimer/` page (TRST-03, D-10) following the `corrections-policy.astro` `.article-header` + `.article-container.prose` pattern, expanding `site.disclosure` into educational-scope / no-personalized-advice / investment-risk / contact sections. Add a "Tìm hiểu thêm" link to `src/components/Disclaimer.astro` pointing at `/disclaimer/` without changing its `text` prop contract (D-11, D-12). Expand `/editorial-policy/` (TRST-02, D-13) with two new sections: detailed fact-check process, and a description of the `<CitationBox />` rendered output (sources list + last-updated date) so readers can verify it on article pages.

Purpose: Connect the short inline disclaimer notice to a full legal/educational page, and deepen the editorial-policy page's fact-check narrative with a concrete reference to the Citation box readers already see on articles.
Output: New `src/pages/disclaimer.astro`; modified `src/components/Disclaimer.astro` (additive link); modified `src/pages/editorial-policy.astro` (two new `<h2>` sections).
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-homepage-category-trust-pages/05-CONTEXT.md
@.planning/phases/05-homepage-category-trust-pages/05-PATTERNS.md
@.planning/phases/05-homepage-category-trust-pages/05-UI-SPEC.md
@.antigravity/rules/content-anti-ai.md
@.antigravity/memory/instincts.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/pages/disclaimer.astro (TRST-03, D-10, D-12)</name>
  <files>src/pages/disclaimer.astro</files>
  <read_first>
    src/pages/corrections-policy.astro (full 26-line file — exact structure to replicate: `<BaseLayout title=... description=...>`, `<section class="article-header"><div class="article-container"><h1>/<p class="lead">`, then `<section class="article-container prose">` with `<h2>`/`<p>`/`<ul>` body, no `<style>` block)
    src/data/site.ts (`site.disclosure` text — the short version this page expands on)
    .antigravity/rules/content-anti-ai.md and .antigravity/memory/instincts.md (apply to all new body copy — no AI-typical openings, no emphatic quote marks, direct phrasing)
  </read_first>
  <action>
    Create `src/pages/disclaimer.astro` importing `BaseLayout` from `@/layouts/BaseLayout.astro`. Use `<BaseLayout title="Miễn trừ trách nhiệm" description="Phạm vi nội dung giáo dục và giới hạn trách nhiệm của ValueInvesting.com.vn.">`. Structure exactly mirrors `corrections-policy.astro`:
    - `<section class="article-header"><div class="article-container"><h1>Miễn trừ trách nhiệm</h1><p class="lead">Nội dung trên ValueInvesting.com.vn chỉ mang tính giáo dục và không phải lời khuyên đầu tư cá nhân hóa.</p></div></section>` (exact lead copy per UI-SPEC Copywriting Contract)
    - `<section class="article-container prose">` with four `<h2>` + `<p>` (or `<ul>`) groups:
      1. `<h2>Phạm vi nội dung</h2>` — explain content is educational, covers investing concepts/terms/products for beginners, expands on `site.disclosure`'s scope statement.
      2. `<h2>Không phải lời khuyên đầu tư cá nhân hóa</h2>` — explicit: not personalized investment advice, not brokerage, no trading signals/recommendations (mirrors `site.disclosure` wording but expanded into 1-2 paragraphs).
      3. `<h2>Rủi ro đầu tư</h2>` — readers bear responsibility for their own investment decisions, market/product risks vary, content does not guarantee outcomes.
      4. `<h2>Liên hệ và phản hồi</h2>` — cross-link: "Nếu phát hiện thông tin lỗi thời hoặc không chính xác, vui lòng xem [Chính sách đính chính](/corrections-policy/)" or similar — link to `/corrections-policy/`.
    No `<style>` block needed — `.article-header`/`.article-container`/`.prose`/`.lead` are global per `src/styles/global.css`.
  </action>
  <acceptance_criteria>
    - src/pages/disclaimer.astro exists
    - File contains `<h1>Miễn trừ trách nhiệm</h1>` and the exact lead copy "Nội dung trên ValueInvesting.com.vn chỉ mang tính giáo dục và không phải lời khuyên đầu tư cá nhân hóa."
    - File contains exactly the structure `<section class="article-header">` followed by `<section class="article-container prose">`
    - File contains 4 `<h2>` elements: "Phạm vi nội dung", "Không phải lời khuyên đầu tư cá nhân hóa", "Rủi ro đầu tư", "Liên hệ và phản hồi"
    - File contains `href="/corrections-policy/"`
    - File has no `<style>` block
    - `npx astro check` exits 0
    - `npm run build` exits 0 and `dist/disclaimer/index.html` exists
  </acceptance_criteria>
  <verify>
    <automated>npm run build && test -f dist/disclaimer/index.html</automated>
  </verify>
  <done>src/pages/disclaimer.astro exists with title "Miễn trừ trách nhiệm", the UI-SPEC lead copy, 4 H2 sections (Phạm vi nội dung / Không phải lời khuyên đầu tư cá nhân hóa / Rủi ro đầu tư / Liên hệ và phản hồi), a link to /corrections-policy/, no <style> block, and npm run build produces dist/disclaimer/index.html.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add "Tìm hiểu thêm" link to Disclaimer.astro and append fact-check/CitationBox sections to editorial-policy.astro (D-11, D-13)</name>
  <files>src/components/Disclaimer.astro, src/pages/editorial-policy.astro</files>
  <read_first>
    src/components/Disclaimer.astro (current 37-line file — `Props { text?: string }`, `<aside class="disclaimer" aria-label="Lưu ý tài chính">` with `<strong>` + `<span>{text}</span>`, scoped `<style>` with `display: grid; gap: var(--space-1);`)
    src/pages/editorial-policy.astro (current 29-line file — existing 3 sections: "Nguyên tắc cốt lõi", "Quy trình rà soát", "Độc lập biên tập", inside `<section class="article-container prose">`)
    src/components/CitationBox.astro (component file — confirm prop names sources?/citations?/factCheckedDate?/updatedDate for accurate textual description; do NOT add a new instance, only describe its rendered output in prose)
  </read_first>
  <action>
    In `src/components/Disclaimer.astro`: add `<a class="disclaimer-link" href="/disclaimer/">Tìm hiểu thêm</a>` as a new element inside `<aside class="disclaimer">`, placed after `<span>{text}</span>`. Do NOT change the `Props { text?: string }` interface or the `text = site.disclosure` default — purely additive (D-11). Append to the existing `<style>` block: `.disclaimer-link { color: var(--brand); font-size: 0.85rem; font-weight: 700; justify-self: start; }` — relies on the existing `display: grid` on `.disclaimer` to place the link on its own row.

    In `src/pages/editorial-policy.astro`: append two new `<h2>` + `<p>` groups inside the existing `<section class="article-container prose">`, after the current "Độc lập biên tập" section and before `</section>`:
    1. `<h2>Quy trình fact-check chi tiết</h2>` — expand on the existing "Quy trình rà soát" section with concrete steps (e.g., kiểm tra số liệu/định nghĩa với nguồn gốc, đối chiếu ngày cập nhật quy định/phí, rà soát lại khi có thay đổi từ cơ quan quản lý hoặc sàn giao dịch).
    2. `<h2>Hộp nguồn tham khảo trên từng bài viết</h2>` — describe what `<CitationBox />` (Phase 3 component, already wired into ArticleLayout per Phase 4) shows on every article page: a "Nguồn tham khảo" list of sources used, plus a "Cập nhật lần cuối" / "Kiểm tra nguồn lần cuối" date — explain that readers can verify the fact-check process directly by viewing this box on any article. Reference the CitationBox by describing its rendered output, not by embedding a new instance (this is a non-article page).
  </action>
  <acceptance_criteria>
    - src/components/Disclaimer.astro contains `<a class="disclaimer-link" href="/disclaimer/">Tìm hiểu thêm</a>` inside `<aside class="disclaimer">`
    - src/components/Disclaimer.astro `interface Props` still reads `{ text?: string }` (unchanged signature)
    - src/components/Disclaimer.astro `<style>` block contains `.disclaimer-link`
    - src/pages/editorial-policy.astro contains two new `<h2>` elements: "Quy trình fact-check chi tiết" and "Hộp nguồn tham khảo trên từng bài viết"
    - src/pages/editorial-policy.astro mentions "Cập nhật lần cuối" or "Kiểm tra nguồn lần cuối" in the new CitationBox section
    - src/pages/editorial-policy.astro still contains the original 3 sections ("Nguyên tắc cốt lõi", "Quy trình rà soát", "Độc lập biên tập") unchanged
    - `npx astro check` exits 0
    - `npm run build` exits 0; `dist/editorial-policy/index.html` contains "Hộp nguồn tham khảo trên từng bài viết"; grep for `<Disclaimer` usages (e.g. in ArticleLayout) confirms they still compile (no prop signature break)
  </acceptance_criteria>
  <verify>
    <automated>npm run build && grep -q 'Hộp nguồn tham khảo trên từng bài viết' dist/editorial-policy/index.html</automated>
  </verify>
  <done>Disclaimer.astro renders the existing text plus a "Tìm hiểu thêm" link to /disclaimer/ with unchanged Props{text?: string}; editorial-policy.astro retains its 3 original sections plus 2 new sections ("Quy trình fact-check chi tiết", "Hộp nguồn tham khảo trên từng bài viết"); npm run build exits 0.</done>
</task>

</tasks>

## Artifacts this plan produces

- New file `src/pages/disclaimer.astro` (route `/disclaimer/`)
- New element + CSS class in `src/components/Disclaimer.astro`: `<a class="disclaimer-link" href="/disclaimer/">Tìm hiểu thêm</a>`, `.disclaimer-link`
- Two new `<h2>` sections in `src/pages/editorial-policy.astro`: "Quy trình fact-check chi tiết", "Hộp nguồn tham khảo trên từng bài viết"

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Static content pages/component, no auth/forms/user input. `<Disclaimer />`'s existing call sites (ArticleLayout etc. from Phase 3/4) are untouched aside from the additive link. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | YMYL misleading-advice copy | src/pages/disclaimer.astro | mitigate | All 4 body sections (Phạm vi nội dung, Không phải lời khuyên đầu tư cá nhân hóa, Rủi ro đầu tư, Liên hệ và phản hồi) explicitly state educational-only scope and absence of personalized advice, per `.antigravity/rules/content-anti-ai.md` and D-10 |
| T-05-08 | Tampering (prop-contract break in shared component) | src/components/Disclaimer.astro | mitigate | Change is additive only — `Props { text?: string }` interface unchanged, `text = site.disclosure` default unchanged; existing call sites (ArticleLayout from Phase 4) continue to compile via `npx astro check` |
| T-05-09 | Information Disclosure (XSS via set:html in JSON-LD) | n/a | accept | This plan introduces no `set:html` usage — disclaimer.astro and editorial-policy.astro additions are plain `.prose` text content |
</threat_model>

<verification>
`npx astro check` after each task. `npm run build` at end of wave — must exit 0, `dist/disclaimer/index.html` must exist. Grep all `<Disclaimer` usages across `src/` to confirm they still pass type-check with the unchanged `text?` prop. Manually load `/disclaimer/`, `/editorial-policy/`, and an article page (to see `<Disclaimer />` with the new "Tìm hiểu thêm" link) in `npm run dev`.
</verification>

<success_criteria>
- `npm run build` exits 0
- `/disclaimer/` exists and follows the `.article-header` + `.article-container.prose` pattern with 4 sections
- `<Disclaimer />` shows the existing short text plus a "Tìm hiểu thêm" link to `/disclaimer/`, with no prop-contract change
- `/editorial-policy/` has the 2 new sections, existing 3 sections unchanged
</success_criteria>

<output>
Create `.planning/phases/05-homepage-category-trust-pages/05-04-SUMMARY.md` when done
</output>
