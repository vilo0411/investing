---
phase: 01-design-token-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [DSGN-03, DSGN-04, DSGN-05]

must_haves:
  truths:
    - "npm run build succeeds with the full token system + new fonts in place"
    - "Every one of the 13 route templates has been visually checked at 4 viewports and shows no broken layout"
    - "Article body type scale (~1.7 line-height, ~65ch width) and Vietnamese diacritics in Source Serif 4 / Inter have been visually confirmed"
  artifacts:
    - path: ".planning/phases/01-design-token-foundation/01-route-audit-checklist.md"
      provides: "Completed (checked) DSGN-05 route audit checklist"
      contains: "[x]"
  key_links:
    - from: "npm run preview"
      to: "src/styles/global.css token chain"
      via: "browser rendering of all routes"
      pattern: "n/a — manual verification"
---

<objective>
Run the build, then perform the full manual DSGN-05 route audit (using the checklist created in Plan 02) plus the DSGN-03/DSGN-04 visual checks (article typography, ~65ch width, Vietnamese diacritic rendering in Source Serif 4 + Inter) across the site now that the token system (Plan 01) and font swap (Plan 02) are in place.

Purpose: This is the phase-gate verification step — confirms the entire site re-skins correctly with zero broken layouts and that the long-form reading improvements (DSGN-03) and new brand fonts (DSGN-04) actually look right in a real browser, which cannot be confirmed by `npm run build` alone.

Output: `npm run build` passing with the final token system, completed/checked `01-route-audit-checklist.md`, and a documented final decision on the `--article` width if `65ch` needed adjustment per RESEARCH.md Pitfall 3.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-design-token-foundation/01-CONTEXT.md
@.planning/phases/01-design-token-foundation/01-UI-SPEC.md
@.planning/phases/01-design-token-foundation/01-RESEARCH.md
@.planning/phases/01-design-token-foundation/01-route-audit-checklist.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build and start preview server</name>
  <files></files>
  <read_first>
    .planning/phases/01-design-token-foundation/01-VALIDATION.md (Test Infrastructure, Sampling Rate)
  </read_first>
  <action>
    Run `npm run build` (which runs `astro check && astro build`) from the project root and confirm it completes with zero errors — this validates that Plan 01's token restructure and Plan 02's font swap together produce a working build (DSGN-01/DSGN-02). Then start `npm run preview` (or `npm run dev`) so the site is reachable in a browser for the manual checks in Task 2. Report the local URL (typically `http://localhost:4321`) for the human checkpoint.
  </action>
  <acceptance_criteria>
    - `npm run build` exits with code 0 and produces no `astro check` type/schema errors
    - Preview/dev server starts and serves the site on a local port
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build succeeds; preview server is running and reachable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DSGN-03/04/05 manual route and typography audit</name>
  <files>.planning/phases/01-design-token-foundation/01-route-audit-checklist.md</files>
  <action>
    Walk the full route-audit checklist created in Plan 02 against the running preview server, checking off each of the 13 route templates at 4 viewport widths plus the typography/font/diacritic checks described below.
  </action>
  <what-built>
    The full design token system (navy/gold/tinted-neutral palette, Source Serif 4 + Inter fonts, fluid type scale, ~1.7 line-height / ~65ch article width, expanded transition tokens) is now live across the entire site via `global.css`'s token imports and `BaseLayout.astro`'s font swap. All ~37 legacy CSS variable names resolve through `aliases-legacy.css`, so existing components should render unchanged in structure but with the new navy/gold visual identity.
  </what-built>
  <how-to-verify>
    With `npm run preview` running, walk through `.planning/phases/01-design-token-foundation/01-route-audit-checklist.md` and check off each item:
    1. Visit each of the 13 route templates listed (homepage `/`, a category listing in each tree, an article in each tree, `/dau-tu/`, `/kien-thuc/{slug}/`, `/search/`, `/about/`, `/contact/`, `/editorial-policy/`, `/corrections-policy/`, `/sources-policy/`) at viewport widths 375px, 768px, 1024px, 1440px (use browser devtools responsive mode). Confirm no broken layout — no overlapping elements, no overflow, no unstyled/black-and-white fallback rendering (DSGN-05).
    2. On at least one article page, confirm: heading color is navy (not green), body text uses Inter with visibly looser line spacing (~1.7) than before, and the article column width feels close to the previous width (~680-760px at desktop) (DSGN-03).
    3. On a page containing Vietnamese text with diacritics (e.g. an article titled with "Phân tích cơ bản: Định giá cổ phiếu" or similar), confirm both the heading (Source Serif 4) and body (Inter) render all Vietnamese tone marks and special characters (â, ă, ê, ô, ơ, ư, đ, ₫) correctly with no missing-glyph boxes (DSGN-04). Check in at least Chrome; Firefox/Safari if available.
    4. Confirm `.eyebrow` text (category labels) is legible — should appear in a muted gold/brown tone (gold-700, `#92660a`), not a washed-out light gold.
    5. If the `--article: 65ch` width looks noticeably narrower or wider than the old 760px layout at desktop widths (outside roughly 680-760px), note this — RESEARCH.md Pitfall 3 allows falling back to `clamp(60ch, 70ch, 760px)` or a fixed px value if needed.
  </how-to-verify>
  <verify>
    <human-check>All 13 route-template sections in 01-route-audit-checklist.md are checked off, with no unresolved broken-layout or font-rendering issues reported.</human-check>
  </verify>
  <done>Route audit checklist fully checked off; DSGN-03/DSGN-04/DSGN-05 visually confirmed with no unresolved issues, or issues documented for a gap-closure plan.</done>
  <resume-signal>Type "approved" if all 13 routes pass with no broken layout and typography/fonts/diacritics look correct, or describe specific issues found (route + viewport + problem) for follow-up fixes.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces (Plan 03)

- `.planning/phases/01-design-token-foundation/01-route-audit-checklist.md` — checked/completed (all checkboxes marked) as the DSGN-05 sign-off record
- No source files modified by this plan unless the human checkpoint surfaces issues requiring follow-up gap-closure plans

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan performs build verification and manual visual review only — no code changes, no new trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | n/a | n/a | accept | Verification-only plan; no new code, components, or dependencies introduced. ASVS L1 not applicable. |

This plan is purely a build-and-visual-verification gate for the token/font changes made in Plans 01 and 02. No new trust boundaries, no new dependencies, no security-relevant code changes.
</threat_model>

<verification>
1. `npm run build` exits 0
2. `01-route-audit-checklist.md` has all checkboxes marked `[x]` after the human walkthrough
3. Human checkpoint returns "approved" (or issues are logged for a gap-closure plan)
</verification>

<success_criteria>
- DSGN-01 through DSGN-05 are all satisfied: token files exist and import cleanly, all legacy variable names resolve, article typography meets the ~1.7/~65ch target, Vietnamese diacritics render correctly in the new fonts, and every route has been audited with no broken layout
- `npm run build` passes
- Route audit checklist fully checked off
</success_criteria>

<output>
Create `.planning/phases/01-design-token-foundation/01-03-SUMMARY.md` when done
</output>
