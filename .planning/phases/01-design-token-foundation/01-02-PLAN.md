---
phase: 01-design-token-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - .planning/phases/01-design-token-foundation/01-route-audit-checklist.md
autonomous: true
requirements: [DSGN-04, DSGN-05]

must_haves:
  truths:
    - "Site loads Source Serif 4 and Inter (weights 400/500/700) from Google Fonts instead of DM Serif Display + DM Sans"
    - "A route audit checklist file exists enumerating all 13 page templates at 4 viewport widths, ready for the final manual DSGN-05 sign-off in Plan 03"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Updated Google Fonts <link> for Source Serif 4 + Inter (400/500/700)"
      contains: "Source+Serif+4:wght@400;500;700"
    - path: ".planning/phases/01-design-token-foundation/01-route-audit-checklist.md"
      provides: "Markdown checklist of 13 route templates x 4 viewports for DSGN-05 manual audit"
      min_lines: 20
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "fonts.googleapis.com"
      via: "<link href=\"https://fonts.googleapis.com/css2?family=Source+Serif+4...\">"
      pattern: "fonts\\.googleapis\\.com/css2\\?family=Source\\+Serif\\+4"
---

<objective>
Swap the Google Fonts `<link>` in `BaseLayout.astro` from DM Serif Display + DM Sans to Source Serif 4 + Inter at weights 400/500/700 (DSGN-04), and produce the Wave 0 route-audit checklist artifact (`01-route-audit-checklist.md`) that Plan 03 will use for the final DSGN-05 manual sign-off.

Purpose: Loads the new brand fonts (verified Vietnamese-complete per RESEARCH.md D-08a) and prepares the manual verification scaffold required because no automated visual regression tooling exists in this repo.

Output: Updated `BaseLayout.astro` font `<link>`, new `01-route-audit-checklist.md` in the phase directory.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-foundation/01-CONTEXT.md
@.planning/phases/01-design-token-foundation/01-UI-SPEC.md
@.planning/phases/01-design-token-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Swap Google Fonts link to Source Serif 4 + Inter</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    src/layouts/BaseLayout.astro (lines 49-65 — current head block with the DM Serif Display + DM Sans <link>)
    .planning/phases/01-design-token-foundation/01-UI-SPEC.md (Typography section — exact replacement <link> markup, locked)
    .planning/phases/01-design-token-foundation/01-PATTERNS.md (BaseLayout.astro pattern section — current vs new pattern)
  </read_first>
  <action>
    In `src/layouts/BaseLayout.astro`, locate the `<!-- Google Fonts: DM Serif Display + DM Sans -->` comment block (currently lines 55-61) containing two `<link rel="preconnect">` tags and one `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600;9..40,700&display=swap">`. Replace the comment text with `<!-- Google Fonts: Source Serif 4 + Inter -->`. Keep both `<link rel="preconnect" href="https://fonts.googleapis.com" />` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />` unchanged. Replace the `href` of the stylesheet `<link>` with exactly: `https://fonts.googleapis.com/css2?family=Source+Serif+4:wght@400;500;700&family=Inter:wght@400;500;700&display=swap`. Do not modify any other part of `BaseLayout.astro` (the JSON-LD scripts, Props interface, and rest of the head/body are out of scope per DSGN-02's "no markup changes" constraint, except this specific font link).
  </action>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` contains the string `Source+Serif+4:wght@400;500;700&family=Inter:wght@400;500;700&display=swap`
    - `src/layouts/BaseLayout.astro` does NOT contain `DM+Serif+Display` or `DM+Sans`
    - Both `preconnect` `<link>` tags (`fonts.googleapis.com`, `fonts.gstatic.com`) remain present and unchanged
    - `npx astro check` passes with no new errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && grep -q "Source+Serif+4:wght@400;500;700&family=Inter:wght@400;500;700&display=swap" src/layouts/BaseLayout.astro && ! grep -q "DM+Serif+Display" src/layouts/BaseLayout.astro</automated>
  </verify>
  <done>BaseLayout.astro head loads Source Serif 4 + Inter at weights 400/500/700 via Google Fonts; DM Serif Display/DM Sans references fully removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the DSGN-05 route audit checklist</name>
  <files>.planning/phases/01-design-token-foundation/01-route-audit-checklist.md</files>
  <read_first>
    .planning/phases/01-design-token-foundation/01-RESEARCH.md (section "DSGN-05: Full Route Inventory for Audit Checklist" — the 13-template table and audit approach recommendation)
    .planning/phases/01-design-token-foundation/01-VALIDATION.md (Wave 0 Requirements, Manual-Only Verifications)
  </read_first>
  <action>
    Create `.planning/phases/01-design-token-foundation/01-route-audit-checklist.md` as a markdown checklist. For each of the 13 route templates listed in RESEARCH.md's "DSGN-05: Full Route Inventory" table (`/`, `/{category}/`, `/{category}/{slug}/`, `/dau-tu/`, `/dau-tu/{category}/`, `/dau-tu/{category}/{slug}/`, `/kien-thuc/{slug}/`, `/search/`, `/about/`, `/contact/`, `/editorial-policy/`, `/corrections-policy/`, `/sources-policy/`), create a checkbox section with one representative instance per template (use real existing slugs — e.g. pick one article from `src/content/articles/` for `[category]/[slug]` and one Đầu tư-group article for `dau-tu/[category]/[slug]`, one category from each tree). Under each route, add 4 unchecked checkboxes for viewport widths 375px, 768px, 1024px, 1440px, plus a checkbox for "Vietnamese diacritics render correctly (test string: 'Phân tích cơ bản: Định giá cổ phiếu')" on at least the article-page entries, and a checkbox for "`.eyebrow` text legible (gold-700, not gold-500)" on pages containing `.eyebrow`. Add a final top-level checkbox: "Article column width (`--article: 65ch`) measured at 1280px+ lands in 680-760px range (Pitfall 3)". Include a header noting this checklist is walked manually via `npm run preview` as part of Plan 03's DSGN-05 sign-off.
  </action>
  <acceptance_criteria>
    - File contains 13 distinct route-template sections matching the RESEARCH.md route inventory table
    - Each section has at least 4 viewport checkboxes (375/768/1024/1440)
    - File contains the Vietnamese test string "Phân tích cơ bản: Định giá cổ phiếu"
    - File contains a checkbox referencing `--article` / 65ch / 680-760px range
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^\s*- \[ \]" .planning/phases/01-design-token-foundation/01-route-audit-checklist.md | awk '{exit ($1 < 50) ? 1 : 0}'</automated>
  </verify>
  <done>01-route-audit-checklist.md exists with 13 route-template sections, viewport checkboxes, Vietnamese diacritic test, and the --article width check, ready for Plan 03 to walk.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 02)

- `src/layouts/BaseLayout.astro` — modified: Google Fonts `<link>` (lines ~55-61) now loads Source Serif 4 + Inter at 400/500/700
- `.planning/phases/01-design-token-foundation/01-route-audit-checklist.md` — new file: 13-route-template manual audit checklist for DSGN-05

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser <- fonts.googleapis.com / fonts.gstatic.com | Static `<link>` tags fetch font CSS/woff2 from Google's CDN at render time — same trust boundary as the existing DM Serif Display/DM Sans setup, no new boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Tampering/Spoofing | BaseLayout.astro Google Fonts `<link>` | accept | Identical pattern to pre-existing font loading (already in production); Google Fonts CDN is the established, trusted source; no SRI used currently and adding it is out of scope for this phase |
| T-01-03 | Information Disclosure | BaseLayout.astro Google Fonts `<link>` | accept | Loading Google Fonts discloses visitor IP to Google, same as before this change — no new disclosure introduced, pre-existing acceptance carries forward |

This phase introduces no new external dependencies beyond the existing Google Fonts CDN pattern (font family names change only). ASVS L1 categories not applicable to this static markup edit.
</threat_model>

<verification>
1. `npx astro check` passes with no new errors
2. `grep` confirms BaseLayout.astro href contains `Source+Serif+4` and `Inter`, and no longer contains `DM+Serif+Display`/`DM+Sans`
3. `01-route-audit-checklist.md` exists and contains all 13 route sections
</verification>

<success_criteria>
- BaseLayout.astro loads Source Serif 4 + Inter (400/500/700) via Google Fonts, DM Serif Display/DM Sans fully removed
- Route audit checklist file exists, ready for Plan 03's DSGN-05 manual walk
- `npx astro check` passes
</success_criteria>

<output>
Create `.planning/phases/01-design-token-foundation/01-02-SUMMARY.md` when done
</output>
