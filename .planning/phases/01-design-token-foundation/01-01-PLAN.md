---
phase: 01-design-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/tokens/colors.css
  - src/styles/tokens/typography.css
  - src/styles/tokens/spacing.css
  - src/styles/tokens/effects.css
  - src/styles/tokens/aliases-legacy.css
  - src/styles/global.css
autonomous: true
requirements: [DSGN-01, DSGN-02, DSGN-03]

must_haves:
  truths:
    - "Site builds successfully with `npm run build` after the token restructure"
    - "All 37 pre-existing CSS custom property names referenced in src/components/*.astro, src/layouts/*.astro and global.css's own rules still resolve to valid values"
    - "Article body (.prose p, .prose li) renders with line-height 1.7 and font-size driven by --text-body"
    - "Headings h1/h2/h3 use the new fluid clamp() scale via --text-h1/h2/h3"
    - "--article token is 65ch (DSGN-03 width target)"
  artifacts:
    - path: "src/styles/tokens/colors.css"
      provides: "Navy/neutral/gold primitive scales (50-900) + new semantic color tokens (--bg, --surface, --surface-alt, --text, --muted, --line, --brand, --brand-strong, --accent)"
      contains: "--color-navy-800"
    - path: "src/styles/tokens/typography.css"
      provides: "--font-serif/--font-sans, fluid clamp() type scale (--text-h1/h2/h3/body), --leading-body, --article, --max"
      contains: "--text-body"
    - path: "src/styles/tokens/spacing.css"
      provides: "--space-1 through --space-24, values unchanged"
      contains: "--space-24"
    - path: "src/styles/tokens/effects.css"
      provides: "--radius-*, --shadow-* (unchanged), --transition-fast/base/slow"
      contains: "--transition-base"
    - path: "src/styles/tokens/aliases-legacy.css"
      provides: "Legacy-only primitive name mappings (--color-brand-*, --color-neutral-950/0, --color-accent*, --color-green-*, --transition)"
      contains: "--color-brand-900"
    - path: "src/styles/global.css"
      provides: "Imports the 5 token files in correct order, then existing reset/component CSS with targeted token-driven edits"
      contains: "@import \"./tokens/aliases-legacy.css\""
  key_links:
    - from: "src/styles/global.css"
      to: "src/styles/tokens/colors.css"
      via: "@import"
      pattern: "@import \"\\./tokens/colors\\.css\""
    - from: "src/styles/global.css"
      to: "src/styles/tokens/aliases-legacy.css"
      via: "@import (last token import, after colors/typography/spacing/effects)"
      pattern: "@import \"\\./tokens/aliases-legacy\\.css\""
    - from: ".prose p, .prose li"
      to: "--leading-body"
      via: "line-height: var(--leading-body)"
      pattern: "line-height:\\s*var\\(--leading-body\\)"
---

<objective>
Build the new layered design token system (colors, typography, spacing, effects, legacy aliases) under `src/styles/tokens/`, and restructure `src/styles/global.css` to import these tokens in place of its current monolithic `:root` block — while preserving every pre-existing CSS custom property name via `aliases-legacy.css` so no component breaks (DSGN-01, DSGN-02). Apply the new fluid type scale and 1.7 line-height / 65ch article width to the existing `.prose`/heading rules in `global.css` (DSGN-03).

Purpose: This is the foundational re-skin of the entire site — navy/gold/tinted-neutral palette replaces green, Source Serif 4 + Inter fonts are wired in (font `<link>` itself is Plan 02), and long-form article reading becomes materially more comfortable, all without touching any component markup.

Output: 5 new files under `src/styles/tokens/`, restructured `src/styles/global.css` with `@import` chain + targeted rule edits.
</objective>

<execution_context>
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/workflows/execute-plan.md
@/Users/nguyenvietloc/Documents/investing/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-token-foundation/01-CONTEXT.md
@.planning/phases/01-design-token-foundation/01-UI-SPEC.md
@.planning/phases/01-design-token-foundation/01-PATTERNS.md
@.planning/phases/01-design-token-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the 5 token files under src/styles/tokens/</name>
  <files>
    src/styles/tokens/colors.css
    src/styles/tokens/typography.css
    src/styles/tokens/spacing.css
    src/styles/tokens/effects.css
    src/styles/tokens/aliases-legacy.css
  </files>
  <read_first>
    src/styles/global.css (current monolithic :root block, lines 1-64 — source of every value/name being relocated)
    .planning/phases/01-design-token-foundation/01-UI-SPEC.md (Color Tokens, Typography, Spacing Scale, Effects sections — literal values to write, locked)
    .planning/phases/01-design-token-foundation/01-PATTERNS.md (per-file pattern assignments, exact code blocks for colors.css/typography.css/spacing.css/effects.css/aliases-legacy.css)
    .planning/phases/01-design-token-foundation/01-RESEARCH.md ("Full alias map (DSGN-02)" — full 37-name checklist; Pitfall 1 on duplicate :root declarations)
  </read_first>
  <action>
    Create `src/styles/tokens/colors.css` with a single `:root { ... }` block containing `color-scheme: light;` plus the full primitive scales from UI-SPEC "Color Tokens": `--color-navy-50` through `--color-navy-900` (10 steps, hex values from UI-SPEC table), `--color-neutral-50` through `--color-neutral-900` (10 steps), `--color-gold-50` through `--color-gold-900` (7 steps: 50,100,300,500,600,700,900 — UI-SPEC does not define 200/400/800 for gold, do not invent them). Then define the new semantic tokens exactly once: `--bg: var(--color-neutral-50)`, `--surface: #ffffff`, `--surface-alt: var(--color-navy-50)`, `--text: var(--color-neutral-900)`, `--muted: var(--color-neutral-700)`, `--line: var(--color-neutral-100)`, `--brand: var(--color-navy-800)`, `--brand-strong: var(--color-navy-600)`, `--accent: var(--color-gold-700)`. Also define `--destructive: #dc2626` per UI-SPEC (reserved, not wired this phase).

    Create `src/styles/tokens/typography.css` with `:root { --font-serif: 'Source Serif 4', Georgia, serif; --font-sans: 'Inter', ui-sans-serif, system-ui, -apple-system, sans-serif; --text-h1: clamp(2.25rem, 1.8rem + 2.25vw, 3.5rem); --text-h2: clamp(1.5rem, 1.3rem + 1vw, 2.25rem); --text-h3: clamp(1.15rem, 1.05rem + 0.5vw, 1.5rem); --text-body: clamp(1rem, 0.95rem + 0.25vw, 1.125rem); --leading-body: 1.7; --max: 1120px; --article: 65ch; }`.

    Create `src/styles/tokens/spacing.css` with `:root { --space-1: 0.25rem; --space-2: 0.5rem; --space-3: 0.75rem; --space-4: 1rem; --space-6: 1.5rem; --space-8: 2rem; --space-12: 3rem; --space-16: 4rem; --space-24: 6rem; }` — values copied verbatim from current global.css, no changes (D-11).

    Create `src/styles/tokens/effects.css` with `:root { --radius-sm: 4px; --radius-md: 8px; --radius-lg: 12px; --radius-xl: 20px; --shadow-sm: 0 1px 3px rgba(10, 37, 64, .08); --shadow-md: 0 4px 12px rgba(10, 37, 64, .10); --shadow-lg: 0 8px 24px rgba(10, 37, 64, .12); --transition-fast: 100ms ease; --transition-base: 200ms ease; --transition-slow: 350ms ease; }` — radius/shadow values unchanged from current global.css (D-13); `--transition` (singular legacy name) is NOT declared here.

    Create `src/styles/tokens/aliases-legacy.css` with `:root { --color-brand-900: var(--color-navy-800); --color-brand-700: var(--color-navy-600); --color-brand-100: var(--color-navy-50); --color-neutral-950: var(--color-neutral-900); --color-neutral-0: #ffffff; --color-accent: var(--color-gold-700); --color-accent-soft: var(--color-gold-50); --color-green-700: #006b2c; --color-green-50: #eafaf0; --transition: var(--transition-base); }`. Do NOT redeclare `--color-neutral-700`, `--color-neutral-400`, or `--color-neutral-100` here — these names already exist as new primitives in `colors.css` with the same hex values (`#475569`, `#94a3b8`, `#eef1f5`), so redeclaring would violate the "declare exactly once" rule (Pitfall 1) without changing any value.
  </action>
  <acceptance_criteria>
    - `src/styles/tokens/colors.css` contains `--color-navy-800: #163050`, `--color-gold-700: #92660a`, `--color-neutral-900: #1a2330`, `--brand: var(--color-navy-800)`, `--accent: var(--color-gold-700)`
    - `src/styles/tokens/typography.css` contains `--text-body: clamp(1rem, 0.95rem + 0.25vw, 1.125rem)`, `--leading-body: 1.7`, `--article: 65ch`
    - `src/styles/tokens/spacing.css` contains all 9 `--space-*` tokens (1,2,3,4,6,8,12,16,24) with values identical to current global.css lines 37-45
    - `src/styles/tokens/effects.css` contains `--transition-fast`, `--transition-base: 200ms ease`, `--transition-slow`, and does NOT contain a bare `--transition:` declaration
    - `src/styles/tokens/aliases-legacy.css` contains `--color-brand-900: var(--color-navy-800)`, `--color-neutral-950: var(--color-neutral-900)`, `--transition: var(--transition-base)`, `--color-green-700: #006b2c`
    - `src/styles/tokens/aliases-legacy.css` does NOT contain `--color-neutral-700:`, `--color-neutral-400:`, or `--color-neutral-100:` declarations
  </acceptance_criteria>
  <verify>
    <automated>grep -l "color-navy-800" src/styles/tokens/colors.css && grep -l "transition-base" src/styles/tokens/effects.css && grep -c "transition:" src/styles/tokens/effects.css | grep -q '^0$'</automated>
  </verify>
  <done>All 5 token files exist with the documented :root declarations, each new semantic name declared exactly once across the 5 files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restructure global.css to import tokens and apply type-scale edits</name>
  <files>src/styles/global.css</files>
  <read_first>
    src/styles/global.css (current full file — being restructured)
    .planning/phases/01-design-token-foundation/01-PATTERNS.md (global.css "modified — restructure" section: new top-of-file pattern + 4 targeted edits)
    .planning/phases/01-design-token-foundation/01-UI-SPEC.md (Implementation Checklist items 6, 8 — exact targeted edits)
  </read_first>
  <action>
    Replace lines 1-64 of `src/styles/global.css` (the entire current `:root { ... }` block including the "Design Tokens" comment header) with five `@import` statements in this exact order: `@import "./tokens/colors.css";`, `@import "./tokens/typography.css";`, `@import "./tokens/spacing.css";`, `@import "./tokens/effects.css";`, `@import "./tokens/aliases-legacy.css";`. Keep the `/* ─── Reset ─────... */` comment and everything from the current line 66 (`* { box-sizing: border-box; }`) onward UNCHANGED, except for the 4 targeted edits below.

    Targeted edit 1 — `.mega-item-title` rule (currently `font-size: 0.9rem; font-weight: 600; color: var(--brand);`): change `font-weight: 600;` to `font-weight: 500;` (D-08, only 400/500/700 allowed).

    Targeted edit 2 — heading font-size declarations: in the `h1 { ... }` rule, replace `font-size: clamp(2.2rem, 5.5vw, 4.5rem);` with `font-size: var(--text-h1);`. In the `h2 { ... }` rule, replace `font-size: clamp(1.5rem, 3vw, 2.2rem);` with `font-size: var(--text-h2);`. In the `h3 { ... }` rule, replace `font-size: 1.15rem;` with `font-size: var(--text-h3);`. Keep all other properties (`margin`, etc.) in each rule unchanged.

    Targeted edit 3 — `.prose` rule (currently `font-size: 1.05rem; padding-bottom: 56px;`): replace `font-size: 1.05rem;` with `font-size: var(--text-body);`. Keep `padding-bottom: 56px;` unchanged.

    Targeted edit 4 — `.prose p, .prose li` rule (currently `color: #1a2330; line-height: 1.8;`): replace `color: #1a2330;` with `color: var(--text);` and replace `line-height: 1.8;` with `line-height: var(--leading-body);`.

    Do not modify any other rule in the file. All `var(--brand)`, `var(--surface)`, `var(--line)`, `var(--muted)`, `var(--transition)`, `var(--color-brand-100)`, `var(--color-accent)`, `var(--space-*)`, `var(--radius-*)`, `var(--shadow-*)`, `var(--max)`, `var(--article)`, `var(--font-serif)`, `var(--font-sans)` references throughout the rest of the file (lines 66-509) remain exactly as-is — they resolve through the new token/alias layer.
  </action>
  <acceptance_criteria>
    - `src/styles/global.css` lines 1-5 are the 5 `@import` statements in the documented order, with `aliases-legacy.css` last
    - File does NOT contain a `:root {` block defining `--color-brand-900`, `--color-neutral-950`, etc. (that block is fully removed/relocated)
    - `.mega-item-title` rule contains `font-weight: 500;` and not `font-weight: 600;`
    - `h1`, `h2`, `h3` rules use `var(--text-h1)`, `var(--text-h2)`, `var(--text-h3)` respectively for `font-size`
    - `.prose` rule uses `font-size: var(--text-body);`
    - `.prose p, .prose li` rule uses `line-height: var(--leading-body);` and `color: var(--text);`
    - `npm run build` completes successfully (astro check + astro build, no errors)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/nguyenvietloc/Documents/investing && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>global.css imports the 5 token files in order, all 4 targeted edits applied, npm run build succeeds with no errors.</done>
</task>

</tasks>

## Artifacts this phase produces (Plan 01)

- `src/styles/tokens/colors.css` — new file: navy/neutral/gold primitive scales (50-900) + new semantic tokens (`--bg`, `--surface`, `--surface-alt`, `--text`, `--muted`, `--line`, `--brand`, `--brand-strong`, `--accent`, `--destructive`)
- `src/styles/tokens/typography.css` — new file: `--font-serif`, `--font-sans`, `--text-h1`, `--text-h2`, `--text-h3`, `--text-body`, `--leading-body`, `--max`, `--article`
- `src/styles/tokens/spacing.css` — new file: `--space-1` through `--space-24`
- `src/styles/tokens/effects.css` — new file: `--radius-sm/md/lg/xl`, `--shadow-sm/md/lg`, `--transition-fast/base/slow`
- `src/styles/tokens/aliases-legacy.css` — new file: `--color-brand-900/700/100`, `--color-neutral-950`, `--color-neutral-0`, `--color-accent`, `--color-accent-soft`, `--color-green-700/50`, `--transition`
- `src/styles/global.css` — modified: top-of-file `@import` chain (5 imports), `.mega-item-title` font-weight 500, `h1/h2/h3` font-size via `--text-h1/h2/h3`, `.prose` font-size via `--text-body`, `.prose p, .prose li` line-height/color via `--leading-body`/`--text`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This phase modifies only build-time CSS custom property files in a static Astro site. No user input, no server-side logic, no authentication, no external API calls, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | src/styles/tokens/*.css, src/styles/global.css | accept | Static CSS files compiled at build time into `dist/`; no runtime injection point, no user-controllable input reaches these files |
| T-01-SC | Tampering | npm/package installs | accept | Phase introduces zero new npm/pip/cargo packages (per RESEARCH.md Package Legitimacy Audit: "Not applicable") — no install tasks, no legitimacy checkpoint required |

This phase is a CSS-token-only refactor with no server-side logic, no user input handling, and no new dependencies. ASVS Level 1 categories (input validation, auth, session management, etc.) are not applicable. Threat model is documented as N/A per the security_threat_model_requirement, with the two threats above noted purely for completeness.
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` succeeds (astro check + astro build, no TypeScript/schema errors)
2. `grep -r "var(--brand)" src/components/*.astro src/layouts/*.astro` still returns matches (component code unchanged) and resolves correctly via the new `--brand: var(--color-navy-800)` chain
3. Open `npm run preview` on a sample article page and confirm `.prose p` line-height visually matches ~1.7 (looser than before) and headings render in the new fluid sizes
</verification>

<success_criteria>
- All 5 token files exist under `src/styles/tokens/` with the documented tokens
- `src/styles/global.css` imports them in the documented order and contains the 4 targeted edits
- `npm run build` completes with zero errors
- No `.astro` component/layout file other than (in Plan 02) `BaseLayout.astro`'s font `<link>` is touched
</success_criteria>

<output>
Create `.planning/phases/01-design-token-foundation/01-01-SUMMARY.md` when done
</output>
